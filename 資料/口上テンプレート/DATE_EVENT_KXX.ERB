;------------------------------------
;そのキャラのデートイベント本体を呼び出すハブとなる関数
;------------------------------------
@DATE_EVENT_KXX(場所)
#DIM 場所
#DIM 発生フラグ
発生フラグ = 0

;場所はSHOP_DATEを参考にしてね
SELECTCASE 場所
	CASE 0
		;デート
		CALL DATE_EVENT_KXX_NAMAE_OF_DATE_EVENT(場所)
	CASEELSE
ENDSELECT

;------------------------------------
;デートイベント本体
;創意工夫
;-----------------------------------
;DATE_EVENT_Kの部分はプレフィクスとするので、必ずつけるようお願いします
;NAMAE_OF_DATE_EVENTの部分を適宜変更してくだち
@DATE_EVENT_KXX_NAMAE_OF_DATE_EVENT(場所)
#DIM 場所
#DIM キャラ名

;渡した名前に対応するキャラ番号を拾ってくるNAME_TO_CHARA
;なお、NAME_TO_CHARAに渡す名前は正確にはCSTR:99です
;たいていはキャラの名前です（苗字ではない）　CSVを参照してね
;例：NAME_TO_CHARA("霊夢") NAME_TO_CHARA("マエリベリー")

キャラ名 = NAME_TO_CHARA("キャラ名")

;発生判定をしてくれる便利な関数 CHECK_KOJO_DAILY_HAPPEN
;本来は口上デイリーに使うけど、デートでも確率判定に利用できます
;ARG:0 キャラ名
;ARG:1 確率(1000分率 75なら7.5%に相当する)
;ARG:2 同勢力か		(1 同勢力か	0 別勢力		-1ないし未指定 問わない)
;ARG:3 捕虜か			(1 捕虜		0 捕虜でない	-1ないし未指定 問わない)
;ARG:4 面識があるか	(1 同勢力		0 別勢力		-1ないし未指定 問わない)

;この関数が0を返したならば（イベントが発生しないなら) -1を返す
SIF !CHECK_KOJO_DAILY_HAPPEN(キャラ名, 50, 1, 0, 1)
	RETURN -1

;その他必要そうな判定はこのへん

;このへんに本文
