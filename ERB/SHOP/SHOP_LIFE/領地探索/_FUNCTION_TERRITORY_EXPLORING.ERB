@REGISTER_TERRITORY_EVENT(分類, 英名, 名前)
#DIM 分類
#DIMS 英名
#DIMS 名前

TRYCCALLFORM TERRITORY_EVENT_%英名%_EXISTS()
CATCH
	THROW 指定した領地探索イベント%名前%, %英名%のEXISTS関数が存在しません
ENDCATCH

TERRITORY_EVENT_NAME:分類:(TERRITORY_EVENT_NUM:分類) '= 名前
TERRITORY_EVENT_NAME_ENG:分類:(TERRITORY_EVENT_NUM:分類) '= 英名

TERRITORY_EVENT_NUM:分類 ++


@PRINT_ABILITY(対象, 能力番号)
#DIM 対象
#DIM 能力番号

PRINTFORM %ANAME(対象)%の%ABLNAME:能力番号%:

IF GROUPMATCH(能力番号, GETNUM(ABL, "武闘"), GETNUM(ABL, "防衛"), GETNUM(ABL, "知略"), GETNUM(ABL, "政治"), GETNUM(ABL, "妖術"), GETNUM(ABL, "料理"), GETNUM(ABL, "歌唱"))
	LOCAL = ランク_ＳＬＧ
ELSE
	LOCAL = ランク_その他
ENDIF

CALL PRINT_ALPHABET_RANK(LOCAL, ABL:対象:能力番号)

PRINTFORML {ABL:対象:能力番号}