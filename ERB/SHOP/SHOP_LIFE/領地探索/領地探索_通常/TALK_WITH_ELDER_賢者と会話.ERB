;-------------------
;存在判定
;なにもさせないこと
;-------------------
@TERRITORY_EVENT_TALK_WITH_ELDER_EXISTS()
#DIM 対象
RETURN 1

;-------------------
;発生判定
;-------------------
@TERRITORY_EVENT_TALK_WITH_ELDER_DECISION(対象)
#DIM 対象
RETURN 1

;-------------------
;本体
;-------------------
@TERRITORY_EVENT_TALK_WITH_ELDER(対象)
#DIM 対象
PRINTFORML 領内を探索中、とある賢者の評判を聞いた
PRINTFORML 気になった%ANAME(対象)%は会ってみることにした
PRINTFORMW 賢者は一見普通の好々爺に見えた
IF LIMIT(ABL:対象:政治, 60, 100) > RAND:100
	PRINTFORML しかしいくつか言葉を交わすうちに非常に思慮深いとわかった
	PRINTFORMW %ANAME(対象)%は夜通し彼と語り合った
	CALL PRINT_ADD_EXP(対象, "政治経験値", RAND:6 + 3, 1)
ELSE
	PRINTFORML いくつか言葉を交わしたが、とぼけた返事ばかりでろくに話にならなかった
	PRINTFORMW 評判など当てにならない。がっかりしながら賢者の家を後にした
ENDIF
RETURN 1
