;-------------------------------------------------
;「服を替える」の選択可否
;-------------------------------------------------
;@SHOP_LIFE_CHECK63
;SIF CFLAG:MASTER:捕虜先
;	RETURN 0
;RETURN 1

;-------------------------------------------------
;「服を替える」の選択可能キャラ存在判定
;-------------------------------------------------
;@SHOP_LIFE_CHECKCHARA63(ARG:0)
;非主人公(主人公は別処理)、同一勢力or自国の捕虜
;RETURN ARG:0 != MASTER && (CFLAG:(ARG:0):所属 == CFLAG:MASTER:所属 || CFLAG:(ARG:0):捕虜先 == CFLAG:MASTER:所属) && !IS_ANIMAL(ARG:0)

;-------------------------------------------------
;「服を替える」の右カラム表示処理
;-------------------------------------------------
;@SHOP_LIFE_EVENTBUY_SHOW63
;CALL COLUMN_RIGHT_TITLE("対象者選択, "", "", "", "", "")
;CALL PRINTBUTTON_SHOP(6, "[能力一覧表示]", 20)
;CALL COLUMN_RIGHT_PRINTL
;CALL COLUMN_RIGHT_LINE
;CALL COLUMN_RIGHT_PRINTL
;CALL PRINT_PARTNER_DATA(MASTER, 0, 0, SHOP_LIFE_LIST1_ADD_INPUT, 1, 1)
;CALL COLUMN_RIGHT_PRINTL
;CALL COLUMN_RIGHT_CHARALIST
;RETURN 0

;-------------------------------------------------
;「服を替える」の左カラムメニューの入力処理
;-------------------------------------------------
;@SHOP_LIFE_EVENTBUY63
;FLAG:拠点フェイズページ = 1
;RETURN 0

