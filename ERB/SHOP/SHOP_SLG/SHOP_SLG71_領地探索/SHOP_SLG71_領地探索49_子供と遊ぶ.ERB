;-------------------
;●子供と遊ぶ
;-------------------
@REGISTER_TERRITORY_EVENT49(対象)
#DIM 対象
RETURN 1

@TERRITORY_EVENT49(対象)
#DIM 対象
PRINTFORML 探索中、遊んでる子供達に出会った
PRINTFORML 見学していると、ねだられて%ANAME(対象)%も参加させられた
PRINTFORML …結局一日中付き合う事になった
PRINTFORMW 子供達の遊びに付き合い%ANAME(対象)%は心が穏やかになった
IF 対象 == MASTER
	MAXBASE:対象:気力 += 100
	CALL COLORPRINT(@"気力が100上がって、{MAXBASE:対象:気力}になった", カラー_注意, "W")
ELSE
	LOCAL:0 = 10
	SIF (ABL:対象:野心) < 10
		LOCAL = (ABL:対象:野心)
	ABL:対象:野心 -= LOCAL:0
	CALL COLORPRINT(@"%ANAME(対象)%の野心が{LOCAL:0}減少し、{ABL:対象:野心}になった", カラー_注意, "W")
ENDIF
RETURN 1

