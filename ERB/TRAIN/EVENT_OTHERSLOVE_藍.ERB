;-------------------------------------------------
;キャラARG:0がタチ役と懇意になる条件を満たしているかどうかを判定する関数
;ARG:1=タチ役のキャラ番号
;-------------------------------------------------
;@IS_LOVE_FOR_RAN(ARG:0, ARG:1)
;#FUNCTION
;;<除外> 主人公、タチ役本人、霖之助、男性、幼児
;IF ARG:0 != MASTER && ARG:0 != ARG:1 && CFLAG:(ARG:0):1 == CFLAG:(ARG:1):1 && IS_FEMALE(ARG:0) && !TALENT:(ARG:0):幼児
;	;<除外> タチ役の子供、親愛、隷属、烙印
;	IF CFLAG:(ARG:0):24 != GET_ID(ARG:1) && !TALENT:(ARG:0):親愛 && !TALENT:(ARG:0):隷属 && !TALENT:(ARG:0):烙印
;		;タチ役になびくオプションがONなら恋慕・恋人・服従も対象に
;		IF CONFIG:72 == 1 || (!TALENT:(ARG:0):恋慕 && !TALENT:(ARG:0):恋人 && !TALENT:(ARG:0):服従)
;			;タチ役と同一勢力で捕虜でない
;			IF CFLAG:(ARG:0):1 == CFLAG:(ARG:1):1 && CFLAG:(ARG:0):9 == 0
;				;霖之助・タチ役が手をつける範囲の設定  0=無制限、1=主人公と面識、2=恋慕・服従
;				IF CONFIG:83 == 0
;					RETURNF 1
;				ELSEIF CONFIG:83 == 1 && CFLAG:(ARG:0):17 == 1
;					RETURNF 1
;				ELSEIF CONFIG:82 == 2 && (TALENT:(ARG:0):恋慕 || TALENT:(ARG:0):服従 || TALENT:(ARG:0):藍に恋慕 || TALENT:(ARG:0):藍の恋人)
;					RETURNF 1
;				ENDIF
;			ENDIF
;		ENDIF
;	ENDIF
;ENDIF
;RETURNF 0
;
;
;;-------------------------------------------------
;;キャラARG:0が外道タチ役に摘み食いされる条件を満たしているかどうかを判定する関数
;;ARG:1=タチ役のキャラ番号
;;-------------------------------------------------
;@IS_LOVE_FOR_RAN_B(ARG:0, ARG:1)
;#FUNCTION
;;<除外> 主人公、タチ役本人、霖之助、男性、幼児
;IF ARG:0 != MASTER && ARG:0 != ARG:1 && CFLAG:(ARG:0):1 == CFLAG:(ARG:1):1 && IS_FEMALE(ARG:0) && !TALENT:(ARG:0):幼児
;	LOCAL:2 = 0
;	;条件1:タチ役と同一勢力で捕虜でない
;	IF CFLAG:(ARG:0):1 == CFLAG:(ARG:1):1 && CFLAG:(ARG:0):9 == 0
;		LOCAL:2 = 1
;	;条件2:タチ役勢力の捕虜
;	ELSEIF CFLAG:(ARG:0):9 == CFLAG:(ARG:1):1
;		LOCAL:2 = 1
;	ENDIF
;	;どちらかの条件を満たした場合
;	IF LOCAL:2
;		;暴君タチ役がつまみ食いする範囲の設定  0=無制限、1=主人公と面識、2=恋慕・服従
;		IF CONFIG:84 == 0
;			RETURNF 1
;		ELSEIF CONFIG:84 == 1 && CFLAG:(ARG:0):17 == 1
;			RETURNF 1
;		ELSEIF CONFIG:84 == 2 && (TALENT:(ARG:0):恋慕 || TALENT:(ARG:0):服従 || TALENT:(ARG:0):藍に恋慕 || TALENT:(ARG:0):藍の恋人)
;			RETURNF 1
;		ENDIF
;	ENDIF
;ENDIF
;RETURNF 0
;
;
;
;;-------------------------------------------------
;;タチ役関係のイベント処理
;;-------------------------------------------------
;@EVENT_OTHERSLOVE_RAN
;;このターンタチ役に抱かれる条件を満たしたキャラのリストと数
;#DIM LIST_ABLE, 1000
;#DIM NUM_ABLE
;
;;このターンタチ役に抱かれたキャラのリストと数
;#DIM LIST_SEX, 1000
;#DIM NUM_SEX
;
;;このターンタチ役に抱かれたキャラのリストと数([恋人]持ち限定)
;#DIM LIST_SEX_LOVER, 1000
;#DIM NUM_SEX_LOVER
;
;NUM_ABLE = 0
;NUM_SEX = 0
;NUM_SEX_LOVER = 0
;
;;別れさせフラグが立っていれば別れる
;FOR LOCAL:0, 0, CHARANUM
;	;同一勢力でなくてもメッセージを表示するように変更
;	IF CFLAG:(LOCAL:0):39 && TALENT:(LOCAL:0):藍の恋人
;			PRINTL 
;			PRINTFORMW %ANAME(LOCAL:0)%は%ANAME(CONFIG:88)%との恋人関係を解消したようです…
;			TALENT:(LOCAL:0):藍の恋人 = 0
;			CFLAG:(LOCAL:0):39 = 0
;	ENDIF
;NEXT
;
;;タチ役が不在or主人公or捕虜なら戻る
;;タチ役をCONFIG:88で設定
;LOCAL:5 = CONFIG:88
;IF LOCAL:5 < 0 || LOCAL:5 == MASTER || CFLAG:(LOCAL:5):1 == 0 || CFLAG:(LOCAL:5):9 != 0
;	RETURN
;ENDIF
;
;;コンフィグがOFFなら戻る
;IF !CONFIG:71
;	RETURN
;ENDIF
;
;;タチ役が浮気禁止中なら他の士官に手を出さない
;IF CFLAG:(LOCAL:5):63
;	RETURN
;ENDIF
;
;;現在のメインターゲットが未設定or十分好感度が高いならターゲットを再設定
;LOCAL:6 = ID_TO_CHARA(FLAG:16)
;IF LOCAL:6 < 0 || CFLAG:(LOCAL:6):38 >= 1500 || !IS_LOVE_FOR_RAN(LOCAL:6, LOCAL:5)
;	;メインターゲットとなり得るキャラの数を調べる
;	LOCAL:7 = 0
;	FOR LOCAL:0, 0, CHARANUM
;		IF IS_LOVE_FOR_RAN(LOCAL:0, LOCAL:5) && CFLAG:(LOCAL:0):38 < 1500
;			LOCAL:7 ++
;		ENDIF
;	NEXT
;
;	IF LOCAL:7 >= 1
;		;ランダムにメインターゲットを設定
;		LOCAL:8 = RAND:(LOCAL:7)
;		FOR LOCAL:0, 0, CHARANUM
;			IF IS_LOVE_FOR_RAN(LOCAL:0, LOCAL:5) && CFLAG:(LOCAL:0):38 < 1500
;				LOCAL:8 --
;				IF LOCAL:8 < 0
;					FLAG:16 = GET_ID(LOCAL:0)
;					LOCAL:9 = 0
;					BREAK
;				ENDIF
;			ENDIF
;		NEXT
;	ELSE
;		;候補がいなければ単にメインターゲットを解除
;		FLAG:16 = 0
;	ENDIF
;ENDIF
;
;FOR LOCAL:0, 0, CHARANUM
;	IF IS_LOVE_FOR_RAN(LOCAL:0, LOCAL:5)
;		;メインターゲットなら大きく好感度が上昇
;		IF GET_ID(LOCAL:0) == FLAG:16
;			CFLAG:(LOCAL:0):38 += RAND:70 + 90
;		;主人公に恋慕or恋人なら常時好感度上昇
;		ELSEIF (TALENT:(LOCAL:0):恋慕 || TALENT:(LOCAL:0):恋人) && CFLAG:(LOCAL:0):38 < 2000
;			CFLAG:(LOCAL:0):38 += RAND:40 + 40
;		;それ以外なら微量ランダム
;		ELSEIF CFLAG:(LOCAL:0):38 < 800
;			CFLAG:(LOCAL:0):38 += RAND:30
;		ENDIF
;
;		CFLAG:(LOCAL:0):38 = MIN(CFLAG:(LOCAL:0):38, 5000)
;
;		LOCAL:10 = 0
;
;		;タチ役への惚れ度が一定量を超えたら警告を表示
;		IF CFLAG:(LOCAL:0):90 == 0 && CFLAG:(LOCAL:0):38 >= 500 && !TALENT:(LOCAL:0):藍に恋慕
;			SETCOLOR 0xFFFF99
;			IF TALENT:(LOCAL:0):恋慕 || TALENT:(LOCAL:0):恋人
;				IF CONFIG:82 >= 2
;					PRINTFORMW %ANAME(LOCAL:0)%は%ANAME(MASTER)%に構ってもらえず寂しさを感じているようです…
;				ENDIF
;			ELSE
;				IF CONFIG:82 == 2
;					PRINTFORMW %ANAME(LOCAL:0)%は%ANAME(LOCAL:5)%と接する機会が増えたようだ…
;				ENDIF
;			ENDIF
;			RESETCOLOR
;			CFLAG:(LOCAL:0):90 = 1
;
;		ELSEIF CFLAG:(LOCAL:0):90 == 1 && CFLAG:(LOCAL:0):38 >= 1000 && !TALENT:(LOCAL:0):藍に恋慕
;			SETCOLOR 0xFF4040
;			IF TALENT:(LOCAL:0):恋慕 || TALENT:(LOCAL:0):恋人
;				IF CONFIG:82 >= 2
;					PRINTFORMW %ANAME(LOCAL:0)%は最近よく%ANAME(LOCAL:5)%と親しげに話しているという噂です…
;				ENDIF
;			ELSE
;				IF CONFIG:82 == 2
;					PRINTFORMW %ANAME(LOCAL:0)%は%ANAME(LOCAL:5)%と親しげに会話をしている…
;				ENDIF
;			ENDIF
;			RESETCOLOR
;			CFLAG:(LOCAL:0):90 = 2
;
;		ELSEIF CFLAG:(LOCAL:0):38 >= 1500 && !TALENT:(LOCAL:0):藍に恋慕
;			IF IS_FEMALE(LOCAL:0)
;				;同一勢力でなくてもメッセージを表示するように
;				PRINTL 
;				CALL KOJO_EVENT(LOCAL:0, 410)
;				IF TALENT:(LOCAL:0):恋慕 || TALENT:(LOCAL:0):恋人
;					PRINTFORML %ANAME(MASTER)%になかなか構ってもらえない%ANAME(LOCAL:0)%は
;					PRINTFORMW %ANAME(MASTER)%ではなく%ANAME(LOCAL:5)%に心惹かれるようになってしまったようです…
;				ELSE
;					PRINTFORMW %ANAME(LOCAL:0)%が%ANAME(LOCAL:5)%に恋愛感情を抱いたようです…
;				ENDIF
;				TALENT:(LOCAL:0):藍に恋慕 = 1
;				TALENT:(LOCAL:0):恋慕 = 0
;			ENDIF
;		ELSEIF !TALENT:(LOCAL:0):恋人 && TALENT:(LOCAL:0):藍に恋慕 && !TALENT:(LOCAL:0):藍の恋人
;			IF RAND:4 == 0
;				;同一勢力でなくても(ry)
;				PRINTL 
;				CALL KOJO_EVENT(LOCAL:0, 411)
;				PRINTFORMW %ANAME(LOCAL:0)%が%ANAME(LOCAL:5)%の恋人になったようです…
;				LOCAL:10 = 1
;				TALENT:(LOCAL:0):藍の恋人 = 1
;			ENDIF
;		ENDIF
;
;		;タチ役に抱かれる条件を満たしているならリストに記録
;		IF CFLAG:(LOCAL:0):16 == 0 && (TALENT:(LOCAL:0):藍の恋人 || (TALENT:(LOCAL:0):藍に恋慕 && TALENT:(LOCAL:0):恋人 && CFLAG:(LOCAL:0):38 >= 2000))
;			LIST_ABLE:NUM_ABLE = LOCAL:0
;			NUM_ABLE ++
;		ENDIF
;	ENDIF
;NEXT
;
;;タチ役が君主＆好き勝手モードのときは1/3の確率で完全ランダムな士官をつまみ食いしてしまう
;IF CONFIG:71 == 2 && COUNTRY_BOSS:(CFLAG:(LOCAL:5):1) == GET_ID(LOCAL:5) && RAND:3 == 0
;	LOCAL:6 = -1
;	LOCAL:2 = 0
;	FOR LOCAL:0, 0, CHARANUM
;		IF IS_LOVE_FOR_RAN_B(LOCAL:0, LOCAL:5)
;			LOCAL:2 ++
;		ENDIF
;	NEXT
;	IF LOCAL:2 >= 1
;		LOCAL:3 = RAND:(LOCAL:2)
;		LOCAL:2 = 0
;		FOR LOCAL:0, 0, CHARANUM
;			IF IS_LOVE_FOR_RAN_B(LOCAL:0, LOCAL:5)
;				IF LOCAL:2 == LOCAL:3
;					LOCAL:6 = LOCAL:0
;					BREAK
;				ENDIF
;				LOCAL:2 ++
;			ENDIF
;		NEXT
;	ENDIF
;
;	IF LOCAL:6 >= 0
;		;キャラ番号を記録
;		LIST_SEX:NUM_SEX = LOCAL:6
;		NUM_SEX ++
;		IF TALENT:(LOCAL:6):恋人
;			LIST_SEX_LOVER:NUM_SEX_LOVER = LOCAL:6
;			NUM_SEX_LOVER ++
;		ENDIF
;	ENDIF
;
;ELSE
;	;タチ役による性交判定(1ターンあたり最大5人)
;	FOR LOCAL:0, 0, 5
;		IF NUM_ABLE <= 0
;			BREAK
;		ELSE
;			;抱くキャラをランダムに選択(ただし恋人持ちが高確率になるよう調整)
;			FOR LOCAL:1, 0, 10
;				LOCAL:9 = RAND:NUM_ABLE
;				LOCAL:6 = LIST_ABLE:(LOCAL:9)
;				IF TALENT:(LOCAL:6):恋人
;					BREAK
;				ENDIF
;			NEXT
;
;			;選択されたキャラをリストから削除
;			FOR LOCAL:1, LOCAL:9 + 1, NUM_ABLE
;				LIST_ABLE:(LOCAL:1 - 1) = LIST_ABLE:(LOCAL:1)
;			NEXT
;			NUM_ABLE --
;
;			;キャラ番号を記録
;			LIST_SEX:NUM_SEX = LOCAL:6
;			NUM_SEX ++
;			IF TALENT:(LOCAL:6):恋人
;				LIST_SEX_LOVER:NUM_SEX_LOVER = LOCAL:6
;				NUM_SEX_LOVER ++
;			ENDIF
;		ENDIF
;	NEXT
;ENDIF
;
;FOR LOCAL:0, 0, NUM_SEX
;	LOCAL:6 = LIST_SEX:(LOCAL:0)
;
;	;抱かれたキャラは好感度上昇
;	CFLAG:(LOCAL:6):38 = MIN(CFLAG:(LOCAL:6):38 + RAND:60 + 60, 5000)
;
;	LOCAL:4 = 0
;	IF TALENT:(LOCAL:6):藍に恋慕
;		;キスの共通処理
;		SELECTCASE RAND:3
;			CASE 0
;				CALL KISS_COMMON(LOCAL:5, @"%ANAME(LOCAL:6)%の唇")
;				CALL KISS_COMMON(LOCAL:6, @"%ANAME(LOCAL:5)%の唇")
;				LOCAL:4 = RAND:7 + 1
;			CASE 1
;				IF HAS_PENIS(LOCAL:6)
;					CALL KISS_COMMON(LOCAL:6, @"%ANAME(LOCAL:5)%のペニス")
;				ELSE
;					CALL KISS_COMMON(LOCAL:5, @"%ANAME(LOCAL:6)%の唇")
;					CALL KISS_COMMON(LOCAL:6, @"%ANAME(LOCAL:5)%の唇")
;				ENDIF
;			CASE 2
;				CALL KISS_COMMON(LOCAL:6, @"%ANAME(LOCAL:5)%の足")
;		ENDSELECT
;	ENDIF
;
;	IF VAGINA(LOCAL:6) && (TALENT:(LOCAL:6):藍に恋慕 || RAND:15 == 0)
;		CALL VIRGIN_COMMON(LOCAL:6, @"%ANAME(LOCAL:5)%")
;		EXP:(LOCAL:6):Ｖ感経験値 += (ABL:(LOCAL:6):Ｖ感 + 1) * (RAND:4 + 3)
;		IF HAS_PENIS(LOCAL:5)
;			CALL DOUTEI_COMMON(LOCAL:5, @"%ANAME(LOCAL:6)%の膣")
;		ENDIF
;	ENDIF
;
;	IF ABL:(LOCAL:5):主導度Ｕ < 800
;		ABL:(LOCAL:5):主導度Ｕ += 1
;	ENDIF
;	IF ABL:(LOCAL:6):主導度Ｕ > -500
;		ABL:(LOCAL:6):主導度Ｕ -= 10
;	ENDIF
;	EXP:(LOCAL:6):Ｃ感経験値 += (ABL:(LOCAL:6):Ｃ感 + 1) * (RAND:4 + 3)
;	EXP:(LOCAL:6):Ｂ感経験値 += (ABL:(LOCAL:6):Ｂ感 + 1) * (RAND:4 + 3)
;	EXP:(LOCAL:6):Ｍ感経験値 += (ABL:(LOCAL:6):Ｍ感 + 1) * (RAND:4 + 3)
;	IF RAND:5 == 0 || !VAGINA(LOCAL:6)
;		EXP:(LOCAL:6):Ａ感経験値 += (ABL:(LOCAL:6):Ａ感 + 1) * (RAND:7 + 6)
;	ENDIF
;	EXP:(LOCAL:5):Ｃ感経験値 += (ABL:(LOCAL:5):Ｃ感 + 1) * MAX(RAND:10 - 7, 0)
;	EXP:(LOCAL:5):Ｂ感経験値 += (ABL:(LOCAL:5):Ｂ感 + 1) * MAX(RAND:16 - 13, 0)
;	EXP:(LOCAL:5):Ｍ感経験値 += (ABL:(LOCAL:5):Ｍ感 + 1) * MAX(RAND:10 - 7, 0)
;	EXP:(LOCAL:6):奉仕経験値 += ABL:(LOCAL:6):奉仕 * (RAND:3)
;	EXP:(LOCAL:5):奉仕経験値 += ABL:(LOCAL:5):奉仕 * (RAND:7)
;	EXP:(LOCAL:6):欲望経験値 += ABL:(LOCAL:6):欲望 * (RAND:3 + 3)
;	EXP:(LOCAL:5):欲望経験値 += ABL:(LOCAL:5):欲望 * (RAND:2 + 1)
;	LOCAL:8 = MIN(ABL:(LOCAL:6):欲望 + 1, 11) * (RAND:21 + 20)
;	EXP:(LOCAL:6):性技経験値 += LOCAL:8 / 15
;	EXP:(LOCAL:5):性技経験値 += LOCAL:8 / 8
;	EXP:(LOCAL:6):性交経験値 += LOCAL:8 / 30
;	EXP:(LOCAL:5):性交経験値 += LOCAL:8 / 30
;	EXP:(LOCAL:6):絶頂経験 += LOCAL:8 / 8
;	EXP:(LOCAL:5):絶頂経験 += LOCAL:8 / 40
;	IF HAS_PENIS(LOCAL:6)
;		EXP:(LOCAL:6):射精経験値 += LOCAL:8 / 20
;		EXP:(LOCAL:5):精愛経験値 += LOCAL:8 / 20
;	ENDIF
;	IF HAS_PENIS(LOCAL:5)
;		EXP:(LOCAL:5):射精経験値 += LOCAL:8 / 20
;		EXP:(LOCAL:6):精愛経験値 += LOCAL:8 / 20
;	ENDIF
;	LOCAL:7 = RAND:4 + 1
;	EXP:(LOCAL:6):キス経験 += LOCAL:4
;	EXP:(LOCAL:5):キス経験 += LOCAL:4
;	EXP:(LOCAL:6):口淫経験 += RAND:3
;	EXP:(LOCAL:6):露出経験値 += RAND:4
;	CALL TRAIN_AUTO_ABLUP(LOCAL:5, 1)
;	CALL TRAIN_AUTO_ABLUP(LOCAL:6, 1)
;
;	;恋人になってから7期以上経過していて、タチ役に竿があれば妊娠判定
;	IF CONFIG:76 == 1 && CFLAG:(LOCAL:6):37 >= 7 && HAS_PENIS(LOCAL:5) && (VAGINA(LOCAL:6) || TALENT:(LOCAL:6):孕み可能)
;		;中出しと妊娠判定
;		FOR LOCAL:1, 440, 480
;			TCVAR:(LOCAL:6):(LOCAL:1) = 0
;		NEXT
;		CALL RECORD_INJECTION(LOCAL:6, GET_ID(LOCAL:5), 6)
;		CALL CHECK_PREGNANT(LOCAL:6)
;	ENDIF
;NEXT
;
;;主人公が食べられる確率の設定
;;★主人公が男でも食べられるように
;	LOCAL:0 = 0
;IF ABL:(LOCAL:5):主導度Ｕ >= 0
;	IF CFLAG:(LOCAL:5):2 >= 10000
;		LOCAL:0 = 100
;	ELSEIF CFLAG:(LOCAL:5):2 >= 1500
;		LOCAL:0 = 40
;	ELSEIF IS_FEMALE(MASTER)
;		LOCAL:0 = 20
;	ELSEIF CONFIG:79 == 2
;		LOCAL:0 = 20
;	ENDIF
;ENDIF
;
;;タチ役が君主＆好き勝手モードのときは主人公も食べられてしまう
;IF CONFIG:71 == 2 && (CONFIG:79 == 1 && IS_FEMALE(MASTER) || CONFIG:79 == 2) && COUNTRY_BOSS:(CFLAG:MASTER:1) == GET_ID(LOCAL:5) && CFLAG:MASTER:9 == 0 && RAND:100 < LOCAL:0
;	CALL SINGLE_DRAWLINE
;	;初めての場合
;	IF FLAG:45 == 0
;		FLAG:45 = 1
;
;		;タチ役の対面済みフラグをＯＮ
;		CFLAG:(LOCAL:5):17 = 1
;
;		PRINTFORMW その日の仕事が終わった後、%ANAME(MASTER)%は君主である%ANAME(LOCAL:5)%の私室に呼び出された
;		PRINTW ………………
;		PRINTFORMW %ANAME(MASTER)%が%ANAME(LOCAL:5)%の部屋に入ると、%ANAME(LOCAL:5)%は%ANAME(MASTER)%に寝台の上に仰向けで横になるように命令した
;		PRINTFORML %ANAME(MASTER)%が%ANAME(LOCAL:5)%の命令に従って寝台に寝そべると、
;		PRINTFORMW %ANAME(LOCAL:5)%はどこからか取り出した縄で瞬く間に%ANAME(MASTER)%の手足を縛り上げてしまった
;		PRINTFORML すっかり身動きが取れなくなった%ANAME(MASTER)%の上に、
;		PRINTFORMW 妖艶な表情で舌舐めずりをする%ANAME(LOCAL:5)%が覆い被さってくる…
;		PRINTFORM %ANAME(LOCAL:5)%は%ANAME(MASTER)%の服をめくり上げて、%ANAME(MASTER)%の
;		IF IS_FEMALE(MASTER)
;		IF TALENT:MASTER:巨乳 || TALENT:MASTER:爆乳
;			PRINT 豊満な
;		ELSEIF TALENT:MASTER:絶壁 || TALENT:MASTER:貧乳
;			PRINT 小ぶりな
;		ENDIF
;		PRINTFORMW 胸を揉みしだき始めた…
;		ELSE
;			PRINTFORMW 胸板をなぞり始めた…
;		ENDIF
;
;		CALL ASK_YN("や、止めて下さいっ！", @"ああ、そんな、困ります…%UNICODE(0x2764)%")
;		REDRAW 0
;		CLEARLINE 3
;
;		;主人公に竿がある場合
;		IF HAS_PENIS(MASTER)
;			IF RESULT == 0
;				PRINTL 
;				PRINTL →「や、止めて下さいっ！」
;				PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の抗議をまるで聞いていないかのように無視し、
;				PRINTFORMW 片方の手で服の上から%ANAME(MASTER)%のペニスをやわやわと揉みしたいだ
;			ELSE
;				FLAG:45 += 2
;				PRINTL 
;				PRINTFORML →「ああ、そんな、困ります…%UNICODE(0x2764)%」
;				PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の好意的な反応にクスリと妖艶な笑みを浮かべ、
;				PRINTFORMW 片方の手で服の上から%ANAME(MASTER)%のペニスをやわやわと揉みしだいた
;			ENDIF
;			REDRAW 1
;
;			PRINTFORML %ANAME(LOCAL:5)%の優しい指使いに、%ANAME(MASTER)%の服の中の男根は瞬く間にそそり勃ち、
;			PRINTFORMW それと同時に腰の奥からムラムラとした情欲が湧き上がる
;			PRINTFORMW だが、%ANAME(MASTER)%が気持ち良くなってきたところで、%ANAME(LOCAL:5)%は唐突に指を止めてしまった
;			PRINTFORML %ANAME(MASTER)%が物欲しそうな視線で%ANAME(LOCAL:5)%を見つめると、
;			PRINTFORMW %ANAME(LOCAL:5)%は、続きがして欲しければまた私の部屋に来なさい、と言って%ANAME(MASTER)%の縄を解いた
;			PRINTW ………………
;			PRINTFORML %ANAME(MASTER)%はすっかり勃起したペニスを隠すように前かがみになりながら、
;			PRINTFORMW 自分の部屋へと戻っていった…
;
;			PRINTFORML <%ANAME(MASTER)%>
;			CALL PRINT_ADD_EXP(MASTER, "Ｃ感経験値", 2 + ABL:MASTER:Ｃ感 * 2)
;			SIF IS_FEMALE(MASTER)
;				CALL PRINT_ADD_EXP(MASTER, "Ｂ感経験値", 10 + ABL:MASTER:Ｂ感 * 10)
;
;		;主人公に竿がない場合
;		ELSE
;			IF RESULT == 0
;				PRINTL 
;				PRINTL →「や、止めて下さいっ！」
;				PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の抗議をまるで聞いていないかのように無視し、
;				PRINTFORMW 片方の手を%ANAME(MASTER)%の下着の中に入れ、%ANAME(MASTER)%の秘所をなぞるように弄んできた
;			ELSE
;				FLAG:45 += 2
;				PRINTL 
;				PRINTFORML →「ああ、そんな、困ります…%UNICODE(0x2764)%」
;				PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の好意的な反応にクスリと妖艶な笑みを浮かべ、
;				PRINTFORMW 片方の手を%ANAME(MASTER)%の下着の中に入れ、%ANAME(MASTER)%の秘所をなぞるように弄んできた
;			ENDIF
;			REDRAW 1
;			IF ABL:MASTER:Ｃ感 >= 2 || ABL:MASTER:Ｂ感 >= 2
;				PRINTFORMW %ANAME(LOCAL:5)%の巧みな指使いに、%ANAME(MASTER)%は思わず腰をくねらせ甘い声を上げてしまう
;				PRINTFORMW だが、あと一歩で達するという所で、%ANAME(LOCAL:5)%は唐突に指を止め、%ANAME(MASTER)%の上から降りてしまった
;				PRINTFORML %ANAME(MASTER)%が物欲しそうな視線で%ANAME(LOCAL:5)%を見つめると、
;				PRINTFORMW %ANAME(LOCAL:5)%は、次回からもっと本格的に可愛がってあげるわ、と言って%ANAME(MASTER)%の縄を解いた
;				PRINTW ………………
;				PRINTFORMW %ANAME(MASTER)%は火照る体を持て余しながら、おぼつかない足取りで自分の部屋へと戻っていった…
;			ELSE
;				PRINTFORMW %ANAME(LOCAL:5)%の巧みな指使いに、未開発な%ANAME(MASTER)%の体の奥からムズムズとした感覚が湧き上がる
;				PRINTFORML 無意識のうちに%ANAME(MASTER)%の息が荒くなり、
;				PRINTFORMW やがて%ANAME(LOCAL:5)%に撫でられている股間から、クチュリ、と小さな水音が響いた
;				PRINTFORML %ANAME(LOCAL:5)%は戸惑う%ANAME(MASTER)%の表情を楽しそうに一目見ると、%ANAME(MASTER)%の上から降り、
;				PRINTFORMW 今回はこのくらいにしておいてあげる、と言って%ANAME(MASTER)%の縄を解いた
;				PRINTW ………………
;				PRINTFORML %ANAME(MASTER)%は、%ANAME(LOCAL:5)%に撫でられた部分に熱が残るのを感じながら、
;				PRINTFORMW おぼつかない足取りで自分の部屋へと戻っていった…
;			ENDIF
;
;			PRINTFORML <%ANAME(MASTER)%>
;			CALL PRINT_ADD_EXP(MASTER, "Ｃ感経験値", 10 + ABL:MASTER:Ｃ感 * 10)
;			CALL PRINT_ADD_EXP(MASTER, "Ｂ感経験値", 10 + ABL:MASTER:Ｂ感 * 10)
;		ENDIF
;
;		CALL TRAIN_AUTO_ABLUP(MASTER, 0)
;		WAIT
;
;	;主人公が処女かつある程度開発が進んでいる場合
;	ELSEIF TALENT:MASTER:処女 && FLAG:45 >= 4 && IS_FEMALE(MASTER)
;		PRINTFORMW %ANAME(MASTER)%は再び%ANAME(LOCAL:5)%からの呼び出しを受けた
;		PRINTW …………
;		PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(MASTER)%を裸にすると、手足を縛って寝台の上に仰向けの状態で固定した
;		PRINTFORML %ANAME(MASTER)%の体に覆い被さった%ANAME(LOCAL:5)%は、%ANAME(MASTER)%の乳首をねっとりとした舌使いで舐め回し、
;		IF HAS_PENIS(MASTER)
;			PRINTFORMW さらに指で%ANAME(MASTER)%のペニスを焦らすように撫で回した
;		ELSE
;			PRINTFORMW さらに指で%ANAME(MASTER)%のクリトリスを摘んで優しく念入りにこね回してくる
;		ENDIF
;		PRINTFORMW 度重なる調教で開発されてしまった%ANAME(MASTER)%は、快感のあまり甘い声を上げながら悶えてしまう
;
;		;タチ役に竿がある場合
;		IF HAS_PENIS(LOCAL:5)
;			PRINTFORML やがて、%ANAME(MASTER)%の秘唇が流れ出る愛液ですっかり濡れそぼったのを確認すると、
;			PRINTFORMW %ANAME(LOCAL:5)%は自らも服を脱ぎ、すっかり勃ち上がったペニスを晒した
;			PRINTFORML %ANAME(LOCAL:5)%は自身の固くそそり勃ったモノを見せつけるように%ANAME(MASTER)%の顔の前まで持ってくると、
;			PRINTFORMW %ANAME(MASTER)%に対して、処女を奪ってもいいかどうか聞いてきた…
;			CALL ASK_MULTI("や、止めて下さいっ！", "はい……", @"どうぞ、好きにして下さい…%UNICODE(0x2764)%")
;			REDRAW 0
;			CLEARLINE 3
;			IF RESULT == 0
;				PRINTL 
;				PRINTL →「や、止めて下さいっ！」
;				PRINTFORML だが%ANAME(LOCAL:5)%は嗜虐的な表情でクスリと笑うと、
;				PRINTFORMW %ANAME(MASTER)%の股間にソレをあてがい、容赦なく%ANAME(MASTER)%の奥へと突きこんできた
;			ELSEIF RESULT == 1
;				FLAG:45 += 1
;				PRINTL 
;				PRINTL →「はい……」
;				PRINTFORML すっかり従順になった%ANAME(MASTER)%の様子を見て、%ANAME(LOCAL:5)%は満足そうに小さく嗤うと、
;				PRINTFORMW %ANAME(MASTER)%の股間にペニスをあてがい、一気に%ANAME(MASTER)%の奥へと突きこんできた
;			ELSEIF RESULT == 2
;				FLAG:45 += 2
;				PRINTL 
;				PRINTFORML →「どうぞ、好きにして下さい…%UNICODE(0x2764)%」
;				PRINTFORML すっかり乗り気な%ANAME(MASTER)%の態度を見て、%ANAME(LOCAL:5)%は呆れたように苦笑すると、
;				PRINTFORMW %ANAME(MASTER)%の股間にペニスをあてがい、一気に%ANAME(MASTER)%の奥へと突きこんできた
;			ENDIF
;			REDRAW 1
;			PRINTL 
;			SETCOLOR COLOR("注意")
;			PRINTFORMW 処女喪失(%ANAME(MASTER)%)
;			TALENT:MASTER:処女 = 0
;			CFLAG:MASTER:41 = GET_ID(LOCAL:5)
;			RESETCOLOR
;			PRINTL 
;			PRINTFORML %ANAME(MASTER)%が破瓜の痛みに呻き声を上げると、
;			PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(MASTER)%の声を抑えるかのように口付けし、強引に舌を入れて深くキスをしてきた
;
;			;キスの共通処理
;			CALL KISS_COMMON(LOCAL:5, @"%ANAME(MASTER)%の唇")
;			CALL KISS_COMMON(MASTER, @"%ANAME(LOCAL:5)%の唇")
;
;			EXP:(LOCAL:5):キス経験 += 1
;			EXP:MASTER:キス経験 += 1
;
;			PRINTFORMW %ANAME(LOCAL:5)%の優しくも激しいキスに、%ANAME(MASTER)%は思わず脱力して身を委ねてしまう
;			PRINTFORML %ANAME(MASTER)%の力が抜けたのを確認すると、
;			PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(MASTER)%の腰をしっかりと押さえ、男根を奥まで突き込んできた
;			IF HAS_PENIS(MASTER)
;				PRINTFORMW 同時に、空いている方の手で%ANAME(MASTER)%のペニスを弄んでくる
;				PRINTFORML 前もって高められていた%ANAME(MASTER)%の体は、ペニスからもたらされる快感ですっかり欲情し、
;			ELSE
;				PRINTFORMW 同時に、空いている方の手で%ANAME(MASTER)%のクリトリスを弄んでくる
;				PRINTFORML 前もって高められていた%ANAME(MASTER)%の体は、クリトリスからもたらされる激しい快感に打ち震え、
;			ENDIF
;			PRINTFORMW それと共に破瓜の痛みが残る腰の奥底から、得体の知れない熱さが込み上げてくる
;			PRINTFORMW 徐々に痛みが快感に塗りつぶされ、%ANAME(MASTER)%は%ANAME(LOCAL:5)%に突かれるたび髪を振り乱しながら悶えてしまう
;			PRINTFORML %ANAME(LOCAL:5)%が一際力強くペニスを突き込むと、その瞬間の体の中で一気に快感が弾け、
;			IF HAS_PENIS(MASTER)
;				PRINTFORMW %ANAME(MASTER)%は自身のペニスから大量の精液を吹き出し、甲高い喘ぎを上げながらガクガクと痙攣した
;			ELSE
;				PRINTFORMW %ANAME(MASTER)%は甲高い喘ぎを上げながらガクガクと痙攣した
;			ENDIF
;			PRINTFORMW 薄れる意識の中、%ANAME(MASTER)%は膣内で%ANAME(LOCAL:5)%のペニスが震えるのを感じた…
;			PRINTW ………………
;			PRINTFORML %ANAME(LOCAL:5)%に処女を奪われてしまった%ANAME(MASTER)%は、未だ腰の奥に残る強い違和感を感じながら、
;			PRINTFORMW フラフラとした足取りで%ANAME(LOCAL:5)%の部屋を後にした…
;			PRINTL 
;
;			PRINTFORML <%ANAME(MASTER)%>
;			IF HAS_PENIS(MASTER)
;				CALL PRINT_ADD_EXP(MASTER, "Ｃ感経験値", 3 + ABL:MASTER:Ｃ感 * 3)
;				CALL PRINT_ADD_EXP(MASTER, "Ｖ感経験値", 30 + ABL:MASTER:Ｖ感 * 50)
;				CALL PRINT_ADD_EXP(MASTER, "Ｂ感経験値", 10 + ABL:MASTER:Ｂ感 * 20)
;				CALL PRINT_ADD_EXP(MASTER, "Ｍ感経験値", 20 + ABL:MASTER:Ｍ感 * 20)
;				CALL PRINT_ADD_EXP(MASTER, "射精経験値", 2)
;				CALL PRINT_ADD_EXP(MASTER, "精愛経験値", 4)
;				CALL PRINT_ADD_EXP(MASTER, "絶頂経験", 3)
;			ELSE
;				CALL PRINT_ADD_EXP(MASTER, "Ｃ感経験値", 15 + ABL:MASTER:Ｃ感 * 20)
;				CALL PRINT_ADD_EXP(MASTER, "Ｖ感経験値", 30 + ABL:MASTER:Ｖ感 * 50)
;				CALL PRINT_ADD_EXP(MASTER, "Ｂ感経験値", 10 + ABL:MASTER:Ｂ感 * 20)
;				CALL PRINT_ADD_EXP(MASTER, "Ｍ感経験値", 20 + ABL:MASTER:Ｍ感 * 20)
;				CALL PRINT_ADD_EXP(MASTER, "精愛経験値", 2)
;				CALL PRINT_ADD_EXP(MASTER, "絶頂経験", 1)
;			ENDIF
;
;		;タチ役に竿がない場合
;		ELSE
;			PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(MASTER)%を裸にすると、手足を縛って寝台の上に仰向けの状態で固定した
;			PRINTFORML %ANAME(MASTER)%の体に覆い被さった%ANAME(LOCAL:5)%は、%ANAME(MASTER)%の乳首をねっとりとした舌使いで舐め回し、
;			PRINTFORMW さらに指で%ANAME(MASTER)%のクリトリスを摘んで優しく念入りにこね回してくる
;			PRINTFORMW 度重なる調教で開発されてしまった%ANAME(MASTER)%は、快感のあまり甘い声を上げながら悶えてしまう
;			PRINTFORML やがて、%ANAME(MASTER)%の秘唇が流れ出る愛液ですっかり濡れそぼったのを確認すると、
;			PRINTFORMW %ANAME(LOCAL:5)%は横に置いてあった張り型を手に取り、見せつけるように%ANAME(MASTER)%の顔の前まで持ってきた…
;			PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(MASTER)%に、処女を奪ってもいいかどうか聞いてきた…
;			CALL ASK_MULTI("や、止めて下さいっ！", "はい……", @"どうぞ、好きにして下さい…%UNICODE(0x2764)%")
;			REDRAW 0
;			CLEARLINE 3
;			IF RESULT == 0
;				PRINTL 
;				PRINTL →「や、止めて下さいっ！」
;				PRINTFORML だが%ANAME(LOCAL:5)%は嗜虐的な表情でクスリと笑うと、
;				PRINTFORMW %ANAME(MASTER)%の股間に張り型をあてがい、容赦なく%ANAME(MASTER)%の奥へと突きこんできた
;			ELSEIF RESULT == 1
;				FLAG:45 += 1
;				PRINTL 
;				PRINTL →「はい……」
;				PRINTFORML すっかり従順になった%ANAME(MASTER)%の様子を見て、%ANAME(LOCAL:5)%は満足そうに小さく嗤うと、
;				PRINTFORMW %ANAME(MASTER)%の股間に張り型をあてがい、一気に%ANAME(MASTER)%の奥へと突きこんできた
;			ELSEIF RESULT == 2
;				FLAG:45 += 2
;				PRINTL 
;				PRINTFORML →「どうぞ、好きにして下さい…%UNICODE(0x2764)%」
;				PRINTFORML すっかり乗り気な%ANAME(MASTER)%の態度を見て、%ANAME(LOCAL:5)%は呆れたように苦笑すると、
;				PRINTFORMW %ANAME(MASTER)%の股間に張り型をあてがい、一気に%ANAME(MASTER)%の奥へと突きこんできた
;			ENDIF
;			REDRAW 1
;			PRINTL 
;			SETCOLOR COLOR("注意")
;			PRINTFORMW 処女喪失(%ANAME(MASTER)%)
;			TALENT:MASTER:処女 = 0
;			CFLAG:MASTER:41 = GET_ID(LOCAL:5)
;			RESETCOLOR
;			PRINTL 
;			PRINTFORML %ANAME(MASTER)%が破瓜の痛みに呻き声を上げると、
;			PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(MASTER)%の声を抑えるかのように口付けし、強引に舌を入れて深くキスをしてきた
;
;			;キスの共通処理
;			CALL KISS_COMMON(LOCAL:5, @"%ANAME(MASTER)%の唇")
;			CALL KISS_COMMON(MASTER, @"%ANAME(LOCAL:5)%の唇")
;
;			EXP:(LOCAL:5):キス経験 += 1
;			EXP:MASTER:キス経験 += 1
;
;			PRINTFORMW %ANAME(LOCAL:5)%の優しくも激しいキスに、%ANAME(MASTER)%は思わず脱力して身を委ねてしまう
;			PRINTFORMW %ANAME(MASTER)%の力が抜けたのを確認すると、%ANAME(LOCAL:5)%は手に持った張り型をゆっくりと動かし始めた
;			IF HAS_PENIS(MASTER)
;				PRINTFORMW 同時に、空いている方の手で%ANAME(MASTER)%のペニスを弄んでくる
;				PRINTFORML 前もって高められていた%ANAME(MASTER)%の体は、ペニスからもたらされる快感ですっかり欲情し、
;			ELSE
;				PRINTFORMW 同時に、空いている方の手で%ANAME(MASTER)%のクリトリスを弄んでくる
;				PRINTFORML 前もって高められていた%ANAME(MASTER)%の体は、クリトリスからもたらされる激しい快感に打ち震え、
;			ENDIF
;			PRINTFORMW それと共に破瓜の痛みが残る腰の奥底から、得体の知れない熱さが込み上げてくる
;			PRINTFORMW 徐々に痛みが快感に塗りつぶされ、%ANAME(MASTER)%は髪を振り乱しながら悶えてしまう
;			PRINTFORML %ANAME(LOCAL:5)%が機を見計らって一際強く張り型を突き込むと、その瞬間の体の中で一気に快感が弾け、
;			IF HAS_PENIS(MASTER)
;				PRINTFORMW %ANAME(MASTER)%は自身のペニスから大量の精液を吹き出し、甲高い喘ぎを上げながらガクガクと痙攣した
;			ELSE
;				PRINTFORMW %ANAME(MASTER)%は甲高い喘ぎを上げながらガクガクと痙攣した
;			ENDIF
;			PRINTW ………………
;			PRINTFORML %ANAME(LOCAL:5)%に処女を奪われてしまった%ANAME(MASTER)%は、未だ腰の奥に残る強い違和感を感じながら、
;			PRINTFORMW フラフラとした足取りで%ANAME(LOCAL:5)%の部屋を後にした…
;			PRINTL 
;
;			PRINTFORML <%ANAME(MASTER)%>
;			IF HAS_PENIS(MASTER)
;				CALL PRINT_ADD_EXP(MASTER, "Ｃ感経験値", 3 + ABL:MASTER:Ｃ感 * 3)
;				CALL PRINT_ADD_EXP(MASTER, "Ｖ感経験値", 20 + ABL:MASTER:Ｖ感 * 40)
;				CALL PRINT_ADD_EXP(MASTER, "Ｂ感経験値", 10 + ABL:MASTER:Ｂ感 * 20)
;				CALL PRINT_ADD_EXP(MASTER, "Ｍ感経験値", 20 + ABL:MASTER:Ｍ感 * 20)
;				CALL PRINT_ADD_EXP(MASTER, "射精経験値", 2)
;				CALL PRINT_ADD_EXP(MASTER, "絶頂経験", 3)
;			ELSE
;				CALL PRINT_ADD_EXP(MASTER, "Ｃ感経験値", 15 + ABL:MASTER:Ｃ感 * 20)
;				CALL PRINT_ADD_EXP(MASTER, "Ｖ感経験値", 20 + ABL:MASTER:Ｖ感 * 40)
;				CALL PRINT_ADD_EXP(MASTER, "Ｂ感経験値", 10 + ABL:MASTER:Ｂ感 * 20)
;				CALL PRINT_ADD_EXP(MASTER, "Ｍ感経験値", 20 + ABL:MASTER:Ｍ感 * 20)
;				CALL PRINT_ADD_EXP(MASTER, "絶頂経験", 1)
;			ENDIF
;		ENDIF
;
;		CALL TRAIN_AUTO_ABLUP(MASTER, 0)
;		WAIT
;
;	;二回目以降
;	ELSE
;		PRINTFORMW %ANAME(MASTER)%は再び%ANAME(LOCAL:5)%からの呼び出しを受けた
;		PRINTW …………
;		;IF RAND:12 == 0
;		;	PRINTFORMW 今日は機嫌がいいらしく、プレイの内容をこちらで決めて良いそうだ…
;		;ENDIF
;
;		;主人公がキス未経験かつある程度開発が進んでいる場合(非処女限定イベント)
;		IF TALENT:MASTER:キス未経験 && FLAG:45 >= 4 && IS_FEMALE(MASTER)
;			PRINTFORMW %ANAME(LOCAL:5)%はどうやら機嫌が悪いらしく、%ANAME(MASTER)%を前にしてもイラ立ちを隠せない様子だ…
;			PRINTFORML %ANAME(LOCAL:5)%はツカツカと%ANAME(MASTER)%の前に歩み寄ると、
;			PRINTFORMW 突然%ANAME(MASTER)%を強く抱き寄せ、強引にキスをしてきた
;			PRINTL 
;
;			CALL KISS_COMMON(LOCAL:5, @"%ANAME(MASTER)%の唇")
;			CALL KISS_COMMON(MASTER, @"%ANAME(LOCAL:5)%の唇")
;
;			EXP:(LOCAL:5):キス経験 += 1
;			EXP:MASTER:キス経験 += 1
;
;			PRINTL 
;			PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の口の中に無理やり舌をねじ込み、
;			PRINTFORMW %ANAME(MASTER)%の口内を荒々しく、貪るように蹂躙してきた
;			PRINTFORML %ANAME(MASTER)%は%ANAME(LOCAL:5)%の激しい舌使いと息苦しさとで、意識が朦朧としてしまい、
;			PRINTFORMW 口の端から二人分のヨダレをこぼしながら、なすがまま口虐を受け入れるしかなかった
;			PRINTFORML 長く激しい口付けの後、%ANAME(LOCAL:5)%がようやく%ANAME(MASTER)%を解放すると、
;			PRINTFORMW %ANAME(MASTER)%は立っていることができずにその場にへたり込んでしまった…
;
;			CALL ASK_YN("は、初めてだったのに…", "キスがこんなに気持ちいいなんて…")
;			REDRAW 0
;			CLEARLINE 3
;			IF RESULT == 0
;				PRINTL 
;				PRINTL →「は、初めてだったのに…」
;				PRINTFORML %ANAME(MASTER)%の言葉を聞いた%ANAME(LOCAL:5)%は、少し驚いたように目を見開くと、
;				PRINTFORMW クスクスと笑いながら、悪かったわね、と悪びれもなく言い放った
;			ELSE
;				FLAG:45 += 1
;				PRINTL 
;				PRINTFORML →「キスがこんなに気持ちいいなんて…」
;				PRINTFORML %ANAME(MASTER)%の初々しい反応に、ようやく%ANAME(LOCAL:5)%は%ANAME(MASTER)%のファーストキスを奪ったことに気付き、
;				PRINTFORMW まさかキスが初めてだとは思わなかったわ、と言ってクスクスと上機嫌に笑った
;			ENDIF
;			REDRAW 1
;
;			PRINTL 
;			CALL PRINT_ADD_EXP(MASTER, "Ｍ感経験値", 30 + ABL:MASTER:Ｍ感 * 40)
;			CALL TRAIN_AUTO_ABLUP(MASTER, 0)
;			WAIT
;
;			PRINTW …………
;		ENDIF
;
;		;★　あなたが男ならば常に搾精　……男の娘ならAもあり……か？
;		IF (TALENT:MASTER:処女 && IS_FEMALE(MASTER)) || FLAG:45 <= 4 || RAND:3 == 0 || TALENT:MASTER:性別 == 0 || TALENT:MASTER:性別 == 3 || (TALENT:MASTER:性別 >= 4 && RAND:2 == 0)
;			;★　あなたが男ならば常に搾精
;			SELECTCASE IS_MALE(MASTER) ? 0 # RAND:2
;				;Ｃ開発 or 搾精
;				CASE 0
;					IF HAS_PENIS(MASTER)
;						PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%を寝台に寝かせると、
;						PRINTFORMW %ANAME(MASTER)%のペニスを手のひらで包み込み、亀頭を集中的に撫で回してきた
;						IF ABL:MASTER:Ｃ感 >= 2
;							PRINTFORMW 敏感な亀頭への刺激に、%ANAME(MASTER)%は思わず腰をのけぞらせて悶えてしまう
;							PRINTFORML やがて腰の疼きに耐え切れなくなった%ANAME(MASTER)%が射精を懇願すると、
;							PRINTFORMW %ANAME(LOCAL:5)%は満足そうにクスッと笑い、%ANAME(MASTER)%のペニスを根本からシゴき上げてきた
;							PRINTFORML 精を搾り出す手の動きに、%ANAME(MASTER)%は一気に射精感を高められ、あっという間に精液を吹き出した
;							PRINTFORML だが、甘い放出感に浸る間もなく、%ANAME(LOCAL:5)%は%ANAME(MASTER)%の亀頭を再び乱暴に掴むと、
;							PRINTFORMW 射精後の敏感な部分をグチュグチュと握りつぶすほど激しく弄んできた
;							PRINTFORMW 暴力的な快感に%ANAME(MASTER)%は髪を振り乱して悶え狂い、獣のような叫び声を上げてしまう
;							PRINTFORMW 快感に震える%ANAME(MASTER)%のペニスから、ぷしゃっ、と潮が吹き出した
;							PRINTW ………………
;							PRINTFORML 徹底的にペニスを苛め抜かれた%ANAME(MASTER)%は、何度も精を搾られてすっかり疲れ果て、
;							PRINTFORMW フラフラとした重い足取りで%ANAME(LOCAL:5)%の部屋を後にした…
;						ELSE
;							PRINTFORML 慣れない亀頭への刺激に、%ANAME(MASTER)%は思わず腰を引いてしまいそうになるが、
;							PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(MASTER)%の下半身を押さえこんで、念入りに竿の先をこね回してくる
;							PRINTFORML やがて腰の奥からムズムズとしたものが這い上がり、%ANAME(MASTER)%が物欲しげに%ANAME(LOCAL:5)%の方を見ると、
;							PRINTFORMW %ANAME(LOCAL:5)%は小さくクスリと笑い、%ANAME(MASTER)%のペニスを根本からシゴき上げてきた
;							PRINTFORML 精を搾り出す手の動きに、%ANAME(MASTER)%は耐える間もなく一気に射精感を高められ、
;							PRINTFORMW あられもない声を上げながら精を漏らしてしまう
;							PRINTFORML 腰が痺れるような心地よい放出感に、すっかり呆けていると、
;							PRINTFORMW 敏感になった亀頭を突然ギュッと強く掴まれ、%ANAME(MASTER)%はビクリと体を震わせて呻いた
;							PRINTW ………………
;							PRINTFORML その後も徹底的にペニスを苛め抜かれた%ANAME(MASTER)%は、快感の余韻でふらつく足取りで、
;							PRINTFORMW %ANAME(LOCAL:5)%の部屋を後にした…
;						ENDIF
;						PRINTL 
;						PRINTFORML <%ANAME(MASTER)%>
;						CALL PRINT_ADD_EXP(MASTER, "Ｃ感経験値", 15 + ABL:MASTER:Ｃ感 * 20)
;						LOCAL:0 = 3 + (ABL:MASTER:射精 + ABL:MASTER:Ｃ感) * (RAND:61 + 70) / 200
;						CALL PRINT_ADD_EXP(MASTER, "射精経験値", LOCAL:0)
;						IF ABL:MASTER:Ｃ感 >= 2
;							LOCAL:0 += (ABL:MASTER:Ｃ感 * 2 + RAND:(ABL:MASTER:Ｃ感)) * 2 / 3
;							FLAG:45 += 1
;						ENDIF
;						CALL PRINT_ADD_EXP(MASTER, "絶頂経験", LOCAL:0)
;
;					ELSE
;						PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%を寝台に寝かせると、
;						PRINTFORMW %ANAME(MASTER)%の秘唇に口付け、這い回るようなねっとりとした舌使いで%ANAME(MASTER)%のクリをひたすら苛めてきた
;						IF ABL:MASTER:Ｃ感 >= 2
;							PRINTFORML %ANAME(LOCAL:5)%の舌が%ANAME(MASTER)%の敏感な部分を舐め上げるたび、
;							PRINTFORMW %ANAME(MASTER)%の口から抑えられない喘ぎが漏れ、腰の奥から手足や頭に向けて快感が走り抜ける
;							PRINTFORML やがて蓄積された快感が限界まで膨れ上がり、
;							PRINTFORMW %ANAME(MASTER)%は自分の意志とは無関係に体を反らし、あられもない声を上げて絶頂した
;							PRINTFORML だが、%ANAME(LOCAL:5)%は何事も無かったかのように、チロチロとクリを舌で弄び続ける
;							PRINTFORML 息つく暇もなく、絶頂でより敏感になったクリを苛められ、%ANAME(MASTER)%は口の端から涎を垂らして悶え狂った
;							PRINTW ………………
;							PRINTFORML 徹底的にクリトリスを苛め抜かれた%ANAME(MASTER)%は、激しい絶頂の余韻で夢見心地になりながら、
;							PRINTFORMW フラフラとした足取りで%ANAME(LOCAL:5)%の部屋を後にした…
;						ELSE
;							PRINTFORML まだ未開発なクリトリスを舐め上げられると、
;							PRINTFORMW %ANAME(MASTER)%の腰の奥から徐々にムズムズした気持ち悪いような何かが這い上がってくる
;							PRINTFORML %ANAME(MASTER)%が思わず腰を引いて逃げようとすると、
;							PRINTFORMW %ANAME(LOCAL:5)%はそれを許さずガッチリと%ANAME(MASTER)%の腰を抱え込んで%ANAME(MASTER)%のクリを苛め続けた
;							PRINTW ………………
;							PRINTFORML しばらくの間、徹底的にクリトリスを開発され、
;							PRINTFORML %ANAME(MASTER)%は腰の奥にズクズクと渦を巻く落ち着かない熱さを感じながら、
;							PRINTFORMW 重い足取りで%ANAME(LOCAL:5)%の部屋を後にした…
;						ENDIF
;						PRINTL 
;						PRINTFORML <%ANAME(MASTER)%>
;						CALL PRINT_ADD_EXP(MASTER, "Ｃ感経験値", 25 + ABL:MASTER:Ｃ感 * 30)
;						IF ABL:MASTER:Ｃ感 >= 2
;							CALL PRINT_ADD_EXP(MASTER, "絶頂経験", ABL:MASTER:Ｃ感 * 2 + RAND:(ABL:MASTER:Ｃ感))
;							FLAG:45 += 1
;						ENDIF
;					ENDIF
;
;					CALL TRAIN_AUTO_ABLUP(MASTER, 0)
;					WAIT
;
;				;Ｂ開発
;				CASE 1
;					PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の手首を縛り天井から吊り下げると、
;					PRINTFORM %ANAME(MASTER)%の背後に回って、背中越しに%ANAME(MASTER)%の
;					LOCAL:0 = GET_BUSTSIZE_NUM(MASTER)
;					IF LOCAL:0 <= -1
;						PRINT 平らな胸を撫で回して
;					ELSEIF LOCAL:0 >= 1
;						PRINT 大きな胸を揉みしだいて
;					ELSE
;						PRINT 胸を揉みしだいて
;					ENDIF
;					PRINTW きた
;					IF ABL:MASTER:Ｂ感 >= 2
;						PRINTFORML 既に胸での快感を覚えてしまった%ANAME(MASTER)%は、乳房を優しく揉まれるだけでも力が抜けてしまい、
;						PRINTFORMW ビクビクと体を震わせながら%ANAME(LOCAL:5)%の体にしなだれかかってしまう
;						PRINTFORML 乳房からもたらされる快感が%ANAME(MASTER)%の全身を侵食し、
;						IF HAS_PENIS(MASTER)
;							PRINTFORML やがて腰にジンとした痺れが走ると、%ANAME(MASTER)%の秘所とペニスの両方から
;							PRINTFORMW トロトロと透明な液体が流れ落ちた
;						ELSE
;							PRINTFORMW やがて腰にジンとした痺れが走ると、%ANAME(MASTER)%の秘所からトロトロと大量の愛液が滴り落ちた
;						ENDIF
;						PRINTFORMW 既に%ANAME(MASTER)%の乳首は痛いほどにピンと直立し、今か今かと刺激を待ちわびてジクジクと疼いている
;						PRINTFORML %ANAME(LOCAL:5)%が%ANAME(MASTER)%の勃起した乳首を思い切り強くつねると、
;						PRINTFORMW 瞬間%ANAME(MASTER)%の体に稲妻のような快感が走り抜ける
;						PRINTFORMW %ANAME(MASTER)%は頭が真っ白になるほどの絶頂感を味わいながら、ガクガクと体を痙攣させた
;						PRINTFORML %ANAME(LOCAL:5)%は完全に脱力した%ANAME(MASTER)%の体を支え直すと、
;						PRINTFORMW 絶頂で敏感になった乳首を容赦なくこねくり回し始めた
;						PRINTW ………………
;						PRINTFORML 立ちっぱなしで責められ続けた%ANAME(MASTER)%は、行為の後、疲労のあまり立つこともままならず、
;						PRINTFORMW ガクガクと震える足を引きずるようにして%ANAME(LOCAL:5)%の部屋を後にした…
;					ELSE
;						PRINTFORML まだ十分に胸での快感を知らない%ANAME(MASTER)%だったが、優しく念入りに乳房を揉まれ続けると、
;						PRINTFORMW 徐々に体が熱くなり、自分の意志とは無関係に呼吸が荒くなってしまう
;						IF HAS_PENIS(MASTER)
;							PRINTFORML 今や%ANAME(MASTER)%のペニスはすっかり勃起し、
;							PRINTFORMW 擦り合わせた太股からは、クチュリ、と小さな水音が聞こえてくる
;						ELSE
;							PRINTFORML やがて腰にゾクリとした感覚が走り、%ANAME(MASTER)%の擦り合わせた太股から、
;							PRINTFORMW クチュリ、と小さな水音が聞こえてきた
;						ENDIF
;						PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の体が少しずつ快楽を覚えていく様子を楽しそうに観察しながら、
;						PRINTFORMW 今度はピンと立った乳首を掌で乳房ごと包み込み、ゆっくりと優しくこね回し始めた
;						PRINTW ………………
;						PRINTFORML 延々と胸を弄られ続けた%ANAME(MASTER)%は、乳房に残る少しの痛みと、いつまでも抜けない熱さを抱えながら、
;						PRINTFORMW 重い足取りで%ANAME(LOCAL:5)%の部屋を後にした…
;					ENDIF
;					PRINTL 
;					PRINTFORML <%ANAME(MASTER)%>
;					CALL PRINT_ADD_EXP(MASTER, "Ｂ感経験値", 25 + ABL:MASTER:Ｂ感 * 30)
;					IF ABL:MASTER:Ｂ感 >= 2
;						CALL PRINT_ADD_EXP(MASTER, "絶頂経験", ABL:MASTER:Ｂ感 * 2 + RAND:(ABL:MASTER:Ｂ感))
;						FLAG:45 += 1
;					ENDIF
;					CALL TRAIN_AUTO_ABLUP(MASTER, 0)
;					WAIT
;			ENDSELECT
;
;		;調教度が十分高いとき限定
;		ELSE
;			;★　男の娘ならAもあり……か？
;			SELECTCASE IS_MALE(MASTER) ? 1 # RAND:2
;				;Ｖ開発
;				CASE 0
;					;タチ役に竿がある
;					IF HAS_PENIS(LOCAL:5)
;						PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の手足を縄で縛り、仰向けの状態で寝台に固定すると、
;						PRINTFORMW %ANAME(MASTER)%の秘部を舌で丹念に舐めほぐしてきた
;						PRINTFORML %ANAME(LOCAL:5)%に開発されたソコは舐められるたびに熱を帯び、
;						PRINTFORMW 腰が震えるような快感とともにトロリとした愛液が漏れ出てしまう
;						PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%が十分に濡れたのを確認すると、
;						PRINTFORMW 自らも興奮してすっかり勃ち上がったペニスを、ゆっくりと%ANAME(MASTER)%の膣内へと沈めてきた
;						IF ABL:MASTER:Ｖ感 >= 2
;							PRINTFORML %ANAME(LOCAL:5)%のモノが%ANAME(MASTER)%の膣壁を押し広げる感覚に、
;							PRINTFORMW わずかの苦しさと共に心地よい充足感が%ANAME(MASTER)%の体に広がる
;							PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%に覆いかぶさると、欲情に任せて激しく腰を打ち付けてきた
;							PRINTFORMW %ANAME(LOCAL:5)%に突かれるたび、%ANAME(MASTER)%の全身を痺れるような衝撃が貫く
;							PRINTFORMW やがて、%ANAME(MASTER)%は頭が真っ白になるほどの快感と共に背筋を反らせて絶頂した
;							IF HAS_PENIS(MASTER)
;								PRINTFORML %ANAME(MASTER)%は絶頂の衝撃で自身のペニスからドプドプと精液を垂れ流し、
;								PRINTFORMW 同時に無意識のうちに%ANAME(LOCAL:5)%のモノを膣壁でキュッと締め付けてしまう
;							ELSE
;								PRINTFORMW 同時に%ANAME(MASTER)%の膣が痙攣し、射精をねだるように%ANAME(LOCAL:5)%のモノにしゃぶりつき絡みつく
;							ENDIF
;							PRINTFORML %ANAME(LOCAL:5)%は気持ち良さそうな声を出してビクリと腰を震わせると、
;							PRINTFORMW 快感のあまり表情を緩ませながら、%ANAME(MASTER)%の中に大量の精液を注ぎ込んだ
;							IF HAS_PENIS(MASTER)
;								PRINTFORML %ANAME(MASTER)%と%ANAME(LOCAL:5)%は互いの白濁にまみれたまま脱力してしばらく抱き合っていたが、
;							ELSE
;								PRINTFORML %ANAME(MASTER)%と%ANAME(LOCAL:5)%はしばらくお互いにぐったりと脱力して抱き合っていたが、
;							ENDIF
;							PRINTFORML やがて%ANAME(LOCAL:5)%が体を起こすと、欲情に染まった瞳で%ANAME(MASTER)%の顔を覗き込み、
;							PRINTFORMW 未だ固くそそり勃つソレを%ANAME(MASTER)%の中に挿入したまま再び腰を動かし始めた
;							PRINTW ………………
;							PRINTFORML %ANAME(MASTER)%が目を覚ますと、%ANAME(MASTER)%の頭を撫でながら微笑む%ANAME(LOCAL:5)%と目が合った
;							PRINTFORMW 可愛かったわよ、とキスをされ、%ANAME(MASTER)%は恥ずかしさから逃げるように%ANAME(LOCAL:5)%の部屋を後にした…
;						ELSE
;							PRINTFORML 思わず身構えて力が入ってしまう%ANAME(MASTER)%の様子を見て、
;							PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(MASTER)%に口付けし、強引に口内へと舌をねじ込んできた
;							PRINTFORML %ANAME(LOCAL:5)%の巧みな舌使いで口内を蹂躙され、%ANAME(MASTER)%がすっかり脱力してしまうと、
;							PRINTFORMW その途端に%ANAME(LOCAL:5)%は一気に%ANAME(MASTER)%の奥までペニスをねじ込んできた
;							PRINTFORMW わずかな痛みと激しい衝撃に、%ANAME(MASTER)%は小さく背を反らせながら声を上げてしまう
;							PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(MASTER)%を抱きしめながら、無理せず優しく腰を動かしてきた
;							IF HAS_PENIS(MASTER)
;								PRINTFORML %ANAME(LOCAL:5)%の腹で%ANAME(MASTER)%のペニスが擦られ、
;								PRINTFORMW ムズムズとした快感に%ANAME(MASTER)%は思わず甘い声を出してしまう
;							ELSE
;								PRINTFORMW 奥を突かれるたび、%ANAME(MASTER)%の内から少しずつ熱が湧き上がってくる
;							ENDIF
;							PRINTFORML やがて限界に達した%ANAME(LOCAL:5)%は%ANAME(MASTER)%の膣の一際奥までペニスを突き入れると、
;							PRINTFORMW 気持ち良さそうな声を上げながら%ANAME(MASTER)%の中に精液を注ぎ込んでいく…
;							IF HAS_PENIS(MASTER)
;								PRINTFORMW 全身を包む不思議な充足感に、%ANAME(MASTER)%のペニスがビクビクと震えた
;							ELSE
;								PRINTFORMW %ANAME(MASTER)%は全身を包む不思議な充足感に、ゾクリと背筋を震わせた
;							ENDIF
;							PRINTW ………………
;							PRINTFORML %ANAME(LOCAL:5)%に何度も中出しされた%ANAME(MASTER)%は、
;							PRINTFORMW 未だ膣内にペニスを挿れられているような違和感を感じながら、%ANAME(LOCAL:5)%の部屋を後にした…
;						ENDIF
;						PRINTL 
;						PRINTFORML <%ANAME(MASTER)%>
;						CALL PRINT_ADD_EXP(MASTER, "Ｃ感経験値", 5 + ABL:MASTER:Ｃ感 * 5)
;						CALL PRINT_ADD_EXP(MASTER, "Ｖ感経験値", 30 + ABL:MASTER:Ｖ感 * 50)
;						IF ABL:MASTER:Ｖ感 >= 2
;							LOCAL:0 = 0
;							IF HAS_PENIS(MASTER)
;								LOCAL:0 = 3 + (ABL:MASTER:射精 + ABL:MASTER:Ｖ感) * (RAND:61 + 70) / 200
;								CALL PRINT_ADD_EXP(MASTER, "射精経験値", LOCAL:0)
;							ENDIF
;							CALL PRINT_ADD_EXP(MASTER, "絶頂経験", ABL:MASTER:Ｖ感 * 2 + RAND:(ABL:MASTER:Ｖ感) + LOCAL:0)
;							FLAG:45 += 1
;						ENDIF
;						CALL PRINT_ADD_EXP(MASTER, "精愛経験値", 12)
;						PRINTL 
;						PRINTFORML <%ANAME(LOCAL:5)%>
;						CALL PRINT_ADD_EXP(LOCAL:5, "Ｃ感経験値", 2 + ABL:(LOCAL:5):Ｃ感 * 2)
;						CALL PRINT_ADD_EXP(LOCAL:5, "射精経験値", 4 + ABL:(LOCAL:5):射精 * 2)
;
;					;タチ役に竿がない
;					ELSE
;						PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の手足を縄で縛り、仰向けの状態で寝台に固定すると、
;						PRINTFORMW %ANAME(MASTER)%の秘部に丹念にローションを塗り付けてきた
;						PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の中まで冷たいローションが行き渡ったのを確認すると、張り型を手に取り、
;						PRINTFORMW %ANAME(MASTER)%に見せつけるようにしてから、ゆっくりと%ANAME(MASTER)%のワレメに押し当ててきた
;						IF ABL:MASTER:Ｖ感 >= 2
;							PRINTFORML 張り型の先で焦らすようにワレメをなぞられると、%ANAME(MASTER)%の体は張り型で突かれる快感を予期して、
;							PRINTFORMW %ANAME(MASTER)%の意志と無関係に、物欲しそうに腰をくねらせてしまう
;							PRINTFORML たっぷりと焦らされ、すっかり理性の溶けきった%ANAME(MASTER)%が、ねだるような瞳で%ANAME(LOCAL:5)%を見上げると、
;							PRINTFORMW %ANAME(LOCAL:5)%はサディスティックな笑いを浮かべ、体重を掛けて一気に%ANAME(MASTER)%奥まで張り型をねじ込んできた
;							PRINTFORMW 全身が弾けるような衝撃に、%ANAME(MASTER)%は激しく背を反らせて頭が真っ白になるほどの絶頂に身を委ねた
;							PRINTFORML グッタリと脱力する%ANAME(MASTER)%をよそに、%ANAME(LOCAL:5)%は張り型をグポグポと容赦なく抜き差しする
;							PRINTFORML %ANAME(MASTER)%は悲鳴にも似た叫びを上げながら、%ANAME(LOCAL:5)%の前で何度もイキ顔を晒し、
;							PRINTFORMW 何度目かの絶頂の後、完全に意識を手放した
;							PRINTW ………………
;							PRINTFORML %ANAME(MASTER)%が目を覚ますと、%ANAME(MASTER)%の頭を撫でながら微笑む%ANAME(LOCAL:5)%と目が合った
;							PRINTFORMW 可愛かったわよ、とキスをされ、%ANAME(MASTER)%は恥ずかしさから逃げるように%ANAME(LOCAL:5)%の部屋を後にした…
;						ELSE
;							PRINTFORML 思わず身構えて力が入ってしまう%ANAME(MASTER)%の様子を見て、
;							PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(MASTER)%に口付けし、強引に口内へと舌をねじ込んできた
;							PRINTFORML %ANAME(LOCAL:5)%の巧みな舌使いで口内を蹂躙され、%ANAME(MASTER)%がすっかり脱力してしまうと、
;							PRINTFORMW その途端に%ANAME(LOCAL:5)%は一気に%ANAME(MASTER)%の中へと張り型をねじ込んできた
;							PRINTFORMW わずかな痛みと激しい衝撃に、%ANAME(MASTER)%は小さく背を反らせながら声を上げてしまう
;							PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の性感帯を探るかのように、
;							PRINTFORMW ゆっくりと張り型を前後させ、あるいは%ANAME(MASTER)%の膣内をかき回すように動かしてきた
;							PRINTFORML %ANAME(MASTER)%の膣内でクチュクチュと卑猥な音が響くたび、
;							PRINTFORMW %ANAME(MASTER)%は自分の内から湧き上がる快感が、少しずつ強くなっていくのを否応なしに感じていた
;							PRINTW ………………
;							PRINTFORML %ANAME(LOCAL:5)%に延々と膣内を開発された%ANAME(MASTER)%は、
;							PRINTFORMW 未だ張り型を膣に挿し込まれているかのような違和感を感じながら、%ANAME(LOCAL:5)%の部屋を後にした…
;						ENDIF
;						PRINTL 
;						PRINTFORML <%ANAME(MASTER)%>
;						CALL PRINT_ADD_EXP(MASTER, "Ｖ感経験値", 20 + ABL:MASTER:Ｖ感 * 40)
;						IF ABL:MASTER:Ｖ感 >= 2
;							CALL PRINT_ADD_EXP(MASTER, "絶頂経験", ABL:MASTER:Ｖ感 * 2 + RAND:(ABL:MASTER:Ｖ感))
;							FLAG:45 += 1
;						ENDIF
;					ENDIF
;
;					CALL TRAIN_AUTO_ABLUP(MASTER, 0)
;					CALL TRAIN_AUTO_ABLUP(LOCAL:5, 0)
;					WAIT
;
;				;Ａ開発
;				CASE 1
;					IF ABL:MASTER:Ａ感 >= 2
;						PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の手足を縄で縛り、寝台の上に四つん這いにさせると、
;						PRINTFORMW %ANAME(MASTER)%のアナルを突然指で押し広げ、ヒクつく腸中を観察してきた
;						PRINTFORML %ANAME(MASTER)%が恥ずかしさのあまり寝台に顔を埋めていると、
;						PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の尻穴に冷たいローションを流し込み、
;						PRINTFORMW 尻穴用の張り型を手に取って%ANAME(MASTER)%のアナルに差し込んできた
;						PRINTFORML %ANAME(MASTER)%の開発されたアナルは中を掻き回されるたび、
;						PRINTFORMW 苦しさとともに、お腹の奥から湧き上がるような強い快感を伝えてくる
;						PRINTFORML %ANAME(LOCAL:5)%が激しく張り型を前後させて%ANAME(MASTER)%の腸壁を何度も擦ると、
;						PRINTFORMW %ANAME(MASTER)%はあまりの快感に耐え切れずに、口から大量の涎を漏らしながらビクビク体を震わせ絶頂してしまう
;						IF HAS_PENIS(MASTER)
;							PRINTFORMW 同時に%ANAME(MASTER)%のペニスから、びゅるりと押し出されるように精液が吹き出した
;						ENDIF
;						PRINTFORML %ANAME(LOCAL:5)%はお尻でイってしまった%ANAME(MASTER)%のことを淫らな言葉で罵りながら、
;						PRINTFORMW 粗相をした子供をしつけるかのように、%ANAME(MASTER)%のお尻を強く叩き始めた
;						PRINTFORML %ANAME(MASTER)%は悲鳴を上げながらも、叩かれるたびに腸内の張り型が震えて感じてしまい、
;						IF HAS_PENIS(MASTER)
;							PRINTFORMW ペニスを物欲しげにビクビクと震わせてしまう
;						ELSE
;							PRINTFORMW 秘唇からトロトロと大量の蜜を垂らしてしまう
;						ENDIF
;						PRINTFORML %ANAME(MASTER)%は%ANAME(LOCAL:5)%にお尻を叩かれながら何度となく絶頂させられ、
;						PRINTFORMW たっぷりと被虐の快楽を教えこまれてしまった…
;						PRINTW ………………
;						PRINTFORML すっかり満足して機嫌良さそうに笑う%ANAME(LOCAL:5)%と対照的に、
;						PRINTFORMW %ANAME(MASTER)%はヒリヒリと痛むお尻を押さえ、涙目になりながら%ANAME(LOCAL:5)%の部屋を後にした…
;					ELSE
;						PRINTFORML %ANAME(LOCAL:5)%は%ANAME(MASTER)%の手足を縄で縛り、寝台の上に四つん這いにさせると、
;						PRINTFORMW %ANAME(MASTER)%のアナルに丹念にローションを塗り付けてきた
;						PRINTFORMW %ANAME(LOCAL:5)%はゆっくりと%ANAME(MASTER)%の尻穴に指を入れると、腸内を掻き回すように動かしてきた
;						PRINTFORML まだ開発の不十分な%ANAME(MASTER)%のアナルは、快感よりも痛みが先に立ち、
;						PRINTFORMW %ANAME(MASTER)%は苦しそうに呻きを漏らしてしまう
;						PRINTFORML %ANAME(LOCAL:5)%はまだ尻穴で快感を感じられない%ANAME(MASTER)%の様子を見て取ると、
;						PRINTFORMW 後ろから%ANAME(MASTER)%に覆いかぶさり、耳元で囁くように%ANAME(MASTER)%の尻穴の内部の様子を実況してきた
;						PRINTFORML 腸壁が%ANAME(LOCAL:5)%の指を締め付ける様子を事細かに伝えられると、
;						PRINTFORMW 恥ずかしさのあまり%ANAME(MASTER)%の体が一気に熱くなってしまう
;						IF HAS_PENIS(MASTER)
;							PRINTFORMW それと同期するように、%ANAME(MASTER)%のペニスが自然と震え出し、切ない快感が腰の奥から湧き上がってくる
;						ELSE
;							PRINTFORMW それと同期するように、%ANAME(MASTER)%のアナルからもたらされる感触が、徐々に苦しさから快感に変わっていく
;						ENDIF
;						PRINTFORMW 少しずつ尻穴で快感を感じ始めた%ANAME(MASTER)%を、%ANAME(LOCAL:5)%は今度は罵るような言葉で責め立てた…
;						PRINTW ………………
;						PRINTFORML %ANAME(LOCAL:5)%にアナルを開発されながらたっぷりと言葉責めを受けた%ANAME(MASTER)%は、
;						PRINTFORMW 恥ずかしさで顔を赤く染めたまま、逃げるように%ANAME(LOCAL:5)%の部屋を後にした…
;					ENDIF
;					PRINTL 
;					PRINTFORML <%ANAME(MASTER)%>
;					CALL PRINT_ADD_EXP(MASTER, "Ａ感経験値", 20 + ABL:MASTER:Ａ感 * 40)
;					IF ABL:MASTER:Ａ感 >= 2
;						LOCAL:0 = 0
;						IF HAS_PENIS(MASTER)
;							LOCAL:0 = 3 + (ABL:MASTER:射精 + ABL:MASTER:Ａ感) * (RAND:61 + 70) / 200
;							CALL PRINT_ADD_EXP(MASTER, "射精経験値", LOCAL:0)
;						ENDIF
;						CALL PRINT_ADD_EXP(MASTER, "絶頂経験", ABL:MASTER:Ａ感 * 2 + RAND:(ABL:MASTER:Ａ感))
;						FLAG:45 += 1
;					ENDIF
;					CALL TRAIN_AUTO_ABLUP(MASTER, 0)
;					WAIT
;				;奉仕＆自慰
;				CASE 2
;				;全身愛撫
;				CASE 3
;			ENDSELECT
;		ENDIF
;	ENDIF
;
;;情事覗きイベント
;ELSEIF NUM_SEX >= 1 && JUDGE_EVENT_OTHERS(NUM_SEX_LOVER, 1) && CONFIG:73 == 1 && CFLAG:(LOCAL:5):1 == CFLAG:MASTER:1 && CFLAG:MASTER:9 == 0
;	;タチ役に竿がある場合
;	IF HAS_PENIS(LOCAL:5)
;		PRINTL 
;		IF RAND:3 != 0
;			PRINTFORMW %ANAME(MASTER)%が%ANAME(LOCAL:5)%の部屋を通り掛かると、部屋の中から艶めかしい声が聞こえてきた…
;			LOCAL:12 = 0
;		ELSE
;			PRINTFORMW %ANAME(MASTER)%が%ANAME(LOCAL:5)%の部屋を通り掛かると、部屋の中から妖しげな水音が聞こえてきた…
;			LOCAL:12 = 1
;		ENDIF
;		CALL ASK_YN("こっそり覗く", "無視する")
;
;		IF RESULT == 0
;			;対象となるキャラをランダムに選択([恋人]持ちは優先)
;			IF NUM_SEX_LOVER >= 1
;				LOCAL:11 = LIST_SEX_LOVER:(RAND:NUM_SEX_LOVER)
;			ELSE
;				LOCAL:11 = LIST_SEX:(RAND:NUM_SEX)
;			ENDIF
;
;			IF LOCAL:12 == 0
;				IF RAND:2
;					PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、%ANAME(LOCAL:5)%が裸の%ANAME(LOCAL:11)%を組み敷いて犯していた
;					PRINTFORMW %ANAME(LOCAL:5)%が奥まで腰を突き込むたびに、%ANAME(LOCAL:11)%は気持ちよさそうに甘い声で喘いでいる…
;					PRINTFORML やがて%ANAME(LOCAL:11)%が絶頂に達してビクビクと体を震わせると、
;					PRINTFORMW 感極まった%ANAME(LOCAL:5)%は%ANAME(LOCAL:11)%の膣内に自身の精液をたっぷりと注ぎ込んだ
;					PRINTFORMW 絶頂の余韻でグッタリとする%ANAME(LOCAL:11)%だったが、%ANAME(LOCAL:5)%は休む間もなく腰の動きを再開させる
;					PRINTFORMW %ANAME(LOCAL:11)%はあまりの快感に仰け反りながら、とろけ切った雌の表情で%ANAME(LOCAL:5)%の与える快感に身を任せていた
;					PRINTFORMW …………
;				ELSE
;					PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、%ANAME(LOCAL:5)%が%ANAME(LOCAL:11)%に後ろから覆い被さっているのが見えた
;					PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(LOCAL:11)%をバックの体勢で犯しながら、鞭を構えて%ANAME(LOCAL:11)%の背中に打ち付けている
;					PRINTFORMW %ANAME(LOCAL:11)%は鞭で打たれるたびに悲鳴を上げつつも、自ら快感を貪るように腰をくねらせている
;					PRINTFORML やがて%ANAME(LOCAL:5)%が腰を奥まで打ち付けて%ANAME(LOCAL:11)%の膣内に精液を注ぎ込むと、
;					PRINTFORMW %ANAME(LOCAL:11)%はうっとりとした表情で%ANAME(LOCAL:5)%の子種を受け入れていた
;					PRINTFORMW …………
;				ENDIF
;			ELSE
;				PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、裸の%ANAME(LOCAL:11)%が%ANAME(LOCAL:5)%の前に座っているのが見えた
;				PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(LOCAL:11)%に自慰をさせながら、自身の男根を舐めさせ奉仕させている…
;				PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(LOCAL:11)%の喉奥まで強引に腰を付き入れると、そのまま大量の精液を注ぎ込んだ
;				PRINTFORMW %ANAME(LOCAL:11)%は%ANAME(LOCAL:5)%の子種で溺れそうになりながら、興奮した様子でゾクゾクと体を震わせていた
;				PRINTFORMW …………
;			ENDIF
;		ENDIF
;
;	;タチ役に竿がない場合
;	ELSE
;		PRINTL 
;		PRINTFORMW %ANAME(MASTER)%が%ANAME(LOCAL:5)%の部屋を通り掛かると、部屋の中から艶めかしい声が聞こえてきた…
;		CALL ASK_YN("こっそり覗く", "無視する")
;
;		IF RESULT == 0
;			;対象となるキャラをランダムに選択([恋人]持ちは優先)
;			IF NUM_SEX_LOVER >= 1
;				LOCAL:11 = LIST_SEX_LOVER:(RAND:NUM_SEX_LOVER)
;			ELSE
;				LOCAL:11 = LIST_SEX:(RAND:NUM_SEX)
;			ENDIF
;
;			;レズＳＭパッチ無効(有効でも1/4の確率でこちらの文に)
;			IF CONFIG:78 == 0 || RAND:4 == 0
;				IF HAS_PENIS(LOCAL:11)
;					IF RAND:2
;						PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、%ANAME(LOCAL:5)%の前に%ANAME(LOCAL:11)%が寝そべっているのが見えた
;						PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(LOCAL:11)%を見下ろしながら、%ANAME(LOCAL:11)%の男性器を踏みつけている…
;						PRINTFORMW %ANAME(LOCAL:5)%が足指で竿を根本から擦り上げると、%ANAME(LOCAL:11)%情けない声を上げながら悶え狂った
;						PRINTFORMW やがて%ANAME(LOCAL:11)%は大きく体を震わせ、%ANAME(LOCAL:5)%の足に大量の白濁液をぶちまけた
;						PRINTFORMW %ANAME(LOCAL:5)%が綺麗にするように命令すると、%ANAME(LOCAL:11)%は陶酔した表情で自身の精液を舐めとっていった
;						PRINTFORMW …………
;					ELSE
;						PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、%ANAME(LOCAL:5)%が%ANAME(LOCAL:11)%の顔の上に跨っているのが見えた
;						PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(LOCAL:11)%の顔に秘唇を押し付けながら、%ANAME(LOCAL:11)%のペニスを手で弄っている…
;						PRINTFORML 感極まった%ANAME(LOCAL:11)%が腰を突き上げ射精しようとすると、
;						PRINTFORMW %ANAME(LOCAL:5)%はシゴく手を止め焦らすように亀頭を撫で始めた
;						PRINTFORMW %ANAME(LOCAL:11)%は激しく悶えながら、おねだりするかのように必死で%ANAME(LOCAL:5)%のワレメに奉仕していた
;						PRINTFORMW …………
;					ENDIF
;				ELSE
;					SELECTCASE RAND:3
;						CASE 0
;							PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、裸の%ANAME(LOCAL:11)%が%ANAME(LOCAL:5)%の前に座っているのが見えた
;							PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(LOCAL:11)%に自慰をさせながら、自身の足を舐めさせ奉仕させている…
;							PRINTFORML %ANAME(LOCAL:5)%が快楽を貪る%ANAME(LOCAL:11)%の淫らな姿を罵ると、
;							PRINTFORMW %ANAME(LOCAL:11)%は興奮した様子でビクリと体を震わせた
;							PRINTFORMW …………
;						CASE 1
;							PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、%ANAME(LOCAL:5)%が%ANAME(LOCAL:11)%を組み敷いている様子が見えた
;							PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(LOCAL:11)%の秘唇に張型を挿し入れ、ゆっくりと抜き差ししている…
;							PRINTFORMW %ANAME(LOCAL:11)%が達しそうになると%ANAME(LOCAL:5)%は張型の動きを緩めて焦らし始めた
;							PRINTFORMW たまらず%ANAME(LOCAL:11)%は甘えるような声で%ANAME(LOCAL:5)%に激しくシてくれるようにおねだりした
;							PRINTFORMW …………
;						CASE 2
;							PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、裸で縛り上げられた%ANAME(LOCAL:11)%の姿が見えた
;							PRINTFORMW %ANAME(LOCAL:5)%は背後から%ANAME(LOCAL:11)%の体をまさぐり、首筋を舌でチロチロと刺激している…
;							PRINTFORML 頃合いを見て%ANAME(LOCAL:5)%が%ANAME(LOCAL:11)%のクリトリスをぎゅっと摘み上げると、
;							PRINTFORMW %ANAME(LOCAL:11)%は快感の喘ぎを上げながらビクビクと体を痙攣させた
;							PRINTFORML 余韻に浸る間もなく、絶頂で敏感になった乳首を摘み上げられ、
;							PRINTFORMW %ANAME(LOCAL:11)%はトロけきった甘い声を上げながら、%ANAME(LOCAL:5)%の与える快感に悶え狂った
;							PRINTFORMW …………
;					ENDSELECT
;				ENDIF
;			;レズＳＭパッチ有効
;			ELSE
;				SELECTCASE RAND:4
;					CASE 0
;						PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、四つん這いの状態で縛られている%ANAME(LOCAL:11)%の姿が見えた
;						PRINTFORML %ANAME(LOCAL:5)%は後ろから%ANAME(LOCAL:11)%の首に付けられた首輪のリードを引き、
;						PRINTFORMW %ANAME(LOCAL:11)%の尻を目掛けて幾度となく鞭を振るっている
;						PRINTFORMW %ANAME(LOCAL:11)%は口枷を嵌められており、くぐもった悲鳴を上げながら%ANAME(LOCAL:5)%の責めに耐えている
;						PRINTFORMW やがて%ANAME(LOCAL:5)%は手を休めると、すっかり赤く腫れ上がった%ANAME(LOCAL:11)%の尻に舌を這わせ舐め上げた
;						PRINTFORML %ANAME(LOCAL:11)%は痛みと快感の混じり合った甘美な感覚に、
;						PRINTFORMW ビクビクと体を震わせながら激しく悶え、枷を嵌められた口から涎を垂らした
;						PRINTFORMW …………
;					CASE 1
;						PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、%ANAME(LOCAL:11)%が張型の付いた木馬に乗せられているのが見えた
;						PRINTFORML さらに%ANAME(LOCAL:11)%の乳首は紐で縛られ、その紐の先に鈴が吊り下げられている
;						PRINTFORMW %ANAME(LOCAL:5)%が鈴を指先で鳴らすと、その度に%ANAME(LOCAL:11)%はビクビクと体を震わせている
;						PRINTFORMW 続いて%ANAME(LOCAL:5)%が木馬を揺らし始めると、%ANAME(LOCAL:11)%は痛みとも快楽ともつかぬ悲鳴を上げて悶えた
;						PRINTFORMW %ANAME(LOCAL:5)%に尻を平手で強く叩かれると、%ANAME(LOCAL:11)%は一際高い声を上げてガクガクと痙攣した
;						PRINTFORMW %ANAME(LOCAL:5)%は絶頂に震える%ANAME(LOCAL:11)%に構わず、さらに木馬を揺らし責めを激しくした
;						PRINTFORMW …………
;					CASE 2
;						PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、%ANAME(LOCAL:11)%がベッドに大の字で縛りつけられているのが見えた
;						PRINTFORMW %ANAME(LOCAL:5)%は火のついた蝋燭を取り出すと、溶けた蝋を%ANAME(LOCAL:11)%の体にポタポタと垂らした
;						PRINTFORMW 熱さと痛みのあまり、口枷と目隠しをされた%ANAME(LOCAL:11)%はビクビクと体を震わせる
;						PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(LOCAL:11)%に嘲るような言葉を掛けながら、クリトリスへと蝋を落とす
;						PRINTFORMW %ANAME(LOCAL:11)%は強烈すぎる刺激に身体を仰け反らせ、口枷の奥から狂ったような呻きを上げて絶頂した
;						PRINTFORMW %ANAME(LOCAL:5)%は嗜虐的に微笑むと、再び%ANAME(LOCAL:11)%の体へと蝋を傾けた
;						PRINTFORMW …………
;					CASE 3
;						PRINTFORMW %ANAME(MASTER)%が部屋の中を覗くと、%ANAME(LOCAL:11)%が足を広げた姿勢で椅子に縛り付けられているのが見えた
;						PRINTFORMW %ANAME(LOCAL:11)%の秘所と菊座には張型が挿れられ、%ANAME(LOCAL:5)%がそれをグチュグチュ水音を立てて弄んでいる
;						PRINTFORML 既にどれほどの時間責められ続けていたのか、%ANAME(LOCAL:11)%は快楽のあまり虚ろな目をしており、
;						PRINTFORMW 枷によって開き切った口からは涎が絶え間なく流れ、時折弱々しい呻きが漏れている
;						PRINTFORMW やがて%ANAME(LOCAL:5)%が張型を勢いよく引き抜くと、%ANAME(LOCAL:11)%の秘所が勢い良く潮を吹いた
;						PRINTFORMW %ANAME(LOCAL:5)%は顔に掛かった潮を舐めると、妖しい笑みを浮かべて次の責め具を取りだした
;						PRINTFORMW …………
;				ENDSELECT
;			ENDIF
;		ENDIF
;	ENDIF
;
;;覗き見イベントが発生しなかった場合
;ELSE
;	LOCAL:6 = LIST_SEX:0
;	IF NUM_SEX == 1 && !TALENT:(LOCAL:6):藍の恋人 && !TALENT:(LOCAL:6):藍に恋慕 && CFLAG:(LOCAL:5):1 == CFLAG:MASTER:1 && CFLAG:MASTER:9 == 0
;		PRINTFORMW %ANAME(LOCAL:6)%は%ANAME(LOCAL:5)%につまみ食いされてしまったようです…
;	ENDIF
;ENDIF
;
;
;
;;-------------------------------------------------
;;タチ役による捕虜の調教
;;-------------------------------------------------
;@EVENT_OTHERS_RAN_PRISONER
;#DIM TARGET_LIST, 100
;#DIM TARGET_NUM
;
;;コンフィグがOFFなら戻る
;IF CONFIG:81 == 0
;	RETURN
;ENDIF
;
;LOCAL:8 = 0
;
;;タチ役をCONFIG:88で設定
;LOCAL:5 = CONFIG:88
;LOCAL:4 = GET_COUNTRY_FROM_BOSS_ID(GET_ID(LOCAL:5))
;
;;タチ役が不在or主人公or捕虜 または タチ役が君主でない なら戻る
;IF LOCAL:5 < 0 || LOCAL:5 == MASTER || CFLAG:(LOCAL:5):9 != 0 || LOCAL:4 <= 0
;	RETURN
;ENDIF
;
;;現在調教中の捕虜を取得
;LOCAL:6 = ID_TO_CHARA(CFLAG:(LOCAL:5):45)
;IF LOCAL:6 >= 0
;	;調教条件を満たしていなければ解除
;	IF CONFIG:81 == 1 && IS_MALE(LOCAL:6)
;		LOCAL:6 = -1
;	ENDIF
;	IF CFLAG:(LOCAL:6):9 != LOCAL:4
;		LOCAL:6 = -1
;	ENDIF
;ENDIF
;
;;調教中の捕虜がいない場合は新たに設定
;IF LOCAL:6 < 0
;	TARGET_NUM = 0
;	FOR LOCAL:0, 0, CHARANUM
;		;タチ役勢力の捕虜を調べる
;		IF LOCAL:0 != MASTER && CFLAG:(LOCAL:0):9 == LOCAL:4 && TARGET_NUM < 100 && !(CONFIG:81 == 1 && IS_MALE(LOCAL:0))
;			TARGET_LIST:TARGET_NUM = LOCAL:0
;			TARGET_NUM ++
;		ENDIF
;	NEXT
;	IF TARGET_NUM >= 1
;		;該当する捕虜の中からランダムで調教対象を選択
;		LOCAL:6 = TARGET_LIST:(RAND:TARGET_NUM)
;		CFLAG:(LOCAL:5):45 = GET_ID(LOCAL:6)
;	ELSE
;		LOCAL:6 = -1
;		CFLAG:(LOCAL:5):45 = 0
;	ENDIF
;ENDIF
;
;;それでも調教対象がいなければ戻る
;IF LOCAL:6 < 0
;	RETURN
;ENDIF
;
;IF CFLAG:(LOCAL:6):1 >= 1 && CFLAG:(LOCAL:6):1 == CFLAG:MASTER:1
;	;調教メッセージの表示
;	CALL SINGLE_DRAWLINE
;	PRINTFORMW %ANAME(LOCAL:5)%勢力の捕虜となっている%ANAME(LOCAL:6)%が%ANAME(LOCAL:5)%による調教を受けているようです…
;	PRINTL 
;	;膣がない場合(男性など)
;	IF !VAGINA(LOCAL:6)
;		IF RAND:2 == 0
;			PRINTFORMW %ANAME(LOCAL:6)%は、仰向けの状態で拘束台に腕を縛り付けられている…
;			PRINTFORML %ANAME(LOCAL:5)%は%ANAME(LOCAL:6)%の尻穴にローションを塗り付けると、
;			IF HAS_PENIS(LOCAL:5)
;				PRINTFORMW 自身のペニスで%ANAME(LOCAL:6)%のアナルを犯し始めた
;			ELSE
;				PRINTFORMW ペニスバンドを装着して%ANAME(LOCAL:6)%のアナルを犯し始めた
;			ENDIF
;			PRINTFORML %ANAME(LOCAL:6)%は必死に屈辱と快感を堪えているが、%ANAME(LOCAL:5)%が竿を強く握って擦り上げると、
;			PRINTFORMW %ANAME(LOCAL:6)%は一瞬で絶頂に押し上げられ、甘い声を上げながらビクビクと体を震わせて射精してしまった
;			PRINTFORMW すぐさま再開された%ANAME(LOCAL:5)%の腰使いの前に、%ANAME(LOCAL:6)%は精と共に抵抗する気力を搾り取られていった…
;		ELSE
;			PRINTFORMW %ANAME(LOCAL:6)%は後ろ手に縛られた状態で、仰向けにベッドに寝かされている…
;			PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(LOCAL:6)%の亀頭をゆっくりと掌で撫で回しながら、乳首を舐め上げている
;			PRINTFORML 射精を懇願する%ANAME(LOCAL:6)%に対し、%ANAME(LOCAL:5)%は誘惑するような声色で、
;			PRINTFORMW 忠誠を誓えば好きなだけ射精させてあげると言って、%ANAME(LOCAL:6)%の竿を優しく焦らすように擦り上げた
;			PRINTFORML 耐えかねた%ANAME(LOCAL:6)%が仮初めの忠誠を声に出して誓うと、
;			PRINTFORMW %ANAME(LOCAL:5)%はご褒美と称して%ANAME(LOCAL:6)%の竿を激しくシゴき上げ、射精させた…
;		ENDIF
;
;	;ふたなりの場合
;	ELSEIF HAS_PENIS(LOCAL:6)
;		IF TALENT:(LOCAL:6):処女 == 1
;			PRINTFORMW %ANAME(LOCAL:6)%は、仰向けの状態で拘束台に腕を縛り付けられている…
;			PRINTFORML %ANAME(LOCAL:5)%は%ANAME(LOCAL:6)%の秘唇にローションを塗り付けると、
;			IF HAS_PENIS(LOCAL:5)
;				PRINTFORMW 自身のペニスでまだ乙女の%ANAME(LOCAL:6)%のワレメを貫いた
;			ELSE
;				PRINTFORMW ペニスバンドを装着してまだ乙女の%ANAME(LOCAL:6)%のワレメを貫いた
;			ENDIF
;			PRINTFORML %ANAME(LOCAL:6)%は破瓜の痛みに呻き声を上げながら、睨むように%ANAME(LOCAL:5)%の顔を見上げるが、
;			PRINTFORMW %ANAME(LOCAL:5)%は腰を動かさず、ゆっくりと%ANAME(LOCAL:6)%の竿を弄り始めた
;			PRINTFORMW ペニスへの優しい愛撫に、%ANAME(LOCAL:6)%は痛みも忘れて甘い声を上げてしまう
;			PRINTFORML %ANAME(LOCAL:6)%が十分高まったのを確認した%ANAME(LOCAL:5)%が、一度だけ腰を奥まで打ち付けると、
;			PRINTFORMW %ANAME(LOCAL:6)%は可愛らしい声を上げ、ペニスから精液を吹き出してガクガクと体を痙攣させた
;			PRINTFORMW すっかり放心状態の%ANAME(LOCAL:6)%に対し、%ANAME(LOCAL:5)%は本格的に腰を動かして女の快感を教え始めた…
;		ELSEIF RAND:2 == 0
;			PRINTFORMW %ANAME(LOCAL:6)%は、仰向けの状態で拘束台に腕を縛り付けられている…
;			PRINTFORML %ANAME(LOCAL:5)%は%ANAME(LOCAL:6)%の秘唇にローションを塗り付けると、
;			IF HAS_PENIS(LOCAL:5)
;				PRINTFORMW 自身のペニスで%ANAME(LOCAL:6)%を犯し始めた
;			ELSE
;				PRINTFORMW ペニスバンドを装着して%ANAME(LOCAL:6)%を犯し始めた
;			ENDIF
;			PRINTFORML %ANAME(LOCAL:6)%は必死に屈辱と快感を堪えているが、%ANAME(LOCAL:5)%が竿を強く握って擦り上げると、
;			PRINTFORMW %ANAME(LOCAL:6)%は一瞬で絶頂に押し上げられ、甘い声を上げながらガクガクと体を震わせて射精してしまった
;			PRINTFORMW すぐさま再開された%ANAME(LOCAL:5)%の腰使いの前に、%ANAME(LOCAL:6)%の抵抗の意志は甘く溶かされていった…
;		ELSE
;			PRINTFORMW %ANAME(LOCAL:6)%は前と後ろの穴に張り型を挿れられた状態で、後ろ手に縛られて四つん這いにさせられている…
;			PRINTFORML %ANAME(LOCAL:5)%はヴァギナとアナルの快感で既に反り立った%ANAME(LOCAL:6)%のペニスを、
;			PRINTFORMW 痛みを感じるほど強く手で握り、容赦なくシゴき上げた
;			PRINTFORMW あまりの快感に、%ANAME(LOCAL:6)%は数擦りも耐えられずに大量の精液を漏らしてしまった
;			PRINTFORMW だが%ANAME(LOCAL:5)%は手を止めることなく、時々張り型を奥へ押し込みながら%ANAME(LOCAL:6)%のペニスをシゴき続ける
;			PRINTFORMW %ANAME(LOCAL:6)%は狂ったように悶えながら、まるで乳牛のように白いミルクを絞り出されていった…
;		ENDIF
;
;	;女性の場合
;	ELSE
;		IF TALENT:(LOCAL:6):処女 == 1
;			PRINTFORMW %ANAME(LOCAL:6)%は、仰向けの状態で拘束台に腕を縛り付けられている…
;			PRINTFORML %ANAME(LOCAL:5)%は%ANAME(LOCAL:6)%の秘唇にローションを塗り付けると、
;			IF HAS_PENIS(LOCAL:5)
;				PRINTFORMW 自身のペニスでまだ乙女の%ANAME(LOCAL:6)%のワレメを貫いた
;			ELSE
;				PRINTFORMW ペニスバンドを装着してまだ乙女の%ANAME(LOCAL:6)%のワレメを貫いた
;			ENDIF
;			PRINTFORML %ANAME(LOCAL:6)%は破瓜の痛みに呻き声を上げながら、睨むように%ANAME(LOCAL:5)%の顔を見上げるが、
;			PRINTFORMW %ANAME(LOCAL:5)%は腰を動かさず、ゆっくりと%ANAME(LOCAL:6)%のクリトリスを弄り始めた
;			PRINTFORMW クリへの優しい快楽刺激に、%ANAME(LOCAL:6)%は痛みも忘れて甘い声を上げてしまう
;			PRINTFORML %ANAME(LOCAL:6)%が十分高まったのを確認した%ANAME(LOCAL:5)%が、クリを強く抓りながら一度だけ腰を奥まで打ち付けると、
;			PRINTFORMW %ANAME(LOCAL:6)%は可愛らしい声を上げ、ガクガクと体を痙攣させて絶頂してしまった
;			PRINTFORMW すっかり放心状態の%ANAME(LOCAL:6)%に対し、%ANAME(LOCAL:5)%は本格的に腰を動かして女の快感を教え始めた…
;		ELSEIF RAND:2 == 0
;			PRINTFORMW %ANAME(LOCAL:6)%は股を大きく開かれた状態で拘束台に縛り付けられている…
;			PRINTFORML %ANAME(LOCAL:5)%はあらわになった%ANAME(LOCAL:6)%のワレメにローションを塗り付けると、
;			IF HAS_PENIS(LOCAL:5)
;				PRINTFORMW 自身のペニスで%ANAME(LOCAL:6)%の膣を犯し始めた
;			ELSE
;				PRINTFORMW ペニスバンドを装着して%ANAME(LOCAL:6)%の膣を犯し始めた
;			ENDIF
;			PRINTFORML %ANAME(LOCAL:6)%は必死に屈辱と快感を堪えているが、%ANAME(LOCAL:5)%の巧みな腰使いの前になすすべもなく高められ、
;			PRINTFORMW やがてあられもない声を上げながらガクガクと体を震わせて絶頂した
;			PRINTFORMW 達した直後の敏感な膣内を突き上げられると、%ANAME(LOCAL:6)%は狂ったように悶えながら女の快楽に溺れていった…
;		ELSE
;			PRINTFORMW %ANAME(LOCAL:6)%は前と後ろの穴に張り型を挿れられた状態で、後ろ手に縛られて四つん這いにさせられている…
;			PRINTFORMW %ANAME(LOCAL:5)%は鞭を構え、%ANAME(LOCAL:6)%の背中を何度も、痣が出来るほど激しく打ち据えた
;			PRINTFORML %ANAME(LOCAL:6)%は苦しそうな声を上げているが、鞭で打たれるたびに体が張り型を締め付けてしまい、
;			PRINTFORMW 時折甘い喘ぎが混じるのを抑えられないでいる
;			PRINTFORMW %ANAME(LOCAL:5)%が手を止め、張り型を足でグリグリと奥へ押し込むと、%ANAME(LOCAL:6)%の膣から愛液がぴしゃりと飛び散った…
;		ENDIF
;	ENDIF
;
;;他国の士官は簡易メッセージ
;ELSE
;	PRINTL 
;	LOCAL:7 = GET_COUNTRY_BOSS(CFLAG:(LOCAL:6):1)
;	IF LOCAL:7 >= 0
;		PRINTFORML %ANAME(LOCAL:5)%勢力の捕虜となっている%NAME:(LOCAL:7)%勢力の%ANAME(LOCAL:6)%が%ANAME(LOCAL:5)%による調教を受けているようです…
;	ELSE
;		PRINTFORML %ANAME(LOCAL:5)%勢力の捕虜となっている%ANAME(LOCAL:6)%が%ANAME(LOCAL:5)%による調教を受けているようです…
;	ENDIF
;	LOCAL:8 = 1
;ENDIF
;
;IF VAGINA(LOCAL:6)
;	CALL VIRGIN_COMMON(LOCAL:6, @"%ANAME(LOCAL:5)%")
;	EXP:(LOCAL:6):Ｖ感経験値 += (ABL:(LOCAL:6):Ｖ感 + 1) * (RAND:4 + 3)
;
;	IF HAS_PENIS(LOCAL:5)
;		CALL DOUTEI_COMMON(LOCAL:5, @"%ANAME(LOCAL:6)%の膣")
;	ENDIF
;ENDIF
;
;IF ABL:(LOCAL:5):主導度Ｕ < 800
;	ABL:(LOCAL:5):主導度Ｕ += 1
;ENDIF
;IF ABL:(LOCAL:6):主導度Ｕ > -500
;	ABL:(LOCAL:6):主導度Ｕ -= 10
;ENDIF
;EXP:(LOCAL:6):Ｃ感経験値 += (ABL:(LOCAL:6):Ｃ感 + 1) * (RAND:4 + 3)
;EXP:(LOCAL:6):Ｂ感経験値 += (ABL:(LOCAL:6):Ｂ感 + 1) * (RAND:4 + 3)
;EXP:(LOCAL:6):Ａ感経験値 += (ABL:(LOCAL:6):Ａ感 + 1) * (RAND:4 + 3)
;EXP:(LOCAL:6):奉仕経験値 += ABL:(LOCAL:6):奉仕 * (RAND:3)
;EXP:(LOCAL:5):奉仕経験値 += ABL:(LOCAL:5):奉仕 * (RAND:7)
;EXP:(LOCAL:6):欲望経験値 += ABL:(LOCAL:6):欲望 * (RAND:4 + 4)
;EXP:(LOCAL:5):欲望経験値 += ABL:(LOCAL:5):欲望 * (RAND:2 + 1)
;LOCAL:8 = MIN(ABL:(LOCAL:6):欲望 + 1, 11) * (RAND:21 + 20)
;EXP:(LOCAL:6):性技経験値 += LOCAL:8 / 15
;EXP:(LOCAL:5):性技経験値 += LOCAL:8 / 8
;EXP:(LOCAL:6):性交経験値 += LOCAL:8 / 30
;EXP:(LOCAL:5):性交経験値 += LOCAL:8 / 30
;EXP:(LOCAL:6):絶頂経験 += LOCAL:8 / 8
;IF HAS_PENIS(LOCAL:6)
;	EXP:(LOCAL:6):射精経験値 += LOCAL:8 / 20
;	EXP:(LOCAL:5):精愛経験値 += LOCAL:8 / 40
;ENDIF
;IF HAS_PENIS(LOCAL:5)
;	EXP:(LOCAL:5):絶頂経験 += LOCAL:8 / 20
;;	EXP:(LOCAL:5):射精経験値 += LOCAL:8 / 20
;	EXP:(LOCAL:6):精愛経験値 += LOCAL:8 / 20
;ENDIF
;LOCAL:7 = RAND:4 + 1
;EXP:(LOCAL:6):露出経験値 += RAND:4
;CALL TRAIN_AUTO_ABLUP(LOCAL:5, 1)
;CALL TRAIN_AUTO_ABLUP(LOCAL:6, 1)
;
;;調教対象の解放カウンタをリセット
;CFLAG:(LOCAL:6):43 = 0
;
;;調教されたキャラはタチ役への好感度が上昇
;CFLAG:(LOCAL:6):38 = MIN(CFLAG:(LOCAL:6):38 + 150, 5000)
;
;;依存度が減少
;IF CFLAG:(LOCAL:6):3 > -1000
;	CFLAG:(LOCAL:6):3 = MAX(CFLAG:(LOCAL:6):3 - MAX(CFLAG:(LOCAL:6):3 / 20, 200), -1000)
;ENDIF
;
;;依存度が-500以下になると寝返る
;IF CFLAG:(LOCAL:6):3 <= -500
;	;自国の士官の場合
;	IF CFLAG:(LOCAL:6):1 >= 1 && CFLAG:(LOCAL:6):1 == CFLAG:MASTER:1
;		PRINTFORMW すっかり%ANAME(LOCAL:5)%の虜となった%ANAME(LOCAL:6)%は、仲間を裏切り%ANAME(LOCAL:5)%への忠誠を誓ってしまった
;		CALL LOSE_RELATION_TALENT(LOCAL:6, 1, 1)
;		SETCOLOR COLOR("注意")
;		PRINTFORMW %ANAME(LOCAL:6)%は%ANAME(LOCAL:5)%勢力に寝返ってしまったようです…
;		RESETCOLOR
;	;他国の士官の場合
;	ELSE
;		PRINTFORMW %ANAME(LOCAL:6)%は%ANAME(LOCAL:5)%勢力に寝返った模様です
;	ENDIF
;
;	CFLAG:(LOCAL:6):9 = 0
;	CFLAG:(LOCAL:6):1 = LOCAL:4
;
;	;捕虜調教のターゲットを解除
;	CFLAG:(LOCAL:5):45 = 0
;
;	LOCAL:8 = 0
;ENDIF
;
;IF LOCAL:8
;	WAIT
;ENDIF
;
;
