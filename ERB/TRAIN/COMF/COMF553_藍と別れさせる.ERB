;タチ役と別れさせる
;タチ役をCONFIG:88で設定
;
;;-------------------------------------------------
;;コマンド名称
;;-------------------------------------------------
;@COM_NAME553
;LOCAL:0 = CONFIG:88
;IF LOCAL:0 >= 0
;	RESULTS:0 = %ANAME(LOCAL:0)%と別れさせる
;ELSE
;	RESULTS:0 = !ERR!　と別れさせる
;ENDIF
;
;;-------------------------------------------------
;;選択可否判定
;;-------------------------------------------------
;@COM_ABLE553
;;共通部分
;CALL COM_ABLE_COMMON(553)
;SIF RESULT == 0
;	RETURN 0
;;プレイヤーは最大で1人まで
;SIF MPLY_NUM <= 0 || MPLY_NUM > 1
;	RETURN 0
;;ターゲットは最大で1人まで
;SIF MTAR_NUM <= 0 || MTAR_NUM > 1
;	RETURN 0
;;主人公がプレイヤーのとき限定
;SIF MPLY:0 != MASTER
;	RETURN 0
;;タチ役の恋人のとき限定
;SIF !TALENT:(MTAR:0):藍の恋人
;	RETURN 0
;;別れさせフラグが立っていると不可
;SIF CFLAG:(MTAR:0):39
;	RETURN 0
;;プレイヤーの口が塞がっているなら不可
;SIF IS_M_HOLD(MPLY:0)
;	RETURN 0
;RETURN 1
;
;;-------------------------------------------------
;;メイン処理
;;-------------------------------------------------
;@COM553
;;●実行値の計算
;CVARSET TCVAR, 24, -10000
;CVARSET TCVAR, 25, 10000
;
;TCVAR:(MTAR:0):25 = 100
;
;CALL COM_ORDER_BASE(MTAR:0)
;
;IF TALENT:(MTAR:0):臆病
;	CALL COM_ORDER_ELEMENT(MTAR:0, "臆病", 10)
;ENDIF
;IF TALENT:(MTAR:0):反抗的
;	CALL COM_ORDER_ELEMENT(MTAR:0, "反抗的", -5)
;ENDIF
;IF TALENT:(MTAR:0):気丈
;	CALL COM_ORDER_ELEMENT(MTAR:0, "気丈", -10)
;ENDIF
;IF TALENT:(MTAR:0):素直
;	CALL COM_ORDER_ELEMENT(MTAR:0, "素直", 5)
;ENDIF
;IF TALENT:(MTAR:0):プライド高い
;	CALL COM_ORDER_ELEMENT(MTAR:0, "プライド高い", -15)
;ENDIF
;IF TALENT:(MTAR:0):プライド低い
;	CALL COM_ORDER_ELEMENT(MTAR:0, "プライド低い", 5)
;ENDIF
;IF TALENT:(MTAR:0):孤高
;	CALL COM_ORDER_ELEMENT(MTAR:0, "孤高", -(20 * KOKOU_RATE(MTAR:0) / 100))
;ENDIF
;IF TALENT:(MTAR:0):貞操観念
;	CALL COM_ORDER_ELEMENT(MTAR:0, "貞操観念", -12)
;ENDIF
;IF TALENT:(MTAR:0):貞操無頓着
;	CALL COM_ORDER_ELEMENT(MTAR:0, "貞操無頓着", 7)
;ENDIF
;
;IF TALENT:(MTAR:0):親愛
;	CALL COM_ORDER_ELEMENT(MTAR:0, "親愛", 70)
;ELSEIF TALENT:(MTAR:0):恋慕
;	CALL COM_ORDER_ELEMENT(MTAR:0, "恋慕", 40)
;ELSEIF TALENT:(MTAR:0):親友
;	CALL COM_ORDER_ELEMENT(MTAR:0, "親友", 20)
;ENDIF
;
;IF TALENT:(MTAR:0):隷属
;	CALL COM_ORDER_ELEMENT(MTAR:0, "隷属", 70)
;ELSEIF TALENT:(MTAR:0):服従
;	CALL COM_ORDER_ELEMENT(MTAR:0, "服従", 40)
;ENDIF
;
;IF TALENT:(MTAR:0):烙印
;	CALL COM_ORDER_ELEMENT(MTAR:0, "烙印", 15)
;ENDIF
;
;LOCAL:0 = GET_PALAMLV(PALAM:(MTAR:0):欲情)
;IF LOCAL:0 >= 1
;	CALL COM_ORDER_ELEMENT(MTAR:0, @"欲情Lv{LOCAL:0}", MIN(LOCAL:0 * 2, 30))
;ENDIF
;
;IF TALENT:(MTAR:0):プライド高い
;	LOCAL:0 = 4
;ELSEIF TALENT:(MTAR:0):プライド低い
;	LOCAL:0 = 1
;ELSE
;	LOCAL:0 = 2
;ENDIF
;
;IF MARK:(MTAR:0):既成事実 >= 1
;	CALL COM_ORDER_ELEMENT(MTAR:0, @"既成事実Lv{MARK:(MTAR:0):既成事実}", MARK:(MTAR:0):既成事実 * 3 * LOCAL:0)
;ENDIF
;
;CALL COM_ORDER_ELEMENT(MTAR:0, ANAME(CONFIG:88) + "に恋慕", -50)
;
;;判定結果を表示
;PRINTFORMW  = {TCVAR:(MTAR:0):24} %TOSTR_COMPARE(TCVAR:(MTAR:0):24, TCVAR:(MTAR:0):25)% 実行値{TCVAR:(MTAR:0):25}
;
;;判定成功
;IF TCVAR:(MTAR:0):24 >= TCVAR:(MTAR:0):25
;	CFLAG:(MTAR:0):39 = 1
;
;	;口上表示
;	CALL KOJO_EVENT(MTAR:0, 413)
;	SOURCE:(MTAR:0):屈従 += 500
;	SOURCE:(MTAR:0):恐怖 += 100
;
;	CFLAG:(MTAR:0):38 = MIN(CFLAG:(MTAR:0):38, 500)
;
;;判定失敗
;ELSE
;	;口上表示
;	CALL KOJO_EVENT(MTAR:0, 414)
;	SOURCE:(MTAR:0):反感 += 5000
;ENDIF
;
;RETURN 1
;