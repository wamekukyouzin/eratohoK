;料理を作る

;-------------------------------------------------
;コマンド名称
;-------------------------------------------------
@COM_NAME504
IF LOCAL:0 >= 0 && CFLAG:(MTAR:0):28 <= 2
	RESULTS = 料理を作る
ELSE
	RESULTS = 料理を作る
ENDIF

;-------------------------------------------------
;選択可否判定
;-------------------------------------------------
@COM_ABLE504
;共通部分
CALL COM_ABLE_COMMON(504)
SIF RESULT == 0
	RETURN 0
;主人公がプレイヤーのとき限定
SIF MPLY:0 != MASTER
	RETURN 0
RETURN 1

;-------------------------------------------------
;メイン処理
;-------------------------------------------------
@COM504
;料理を作る
IF LOCAL:0 >= 0 && CFLAG:(MTAR:0):28 <= 2
	;成長度を加算
	CFLAG:(MTAR:0):134 += POWER(2, 5 - CFLAG:(MTAR:0):114)
	;選択回数を加算
	CFLAG:(MTAR:0):114 ++

;料理を作る
ELSE
	;成長度を加算
	CFLAG:(MTAR:0):144 += (150 + ABL:(MPLY:0):料理) / POWER(2, MAX(CFLAG:(MTAR:0):124, 63))
	;選択回数を加算
	CFLAG:(MTAR:0):124 ++
ENDIF

RETURN 1

;-------------------------------------------------
;地の文(前文)
;-------------------------------------------------
@COM_TEXT_BEFORE504
;料理を作る
IF LOCAL:0 >= 0 && CFLAG:(MTAR:0):28 <= 2
	PRINTFORMW %ANAME(MPLY:0)%は%ANAME(MTAR:0)%と一緒に料理を作ることにした

;料理を作る
ELSE
	PRINTFORMW %ANAME(MPLY:0)%は%ANAME(MTAR:0)%と一緒に料理を作ることにした
ENDIF

;--------------------------------------------------------
;地の文(パラメータ・刻印変動後)
;--------------------------------------------------------
@COM_TEXT_LAST504
