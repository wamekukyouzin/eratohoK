;拉致

;-------------------------------------------------
;コマンド名称
;-------------------------------------------------
@COM_NAME557
RESULTS:0 = 不思議な懐中時計

;-------------------------------------------------
;選択可否判定
;-------------------------------------------------
@COM_ABLE557
;共通部分
CALL COM_ABLE_COMMON(557)
SIF RESULT == 0
	RETURN 0
;プレイヤーは最大で1人まで
SIF MPLY_NUM != 1
	RETURN 0
;プレイヤーに主人公が含まれる
SIF MPLY:0 != MASTER
	RETURN 0
;アイテムが必要
SIF ITEM:不思議な懐中時計 <= 0
	RETURN 0

IF TFLAG:56 > 20 && GROUPMATCH(FLAG:調教モード, 調教_閨, 調教_捕虜調教)
	RETURN 0
ELSEIF TFLAG:56 > 10 && GROUPMATCH(FLAG:調教モード, 調教_会う, 調教_子育て)
	RETURN 0
ENDIF

RETURN 1

;-------------------------------------------------
;メイン処理
;-------------------------------------------------
@COM557

CALL COLOR_PRINTL("不思議な懐中時計が効果を発揮した！", カラー_注意)
ITEM:不思議な懐中時計 --

IF GROUPMATCH(FLAG:調教モード, 調教_閨, 調教_捕虜調教)
	TFLAG:56 += 10
ELSE
	TFLAG:56 += 5
ENDIF

TFLAG:55 --

RETURN 1

;-------------------------------------------------

;コマンド区分
;-------------------------------------------------

@COM_AVAILABLE_WHEN557
RETURN コマンド_共通
