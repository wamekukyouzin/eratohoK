@KOJO_DAILY_EXIST_K79()

@KOJO_DAILY_K79()
#DIM 発生フラグ
発生フラグ = 0

;托卵
;DAILY_NAME = 托卵
;CALL KOJO_DAILY_K79_TAKURAN
;SIF RESULT != -1
;	発生フラグ = 1
;SIF DAILY_CANCEL
;	RETURN 1
;RETURN 発生フラグ


;----------------------------------
;挨拶
;----------------------------------
@KOJO_DAILY_K79_GREETINGS()



;----------------------------------
;托卵
;----------------------------------
;@KOJO_DAILY_K79_TAKURAN
;#DIM はたて
;#DIM アップ能力
;
;はたて = NAME_TO_CHARA("はたて")
;
;;はたてが同一勢力所属で捕虜でなく死んでいないなら3.5%で発生
;SIF !CHECK_KOJO_DAILY_HAPPEN(はたて, 35, 1, 0, 1)
;	RETURN -1
;
;;陥落済みならやります
;SIF !(TALENT:はたて:恋慕 == 1 || TALENT:はたて:親愛 == 1 || TALENT:はたて:服従 == 1 || TALENT:はたて:隷属 == 1 || TALENT:はたて:恋人 == 1 || TALENT:はたて:烙印 == 1)
;	RETURN -1
;
;CALL KOJO_DAILY_START
;IF KDVAR:はたて:はたて_托卵 == 0
;	PRINTFORMW 「ねぇ、ちょっといい？」
;	PRINTFORML %ANAME(MASTER)%の部屋に、こそこそと%ANAME(はたて)%が入ってきた
;	PRINTFORMW 何やら大きめの籠を抱えており、要件はそれに関することのようだった
;	PRINTFORMW 「あのね、実はお願いがあって……」
;	PRINTFORML 「この籠の中身なんだけど、なんだと思う？」
;	PRINTFORML 「これね、私の卵なの。無精卵だから、温めても何にもならないんだけどね」
;	PRINTFORMW 「もう1人じゃ処理できないから、協力して欲しくて……」
;	PRINTFORMW %ANAME(MASTER)%は卵の処理くらい安いものだと、快く引き受けた
;	PRINTFORML 「ホント？　ありがとう！　%ANAME(MASTER)%が優しくて、私本当に幸せ……！」
;	PRINTFORMW %ANAME(はたて)%はそう言うと、%ANAME(MASTER)%にキュッと抱きついた
;	PRINTFORMW %ANAME(はたて)%と持ってきた卵を見て、%ANAME(MASTER)%はふと思った
;	PRINTFORMW 烏天狗の卵とは、人間で言えば卵子にあたるのだろうか
;	PRINTFORMW もし、そうだとすれば、これは人間の男が食用にザーメンを配布するようなことなのではないだろうか……いや、考えすぎか
;	PRINTFORMW %ANAME(MASTER)%は%ANAME(はたて)%の首筋を撫でながら、卵の調理法を考え始めた……
;ELSE
;	PRINTFORMW 「ねぇ、また、なんだけど……」
;	PRINTFORMW %ANAME(MASTER)%は快く引き受けることにした
;ENDIF
;
;SELECTCASE RAND:100
;	CASE IS < 20
;		アップ能力 = GETNUM(ABL, "武闘")
;	CASE IS < 40
;		アップ能力 = GETNUM(ABL, "知略")
;	CASE IS < 60
;		アップ能力 = GETNUM(ABL, "政治")
;	CASE IS < 80
;		アップ能力 = GETNUM(ABL, "料理")
;	CASEELSE
;		アップ能力 = GETNUM(ABL, "歌唱")
;ENDSELECT
;PRINTFORML %ANAME(はたて)%の卵を味わった！
;CALL COLORPRINTFORM(@"妖怪の卵により、%ANAME(MASTER)%の%ABLNAME:アップ能力%が1上昇した！", COLOR("注意"), "W")
;ABL:MASTER:アップ能力 += 1
;
;KDVAR:はたて:はたて_托卵 = 1