;-------------------------------------------------
;セーブデータのアップデート処理
;-------------------------------------------------
@UPDATE
IF LASTLOAD_VERSION <= 161
	CONFIG:319 = 0
	FOR LOCAL, 0, 200
		IF EXISTCSV(LOCAL)
			CSVTALENT LOCAL, 334
			TALENT:LOCAL:性的嗜好 = RESULT
		ENDIF
	NEXT
ENDIF