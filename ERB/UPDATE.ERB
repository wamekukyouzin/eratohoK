;-------------------------------------------------
;セーブデータのアップデート処理
;-------------------------------------------------
@UPDATE
;スカウト、奴隷市場
IF LASTLOAD_VERSION <= 190
	CALL SLAVEMARKET_PREPARE_GOODS
	CALL SCOUT_PREPARE_CANDIDATES
ENDIF

;触手部屋管理者をID化
IF LASTLOAD_VERSION <= 193
	CALL COLORPRINTFORM("VER0.200になりました", COLOR("注意"), "W")
	CALL COLORPRINTFORM("シナリオ「東方西欧録」の新マップは致命的な不具合を抱えています。使用している場合、強制周回することをおすすめします", COLOR("注意"), "W")
	CALL COLORPRINTFORM("触手部屋の内部的な管理方法が変更されました。管理者を一旦リセットするので、再設定してください", COLOR("注意"), "W")
	FLAG:触手部屋管理者 = 0
	PRINTFORMW CONFIG:200「特殊性癖イベント一括停止フィルタ」は廃止されました
	CONFIG:200 = 0
	PRINTFORMW 好感度補正が作り直されました
	PRINTFORMW ともなって、CONFIG:310「好感度補正のタイプ」、CONFIG:314「敵勢力への戦闘補正」のEXTRA、EXTRA+が廃止されました
	CONFIG:310 = 0
	SIF GROUPMATCH(CONFIG:314, 4, 5)
		CONFIG:314 = 3
	PRINTFORMW 特殊勢力「自警団」団長のNOが修正されました
	FOR LOCAL, 0, MAX_COUNTRY
		IF IS_COUNTRY(LOCAL) && COUNTRY_EVENT_ID:LOCAL == SP_COUNTRY_ID:(特殊勢力_自警団)
			NO:GET_COUNTRY_BOSS(LOCAL) = GET_EMPTY_NO()
		ENDIF
	NEXT
	
ENDIF

IF LASTLOAD_VERSION <= 200
	CALL COLORPRINTFORM("VER0.201になりました", COLOR("注意"), "W")
	CALL COLORPRINTFORM("デイリーイベントのコンフィグ設定に使う変数を整理しました", COLOR("注意"), "W")
	CALL COLORPRINTFORM("設定が書き換わっているので確認して下さい", COLOR("注意"), "W")
	;所属がおかしくなる不具合に対処
	FOR LOCAL, 0, CHARANUM
		IF CFLAG:LOCAL:所属 == SP_COUNTRY_ID:特殊勢力_野盗
			CFLAG:LOCAL:所属 = 0
			CFLAG:LOCAL:特殊状態 = 1
		ENDIF
	NEXT
	FOR LOCAL, 0, CHARANUM
		CFLAG:LOCAL:能力固定フラグ = 0
	NEXT
ENDIF

IF LASTLOAD_VERSION <= 201
	CALL COLORPRINTFORM("VER0.202になりました", COLOR("注意"), "W")
	CALL COLORPRINTFORM("処女、アナル処女、ファーストキス、童貞の内部的な扱いに変化が生じました", COLOR("注意"), "W")
	CALL COLORPRINTFORM("おそらく問題はありませんが、周回時にでもそれらをリセットすることをおすすめします", COLOR("注意"), "W")
	FOR LOCAL, 0, CHARANUM
		CFLAG:LOCAL:93 = 0
	NEXT
ENDIF

IF LASTLOAD_VERSION <= 204
	CALL COLORPRINTFORM("VER0.205になりました", COLOR("注意"), "W")
	CALL COLORPRINTFORM("シナリオ4、8が廃止されました。遊んでた場合は強制周回をおすすめします", COLOR("注意"), "W")
	PRINTFORMW 文の口上デイリー「新聞勧誘」を遊んでいた場合、続くはたての口上デイリーのメッセージに食い違いが出る可能性があります
	PRINTFORMW ささいな不具合で、遊ぶ上での問題はありません
ENDIF
