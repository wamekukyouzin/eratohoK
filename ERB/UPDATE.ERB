;-------------------------------------------------
;セーブデータのアップデート処理
;-------------------------------------------------
@UPDATE
;スカウト、奴隷市場
IF LASTLOAD_VERSION <= 190
	CALL SLAVEMARKET_PREPARE_GOODS
	CALL SCOUT_PREPARE_CANDIDATES
ENDIF

;触手部屋管理者をID化
IF LASTLOAD_VERSION <= 193
	CALL COLORPRINTFORM("触手部屋の内部的な管理方法が変更されました。管理者を一旦リセットするので、再設定してください", COLOR("注意"), "L")
	FLAG:触手部屋管理者 = 0
	PRINTFORML CONFIG:200「特殊性癖イベント一括停止フィルタ」は廃止されました
	CONFIG:200 = 0
ENDIF