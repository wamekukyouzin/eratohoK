;-------------------------------------------------
;セーブデータのアップデート処理
;-------------------------------------------------
@UPDATE
IF LASTLOAD_VERSION < 10001
	CALL COLORPRINT(@"VER1.0.1になりました", カラー_注意, "W")
	PRINTFORML + ささいな便利化
	PRINTFORML + 不具合修正
ENDIF
