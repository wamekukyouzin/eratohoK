;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_149_PERSONAL_0_EXIST
RETURN 1

@SKILL_149_PERSONAL_0_NAME
RESULTS = アブソリュートルーザー

;レベルは1-5まで
@SKILL_149_PERSONAL_0_LEVEL
RETURN 1

;対象選択
@SKILL_149_PERSONAL_0_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
SIF BATTLE_COMMANDER_NUM:対象側 == 0
	RETURN 0
COMBAT_SKILL_TARGET = RAND:(BATTLE_COMMANDER_NUM:対象側)
RETURN 1

;発動判定
@SKILL_149_PERSONAL_0_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN BATTLE_COMMANDER_NUM:発動側 > 1 && BATTLE_COMMANDER_NUM:対象側 >= 1 

;発動テキストをオーバライドしたいときに。
;「誰それのスキル発動！　○○した！」の「○○した！」の部分を実装したい場合は、
;これじゃなくてINVOKEで書けばいいです。
;@SKILL_149_PERSONAL_0_INVOKE_TEXT(発動者, スキル, ジャンル)
;#DIM 発動者
;#DIM スキル
;#DIMS ジャンル

;効果をここに記述
@SKILL_149_PERSONAL_0_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側

FOR LOCAL, 0, BATTLE_COMMANDER_NUM:発動側
	SIF LOCAL == 発動番号
		CONTINUE
	LOCAL:1 = LOCAL
NEXT

PRINTFORML %ANAME(発動者)%の力で%ANAME(BATTLE_COMMANDER:発動側:(LOCAL:1))%と%ANAME(BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)%が入れ替わった！

LOCAL:2 = BATTLE_COMMANDER:発動側:(LOCAL:1)
LOCAL:3 = BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET

BATTLE_COMMANDER:発動側:(LOCAL:1) = LOCAL:3
BATTLE_武闘:発動側:(LOCAL:1) = ABL:(LOCAL:3):武闘
BATTLE_防衛:発動側:(LOCAL:1) = ABL:(LOCAL:3):防衛
BATTLE_知略:発動側:(LOCAL:1) = ABL:(LOCAL:3):知略
BATTLE_政治:発動側:(LOCAL:1) = ABL:(LOCAL:3):政治
BATTLE_歌唱:発動側:(LOCAL:1) = ABL:(LOCAL:3):歌唱
BATTLE_料理:発動側:(LOCAL:1) = ABL:(LOCAL:3):料理
BATTLE_妖術:発動側:(LOCAL:1) = ABL:(LOCAL:3):妖術
BATTLE_武闘パワー:発動側:(LOCAL:1) = ABL_POWER(BATTLE_武闘:発動側:(LOCAL:1), BATTLE_COMMANDER:発動側:(LOCAL:1))
BATTLE_防衛パワー:発動側:(LOCAL:1) = ABL_POWER(BATTLE_防衛:発動側:(LOCAL:1), BATTLE_COMMANDER:発動側:(LOCAL:1))
BATTLE_知略パワー:発動側:(LOCAL:1) = ABL_POWER(BATTLE_知略:発動側:(LOCAL:1), BATTLE_COMMANDER:発動側:(LOCAL:1))
BATTLE_妖術パワー:発動側:(LOCAL:1) = ABL_POWER(BATTLE_妖術:発動側:(LOCAL:1), BATTLE_COMMANDER:発動側:(LOCAL:1))
BATTLE_歌唱パワー:発動側:(LOCAL:1) = ABL_POWER(BATTLE_歌唱:発動側:(LOCAL:1), BATTLE_COMMANDER:発動側:(LOCAL:1))
BATTLE_料理パワー:発動側:(LOCAL:1) = ABL_POWER(BATTLE_料理:発動側:(LOCAL:1), BATTLE_COMMANDER:発動側:(LOCAL:1))
BATTLE_妖術パワー:発動側:(LOCAL:1) = ABL_POWER(BATTLE_妖術:発動側:(LOCAL:1), BATTLE_COMMANDER:発動側:(LOCAL:1))
BATTLE_SUMMONED_CHARA:発動側:(LOCAL:1) = 1

BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET = LOCAL:2
BATTLE_武闘:対象側:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):武闘
BATTLE_防衛:対象側:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):防衛
BATTLE_知略:対象側:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):知略
BATTLE_政治:対象側:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):政治
BATTLE_歌唱:対象側:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):歌唱
BATTLE_料理:対象側:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):料理
BATTLE_妖術:対象側:COMBAT_SKILL_TARGET = ABL:(LOCAL:2):妖術
BATTLE_武闘パワー:対象側:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_武闘:対象側:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)
BATTLE_防衛パワー:対象側:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_防衛:対象側:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)
BATTLE_知略パワー:対象側:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_知略:対象側:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)
BATTLE_妖術パワー:対象側:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_妖術:対象側:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)
BATTLE_歌唱パワー:対象側:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_歌唱:対象側:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)
BATTLE_料理パワー:対象側:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_料理:対象側:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)
BATTLE_妖術パワー:対象側:COMBAT_SKILL_TARGET = ABL_POWER(BATTLE_妖術:対象側:COMBAT_SKILL_TARGET, BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)
BATTLE_SUMMONED_CHARA:対象側:COMBAT_SKILL_TARGET = 1
RETURN 1

@SKILL_149_PERSONAL_0_EXPLANATION
RESULTS = 味方一人と敵一人を入れ替える。

@SKILL_149_PERSONAL_0_CANT_TELL

@SKILL_149_PERSONAL_0_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 250

