;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_48_PERSONAL_0_EXIST
RETURN 1

@SKILL_48_PERSONAL_0_NAME
RESULTS = アガサクリスＱ

;レベルは1-5まで
@SKILL_48_PERSONAL_0_LEVEL
RETURN 5

;対象選択
@SKILL_48_PERSONAL_0_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
COMBAT_SKILL_TARGET = 発動番号
RETURN 1

;発動判定
@SKILL_48_PERSONAL_0_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;発動テキストをオーバライドしたいときに。
;「誰それのスキル発動！　○○した！」の「○○した！」の部分を実装したい場合は、
;これじゃなくてINVOKEで書けばいいです。
;@SKILL_48_PERSONAL_0_INVOKE_TEXT(発動者, スキル, ジャンル)
;#DIM 発動者
;#DIM スキル
;#DIMS ジャンル

;効果をここに記述
@SKILL_48_PERSONAL_0_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORML %ANAME(発動者)%の知識が武器となる！
PRINTFORML 知略が武闘・防衛として扱われる！
BATTLE_武闘:発動側:COMBAT_SKILL_TARGET = BATTLE_知略:発動側:COMBAT_SKILL_TARGET
BATTLE_武闘パワー:発動側:COMBAT_SKILL_TARGET = BATTLE_知略パワー:発動側:COMBAT_SKILL_TARGET
BATTLE_防衛:発動側:COMBAT_SKILL_TARGET = BATTLE_知略:発動側:COMBAT_SKILL_TARGET
BATTLE_防衛パワー:発動側:COMBAT_SKILL_TARGET = BATTLE_知略パワー:発動側:COMBAT_SKILL_TARGET

@SKILL_48_PERSONAL_0_EXPLANATION
RESULTS = 知略を武闘・防衛として扱う。

@SKILL_48_PERSONAL_0_CANT_TELL


@SKILL_48_PERSONAL_0_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 250
