;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_97_PERSONAL_0_EXIST
RETURN 1

@SKILL_97_PERSONAL_0_NAME
RESULTS = リバースイデオロギー

;レベルは1-5まで
@SKILL_97_PERSONAL_0_LEVEL
RETURN 2

;対象選択
@SKILL_97_PERSONAL_0_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
SIF BATTLE_COMMANDER_NUM:対象側 == 0
	RETURN 0
COMBAT_SKILL_TARGET = RAND:(BATTLE_COMMANDER_NUM:対象側)
RETURN 1

;発動判定
@SKILL_97_PERSONAL_0_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;効果をここに記述
@SKILL_97_PERSONAL_0_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORML 何もかもがひっくり返る！
PRINTFORML %ANAME(BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)%と能力が入れ替わった！
SWAP BATTLE_武闘パワー:発動側:発動番号, BATTLE_武闘パワー:対象側:COMBAT_SKILL_TARGET
SWAP BATTLE_知略パワー:発動側:発動番号, BATTLE_知略パワー:対象側:COMBAT_SKILL_TARGET
SWAP BATTLE_防衛パワー:発動側:発動番号, BATTLE_防衛パワー:対象側:COMBAT_SKILL_TARGET
SWAP BATTLE_妖術パワー:発動側:発動番号, BATTLE_妖術パワー:対象側:COMBAT_SKILL_TARGET
SWAP BATTLE_歌唱パワー:発動側:発動番号, BATTLE_歌唱パワー:対象側:COMBAT_SKILL_TARGET
SWAP BATTLE_料理パワー:発動側:発動番号, BATTLE_料理パワー:対象側:COMBAT_SKILL_TARGET
RETURN 1

;
@SKILL_97_PERSONAL_0_EXPLANATION
RESULTS = 自身と敵一人の能力を入れ替える。

@SKILL_97_PERSONAL_0_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 250
