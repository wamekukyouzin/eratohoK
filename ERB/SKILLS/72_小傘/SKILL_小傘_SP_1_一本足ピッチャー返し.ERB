;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_72_SP_1_EXIST
RETURN 1

@SKILL_72_SP_1_NAME
RESULTS = 一本足ピッチャー返し

;レベルは1-5まで
@SKILL_72_SP_1_LEVEL
RETURN 4

;対象選択
@SKILL_72_SP_1_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;発動判定
@SKILL_72_SP_1_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN RAND:100 < 10 && GET_SOLDIER(発動勢力, 発動部隊) > 0 && GET_SOLDIER(対象勢力, 対象部隊) > 0

;発動テキストをオーバライドしたいときに。
;「誰それのスキル発動！　○○した！」の「○○した！」の部分を実装したい場合は、
;これじゃなくてINVOKEで書けばいいです。
;@SKILL_72_SP_1_INVOKE_TEXT(発動者, スキル, ジャンル)
;#DIM 発動者
;#DIM スキル
;#DIMS ジャンル

;効果をここに記述
@SKILL_72_SP_1_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORML 受けるはずのダメージを与える！

;自部隊が都市防衛をしている場合のみ、防御側に入れないと配列マイナス参照でエラー落ちする。
;BATTLE_CALCは攻撃側が都市であることを考慮していない。
CALL BATTLE_PREPARE_VARS(対象勢力, 対象部隊, 発動勢力, 発動部隊)
LOCAL:2 = BATTLE_INT:発動側
LOCAL:3 = BATTLE_INT:対象側
BATTLE_INT:発動側 = 1
BATTLE_INT:対象側 = 1
CALL BATTLE_CALC_CORE(対象勢力, 対象部隊, 発動勢力, 発動部隊)

VARSET BATTLE_RATE_DMG

CALL DECREASE_SOLDIER(対象勢力, 対象部隊, MAX(BATTLE_DMG:1 / 3, 100), 1)


@SKILL_72_SP_1_EXPLANATION
RESULTS = この戦闘で自部隊が受けるダメージのうちいくらかを敵部隊に与える。
