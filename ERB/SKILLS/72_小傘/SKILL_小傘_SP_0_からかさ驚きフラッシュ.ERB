;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_72_SP_0_EXIST
RETURN 1

@SKILL_72_SP_0_NAME
RESULTS = からかさ驚きフラッシュ

;レベルは1-5まで
@SKILL_72_SP_0_LEVEL
RETURN 2

;対象選択
@SKILL_72_SP_0_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
SIF BATTLE_COMMANDER_NUM:対象側 == 0
	RETURN 0
COMBAT_SKILL_TARGET = RAND:(BATTLE_COMMANDER_NUM:対象側)
RETURN 1

;発動判定
@SKILL_72_SP_0_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN RAND:100 < 30

;効果をここに記述
@SKILL_72_SP_0_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
IF RAND:10 < 2
	PRINTFORML %ANAME(BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)%は驚いてしまった！
	PRINTFORML この戦闘に参加できない！
	CALL ASSASINATE(対象側, COMBAT_SKILL_TARGET)
ELSE
	PRINTFORML %ANAME(BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)%は驚かされたことに怒っている！
	PRINTFORML 一時的に武闘が上昇した！
	BATTLE_武闘:対象側:COMBAT_SKILL_TARGET += 6
ENDIF
RETURN 1

;
@SKILL_72_SP_0_EXPLANATION
RESULTS = 成功時、ＳＬＧ素質「暗殺」相当の効果。失敗すると対象の武闘が上昇。