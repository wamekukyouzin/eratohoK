;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_14_SP_3_EXIST
RETURN 1

@SKILL_14_SP_3_NAME
RESULTS = グリモワールオブアリス

;レベルは1-5まで
@SKILL_14_SP_3_LEVEL
RETURN 4

;対象選択
@SKILL_14_SP_3_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;発動判定
@SKILL_14_SP_3_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN RAND:100 < 7

;効果をここに記述
@SKILL_14_SP_3_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORML %ANAME(NO_TO_CHARA(発動者))%の武闘と知略が一時的に上昇！
PRINTFORML その後恒久的に3減少する！
BATTLE_武闘:(発動側):発動番号 += 7
BATTLE_知略:(発動側):発動番号 += 7
ABL:(NO_TO_CHARA(発動者)):武闘 = MIN_DECREASE(ABL:(NO_TO_CHARA(発動者)):武闘, 3, 1)
ABL:(NO_TO_CHARA(発動者)):知略 = MIN_DECREASE(ABL:(NO_TO_CHARA(発動者)):知略, 3, 1)

RETURN 1

;
@SKILL_14_SP_3_EXPLANATION
RESULTS = 自身の武闘と知略を一時的に7増やし、その後恒久的に3減らす。