;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_124_PERSONAL_0_EXIST
RETURN 1

@SKILL_124_PERSONAL_0_NAME
RESULTS = 夢消失

;レベルは1-5まで
@SKILL_124_PERSONAL_0_LEVEL
RETURN 1

;対象選択
@SKILL_124_PERSONAL_0_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
COMBAT_SKILL_TARGET = 発動番号
RETURN 1

;発動判定
@SKILL_124_PERSONAL_0_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;発動テキストをオーバライドしたいときに。
;「誰それのスキル発動！　○○した！」の「○○した！」の部分を実装したい場合は、
;これじゃなくてINVOKEで書けばいいです。
;@SKILL_124_PERSONAL_0_INVOKE_TEXT(発動者, スキル, ジャンル)
;#DIM 発動者
;#DIM スキル
;#DIMS ジャンル

;効果をここに記述
@SKILL_124_PERSONAL_0_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORML 敵味方全員の能力変動がリセットされた！
FOR LOCAL, 0, BATTLE_COMMANDER_NUM:発動側
	BATTLE_武闘:発動側:LOCAL = ABL:(BATTLE_COMMANDER:発動側:LOCAL):武闘
	BATTLE_防衛:発動側:LOCAL = ABL:(BATTLE_COMMANDER:発動側:LOCAL):防衛
	BATTLE_知略:発動側:LOCAL = ABL:(BATTLE_COMMANDER:発動側:LOCAL):知略
	BATTLE_政治:発動側:LOCAL = ABL:(BATTLE_COMMANDER:発動側:LOCAL):政治
	BATTLE_歌唱:発動側:LOCAL = ABL:(BATTLE_COMMANDER:発動側:LOCAL):歌唱
	BATTLE_料理:発動側:LOCAL = ABL:(BATTLE_COMMANDER:発動側:LOCAL):料理
	BATTLE_妖術:発動側:LOCAL = ABL:(BATTLE_COMMANDER:発動側:LOCAL):妖術
	BATTLE_武闘パワー:発動側:LOCAL = ABL_POWER(BATTLE_武闘:発動側:LOCAL, BATTLE_COMMANDER:発動側:LOCAL)
	BATTLE_防衛パワー:発動側:LOCAL = ABL_POWER(BATTLE_防衛:発動側:LOCAL, BATTLE_COMMANDER:発動側:LOCAL)
	BATTLE_知略パワー:発動側:LOCAL = ABL_POWER(BATTLE_知略:発動側:LOCAL, BATTLE_COMMANDER:発動側:LOCAL)
	BATTLE_妖術パワー:発動側:LOCAL = ABL_POWER(BATTLE_妖術:発動側:LOCAL, BATTLE_COMMANDER:発動側:LOCAL)
	BATTLE_歌唱パワー:発動側:LOCAL = ABL_POWER(BATTLE_歌唱:発動側:LOCAL, BATTLE_COMMANDER:発動側:LOCAL)
	BATTLE_料理パワー:発動側:LOCAL = ABL_POWER(BATTLE_料理:発動側:LOCAL, BATTLE_COMMANDER:発動側:LOCAL)
NEXT
FOR LOCAL, 0, BATTLE_COMMANDER_NUM:対象側
	BATTLE_武闘:対象側:LOCAL = ABL:(BATTLE_COMMANDER:対象側:LOCAL):武闘
	BATTLE_防衛:対象側:LOCAL = ABL:(BATTLE_COMMANDER:対象側:LOCAL):防衛
	BATTLE_知略:対象側:LOCAL = ABL:(BATTLE_COMMANDER:対象側:LOCAL):知略
	BATTLE_政治:対象側:LOCAL = ABL:(BATTLE_COMMANDER:対象側:LOCAL):政治
	BATTLE_歌唱:対象側:LOCAL = ABL:(BATTLE_COMMANDER:対象側:LOCAL):歌唱
	BATTLE_料理:対象側:LOCAL = ABL:(BATTLE_COMMANDER:対象側:LOCAL):料理
	BATTLE_妖術:対象側:LOCAL = ABL:(BATTLE_COMMANDER:対象側:LOCAL):妖術
	BATTLE_武闘パワー:対象側:LOCAL = ABL_POWER(BATTLE_武闘:対象側:LOCAL, BATTLE_COMMANDER:対象側:LOCAL)
	BATTLE_防衛パワー:対象側:LOCAL = ABL_POWER(BATTLE_防衛:対象側:LOCAL, BATTLE_COMMANDER:対象側:LOCAL)
	BATTLE_知略パワー:対象側:LOCAL = ABL_POWER(BATTLE_知略:対象側:LOCAL, BATTLE_COMMANDER:対象側:LOCAL)
	BATTLE_妖術パワー:対象側:LOCAL = ABL_POWER(BATTLE_妖術:対象側:LOCAL, BATTLE_COMMANDER:対象側:LOCAL)
	BATTLE_歌唱パワー:対象側:LOCAL = ABL_POWER(BATTLE_歌唱:対象側:LOCAL, BATTLE_COMMANDER:対象側:LOCAL)
	BATTLE_料理パワー:対象側:LOCAL = ABL_POWER(BATTLE_料理:対象側:LOCAL, BATTLE_COMMANDER:対象側:LOCAL)
NEXT


@SKILL_124_PERSONAL_0_EXPLANATION
RESULTS = 敵味方全員の能力変動をリセットする。




@SKILL_124_PERSONAL_0_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 500
