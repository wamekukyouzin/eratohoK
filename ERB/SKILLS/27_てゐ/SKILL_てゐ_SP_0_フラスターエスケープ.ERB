;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_27_SP_0_EXIST
RETURN 1

@SKILL_27_SP_0_NAME
RESULTS = フラスターエスケープ

;レベルは1-5まで
@SKILL_27_SP_0_LEVEL
RETURN 1

;対象選択
@SKILL_27_SP_0_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;発動判定
@SKILL_27_SP_0_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN GET_SOLDIER(発動勢力, 発動部隊) < GET_SOLDIER(対象勢力, 対象部隊) && RAND:100 < 50

;発動テキストをオーバライドしたいときに。
;「誰それのスキル発動！　○○した！」の「○○した！」の部分を実装したい場合は、
;これじゃなくてINVOKEで書けばいいです。
;@SKILL_27_SP_0_INVOKE_TEXT(発動者, スキル, ジャンル)
;#DIM 発動者
;#DIM スキル
;#DIMS ジャンル

;効果をここに記述
@SKILL_27_SP_0_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORML %ANAME(NO_TO_CHARA(発動者))%は勝てないと判断し、部隊を離脱させた！
FOR LOCAL, 0, BATTLE_COMMANDER_NUM:発動側
	CALL ASSASINATE(発動側, LOCAL)
	CALL FORCE_FREE(BATTLE_COMMANDER:発動側:LOCAL)
NEXT
IF GET_SOLDIER(発動勢力, 発動部隊) > 500
	IF 発動部隊 > 0
		COUNTRY_SOLDIER:発動勢力 += GET_SOLDIER(発動勢力, 発動部隊) - 500
	ELSE
		COUNTRY_SOLDIER:(CITY_OWNER:(発動勢力)) += GET_SOLDIER(発動勢力, 発動部隊) - 500
	ENDIF
	CALL DECREASE_SOLDIER(発動勢力, 発動部隊, GET_SOLDIER(発動勢力, 発動部隊) - 500, 1)
ENDIF

@SKILL_27_SP_0_EXPLANATION
RESULTS = 敵部隊の兵数が自部隊より多い場合、武将を全員部隊から外し、兵を500残して回収する。