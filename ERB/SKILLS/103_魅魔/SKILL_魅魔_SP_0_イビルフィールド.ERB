;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_103_SP_0_EXIST
RETURN 1

@SKILL_103_SP_0_NAME
RESULTS = イビルフィールド

;レベルは1-5まで
@SKILL_103_SP_0_LEVEL
RETURN 4

;対象選択
@SKILL_103_SP_0_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
#DIM 能力, 3
VARSET 能力
RETURN 1

;発動判定
@SKILL_103_SP_0_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
SIF BATTLE_COMMANDER_NUM:対象側 == 0
	RETURN 0
RETURN RAND:100 < 15

;効果をここに記述
@SKILL_103_SP_0_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORML 自分以外の武闘が敵味方問わず一時的に減少する！

FOR LOCAL, 0, BATTLE_COMMANDER_NUM:発動側
	SIF LOCAL != 発動番号
		BATTLE_武闘:発動側:LOCAL = MIN_DECREASE(BATTLE_武闘:発動側:LOCAL, 5, 1)
NEXT

FOR LOCAL, 0, BATTLE_COMMANDER_NUM:対象側
	BATTLE_武闘:対象側:LOCAL = MIN_DECREASE(BATTLE_武闘:対象側:LOCAL, 5, 1)
NEXT

RETURN 1

;
@SKILL_103_SP_0_EXPLANATION
RESULTS = 自身以外の全員の武闘を一時的に5減らす。