;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_38_SP_0_EXIST
RETURN 1

@SKILL_38_SP_0_NAME
RESULTS = 秋の空と乙女の心

;レベルは1-5まで
@SKILL_38_SP_0_LEVEL
RETURN 3

;対象選択
@SKILL_38_SP_0_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
#DIM 能力, 3
VARSET 能力
対象側 = !発動側
SIF BATTLE_COMMANDER_NUM:対象側 == 0
	RETURN 0
RETURN 1

;発動判定
@SKILL_38_SP_0_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN RAND:1000 < 125

;効果をここに記述
@SKILL_38_SP_0_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORML 敵味方全員の武闘が一時的にランダムに変動！
FOR LOCAL, 0, BATTLE_COMMANDER_NUM:発動側
	IF RAND:2
		BATTLE_武闘:発動側:LOCAL = MIN_DECREASE(BATTLE_武闘:発動側:LOCAL, RAND:5, 1)
	ELSE
		BATTLE_武闘:発動側:LOCAL += RAND:5
	ENDIF
NEXT

FOR LOCAL, 0, BATTLE_COMMANDER_NUM:対象側
	IF RAND:2
		BATTLE_武闘:対象側:LOCAL = MIN_DECREASE(BATTLE_武闘:対象側:LOCAL, RAND:5, 1)
	ELSE
		BATTLE_武闘:対象側:LOCAL += RAND:5
	ENDIF
NEXT
RETURN 1

;
@SKILL_38_SP_0_EXPLANATION
RESULTS = 味方と敵の武将全員の武闘を一時的に-5から+5変動させる。