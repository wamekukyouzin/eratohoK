;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_36_BASE_0_EXIST
RETURN 1

@SKILL_36_BASE_0_NAME
RESULTS = 白黒はっきりつける程度の能力

;レベルは1-5まで
@SKILL_36_BASE_0_LEVEL
RETURN 1

;対象選択
@SKILL_36_BASE_0_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;発動判定
@SKILL_36_BASE_0_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN RAND:100 < 10

;発動テキストをオーバライドしたいときに。
;「誰それのスキル発動！　○○した！」の「○○した！」の部分を実装したい場合は、
;これじゃなくてINVOKEで書けばいいです。
;@SKILL_36_BASE_0_INVOKE_TEXT(発動者, スキル, ジャンル)
;#DIM 発動者
;#DIM スキル
;#DIMS ジャンル


;効果をここに記述
@SKILL_36_BASE_0_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
#DIM 攻撃勢力
#DIM 攻撃部隊
#DIM 防御勢力
#DIM 防御部隊
対象側 = !発動側

PRINTFORML 両部隊の能力がリセットされる！

攻撃勢力 = (発動側 == 0 ? 発動勢力 # 対象勢力)
攻撃部隊 = (発動側 == 0 ? 発動部隊 # 対象部隊)
防御勢力 = (発動側 == 0 ? 対象勢力 # 発動勢力)
防御部隊 = (発動側 == 0 ? 対象部隊 # 発動部隊)
;戦闘関連の変数の初期化
VARSET BATTLE_RATE_DMG, 0
VARSET BATTLE_RATE_ATC, 0
VARSET BATTLE_RATE_GRD, 0
VARSET BATTLE_RATE_GRD_B, 0
VARSET BATTLE_FORCE_SCHEME, -1
VARSET BATTLE_SKILL_SEALED, 0

CALL SET_BATTLE_MIRROR_VAL(0, BATTLE_COMMANDER:0:0, BATTLE_COMMANDER:0:1, BATTLE_COMMANDER:0:2)
CALL SET_BATTLE_MIRROR_VAL(1, BATTLE_COMMANDER:1:0, BATTLE_COMMANDER:1:1, BATTLE_COMMANDER:1:2)

;先んじて用意
CALL BATTLE_PREPARE_VARS(攻撃勢力, 攻撃部隊, 防御勢力, 防御部隊)


@SKILL_36_BASE_0_EXPLANATION
RESULTS = 両部隊の部隊能力を計算し直す。