;;触手討伐任務
;;部屋から脱走してフォースの暗黒面に堕ちた悪い触手を処分します
;;どう考えても触手部屋なんて作ってた側が悪いので信用できるキャラ(重要)に討伐を任せることになります
;
;;女性キャラの時は持たせたアイテムと所持素質とかが適当に噛み合うと討伐成功します
;;男性キャラの時は確定成功しますが、リターンが少ないです
;;あなたの時は妖術Lvでの確立勝負になりますが、お金を消費しません
;
;@TENTACLE_SUPPRESSION
;#DIM 兵数
;#DIM 候補数
;#DIM 討伐判定
;#DIM 対策グッズID
;#DIM 処女でした
;#DIM 任命
;#DIM アップ能力
;#DIM 成長率
;;触手部屋をチェック
;SIF !ITEM:触手部屋 || CONFIG:200
;	RETURN -1
;
;	
;;ゼロクリア
;VARSET 兵数
;VARSET 候補数
;VARSET 討伐判定
;VARSET 対策グッズID
;VARSET 処女でした
;VARSET 任命
;VARSET アップ能力
;
;		;MASTERや捕虜でなく、自国で好感度、依存度、従属度のいずれか1000以上＆[虚ろ][崩壊][妊娠]がついていない
;	FOR LOCAL, 0, CHARANUM
;		IF (LOCAL != MASTER)  && (CFLAG:(LOCAL):1 == CFLAG:MASTER:1) && !CFLAG:(LOCAL):9 && (CFLAG:(LOCAL:0):好感度 >= 1000 || CFLAG:(LOCAL:0):依存度 >= 1000  || CFLAG:(LOCAL:0):従属度 >= 1000) && !(TALENT:(LOCAL:0):虚ろ || TALENT:(LOCAL:0):崩壊 || TALENT:(LOCAL:0):妊娠 || TALENT:(LOCAL:0):触手妊娠 || TALENT:(LOCAL:0):Ａ触手妊娠)
;			候補数 ++
;		ENDIF
;	NEXT
;
;;候補がなかったら終わる
;SIF 候補数 == 0
;	RETURN 1
;
;	
;	;自国の君主のキャラ番号を取得
;	LOCAL:1 = GET_COUNTRY_BOSS(CFLAG:MASTER:1)
;	
;	CALL COLORPRINT(@"トラブル発生！", COLOR("警告"))
;	PRINTFORML
;	PRINTFORML
;	PRINTFORML %ANAME(MASTER)%が仕事をしていると、侍従が慌てて報告に来た……
;	PRINTFORML 話を聞くと、城内に突然黒いローブの不審者が現われ、
;	PRINTFORM 触手部屋から力のある
;	CALL COLORPRINT(@"触手", COLOR("緑"))
;	PRINTFORML を奪い去っていったらしいのだ
;	PRINTFORML 
;	PRINTFORML 触手は女を母体に数を増やす危険な生き物だ、扱いを誤れば人類の敵となる
;	PRINTFORML 不審者の狙いが何なのかは分からないが、%ANAME(LOCAL:1)%の領地に被害が出る可能性は高い
;	PRINTFORM しかも発生の根源は
;	CALL COLORPRINT(@"%ANAME(MASTER)%", COLOR("注意"))
;	PRINTFORM の
;	CALL COLORPRINT(@"触手部屋", COLOR("緑"))
;	PRINTFORML である。宜しくない！
;	PRINTFORML 
;	PRINTFORML 早急に手を打つべきだろう。
;	PRINTFORML 信用できる者を討伐に差し向け、秘密裏に処分するしかない！！
;	CALL COLORPRINT(@"危険な任務", COLOR("注意"))
;	PRINTFORM になるだろう……
;	CALL COLORPRINT(@"支度金", COLOR("注意"))
;	PRINTFORML と対抗策を用意せねば！！！
;	PRINTFORML 
;
;	PRINTFORML 現在の資金:{MONEY}
;	;プレイヤーが妖術持ちならセルフ討伐できる
;	if TALENT:(MASTER:1):妖術知識
;		CALL ASK_MULTI_JUDGE(@"金50000を準備金に信用できる者を差し向ける", MONEY >= 50000, "身から出た錆だ、自分で処理する", 1, "まぁいいや", 2)
;	ELSE
;		CALL ASK_MULTI_JUDGE(@"金50000を準備金に信用できる者を差し向ける", MONEY >= 50000, "まぁいいや", 2)
;	ENDIF
;	
;	IF RESULT == 0
;		;今週の秘密道具
;		SELECTCASE RAND:4
;			CASE 0
;				対策グッズID = 0
;			CASE 1
;				対策グッズID = 1
;			CASE 2
;				対策グッズID = 2
;			CASE 3
;				対策グッズID = 3
;		ENDSELECT
;		;あいつなら任せられるかもしれない…！
;		PRINTFORML ・
;		PRINTFORML ・
;		PRINTFORMW ・
;		PRINTFORM %ANAME(MASTER)%は急いで
;		CALL COLORPRINT(@"金50000", COLOR("注意"))
;		PRINTFORML をかき集めた！
;		PRINTFORML そして知り合いの商人から対触手アイテムとして
;		IF 対策グッズID == 0
;			CALL COLORPRINT(@"破壊力抜群「光学ビームスペル」", COLOR("緑"))
;		ELSEIF 対策グッズID == 1
;			CALL COLORPRINT(@"対触手用「封印呪文と封じ札と壺」", COLOR("緑"))
;		ELSEIF 対策グッズID == 2
;			CALL COLORPRINT(@"永琳製「動植物に命令権を持てるドリンク」", COLOR("緑"))
;		ELSEIF 対策グッズID == 3
;			CALL COLORPRINT(@"「旧地獄大蜘蛛の精製毒」を塗ったナイフ", COLOR("緑"))
;		ENDIF
;		PRINTFORML を用意する事に成功しました！
;		PRINTFORML あとは適任者を選抜しなければなりません
;		PRINTFORMW
;		CALL SINGLE_DRAWLINE
;		PRINTFORML 誰に触手討伐の任務を与えますか？
;		PRINTFORML 
;		
;		;全員の調教参加フラグをクリア
;		CVARSET CFLAG, 6, 0
;		;ページ数の計算
;		LOCAL:6 = (候補数 - 1) / 44 + 1
;		LOCAL:7 = 1
;		
;		$SHOW_LOOP
;	
;		;キャラリストの表示
;		LOCAL:8 = 0
;		LOCAL:9 = 0
;		LOCAL:10 = (LOCAL:7 - 1) * 44
;		LOCAL:11 = LOCAL:7 * 44
;		LOCAL:12 = 4
;		FOR LOCAL:0, 0, CHARANUM
;			IF (LOCAL != MASTER)  && (CFLAG:(LOCAL):1 == CFLAG:MASTER:1) && !CFLAG:(LOCAL):9 && (CFLAG:(LOCAL:0):好感度 >= 1000 || CFLAG:(LOCAL:0):依存度 >= 1000  || CFLAG:(LOCAL:0):従属度 >= 1000) && !(TALENT:(LOCAL:0):虚ろ || TALENT:(LOCAL:0):崩壊 || TALENT:(LOCAL:0):妊娠 || TALENT:(LOCAL:0):触手妊娠 || TALENT:(LOCAL:0):Ａ触手妊娠)
;				IF LOCAL:8 >= LOCAL:10 && LOCAL:8 < LOCAL:11
;					IF LOCAL:9 % 2 != 0
;						PRINTPLAIN 　
;					ELSEIF LOCAL:9 >= 1
;						PRINTL 
;						LOCAL:12 ++
;					ENDIF
;					CALL PRINT_PARTNER_DATA(LOCAL:0)
;					LOCAL:9 ++
;				ENDIF
;				LOCAL:8 ++
;			ENDIF
;		NEXT
;		PRINTL 
;		CALL SINGLE_DRAWLINE
;	
;		IF LOCAL:6 >= 2
;			LOCAL:12 += 2
;			IF LOCAL:7 > 1
;				PRINT [  8] 前のページ            
;			ELSE
;				PRINT                             
;			ENDIF
;			LOCALS:0 = page{LOCAL:7}/{LOCAL:6}
;			PRINTPLAINFORM %LOCALS:0, 28, LEFT%
;			IF LOCAL:7 < LOCAL:6
;				PRINT [  9] 後のページ
;			ENDIF
;			PRINTL 
;			CALL SINGLE_DRAWLINE
;		ENDIF
;
;		PRINTL [  0] やっぱりやめた
;
;		REDRAW 0
;
;		$INPUT_LOOP
;		INPUT
;
;			;やっぱりやめた
;			IF RESULT == 0
;				REDRAW 1
;				PRINTFORML と思ったけどまーいいか、今の血気盛んな幻想郷ならどうにかなるだろう
;				PRINTFORMW そう決断した%ANAME(MASTER)%は、真剣な顔で今日の夕食を考え始めた……
;			;前のページ
;			ELSEIF RESULT == 8 && LOCAL:7 > 1
;				LOCAL:7 = MAX(1, LOCAL:7 - 1)
;				CLEARLINE LOCAL:12
;				GOTO SHOW_LOOP
;			;後のページ
;			ELSEIF RESULT == 9 && LOCAL:7 < LOCAL:6
;				LOCAL:7 = MIN(LOCAL:6, LOCAL:7 + 1)
;				CLEARLINE LOCAL:12
;				GOTO SHOW_LOOP
;			ELSE
;				LOCAL:5 = NO_TO_CHARA(RESULT - 100)
;				IF LOCAL:5 >= 0 && (CFLAG:(LOCAL:5):1 == CFLAG:MASTER:1)
;					;選択後
;					IF VAGINA(LOCAL:5)
;						IF LOCAL:5 == GET_COUNTRY_BOSS(CFLAG:MASTER:1)
;							PRINTFORML 話を聞きつけた君主の%ANAME(LOCAL:5)%は、なんと自ら触手討伐に出るとのことです！
;						ELSE
;							PRINTFORML 信頼を置いている%ANAME(LOCAL:5)%に触手討伐の任務を与える事にしました
;						ENDIF
;						PRINTFORM 
;						CALL COLORPRINT(@"金50000", COLOR("注意"))
;						PRINTFORM と
;						IF 対策グッズID == 0
;							CALL COLORPRINT(@"破壊力抜群「光学ビームスペル」", COLOR("緑"))
;						ELSEIF 対策グッズID == 1
;							CALL COLORPRINT(@"対触手用「封印呪文と封じ札と壺」", COLOR("緑"))
;						ELSEIF 対策グッズID == 2
;							CALL COLORPRINT(@"永琳製「動植物に命令権を持てるドリンク」", COLOR("緑"))
;						ELSEIF 対策グッズID == 3
;							CALL COLORPRINT(@"「旧地獄大蜘蛛の精製毒」を塗ったナイフ", COLOR("緑"))
;						ENDIF
;						PRINTFORML を渡して
;						PRINTFORMW 犯人または脱走した触手を捜してもらう事にします
;						PRINTFORML 
;						PRINTFORML ・
;						PRINTFORML ・
;						PRINTFORMW ・ 
;						;成否に関わらず対策しようとしたことにより評判が少し上がる
;						FOR LOCAL:0, 0, MAX_COUNTRY
;							CALL CHANGE_RELATION_C_TO_C(LOCAL, CFLAG:MASTER:1, 10, 0)
;						NEXT
;						;素質によってクエスト成功したりしなかったりする
;						;ただしこういうの使いこなせそうな正邪は確定成功する
;							IF 対策グッズID == 0
;								;破壊力抜群「光学ビームスペル」------------------------------------------------------------------
;								IF LOCAL:5 == NAME_TO_CHARA("咲夜")
;									;状況と能力の相性が良かったので空気を読まずに解決する
;									;ただし普段着、メイド服、執事服を着てない場合は気が抜けてスルーする
;									PRINTFORMW 捜索を始めた%ANAME(LOCAL:5)%は城下町で堂々と黒ローブが休息を取っていたのを見つけました
;									PRINTFORMW %ANAME(LOCAL:5)%は時間を停止させると、黒ローブの八方からナイフを投げつけました
;									PRINTFORM そして
;									CALL COLORPRINT(@"時", COLOR("赤"))
;									PRINTFORML は動き出す……
;									PRINTFORML ・
;									PRINTFORML ・
;									PRINTFORMW ・
;									PRINTFORM 
;									CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手を奪った黒ローブを無事討伐しました！", COLOR("シアン"))
;									PRINTFORML 
;									PRINTFORML 切り札も使わずあっさり終わったので支度金が帰ってきました！
;									PRINTFORML 
;									;おまけ報酬
;									;ランダムで酒を買って帰ってくる。開発してると上位酒になる
;									SELECTCASE RAND:4
;									CASE 0
;										IF ITEMSALES:日本酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『日本酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:日本酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『濁り酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:濁り酒 += 2
;										ENDIF
;									CASE 1
;										IF ITEMSALES:醸造酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『醸造酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:醸造酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『清酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:清酒 += 2
;										ENDIF
;									CASE 2
;										IF ITEMSALES:果実酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『果実酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:果実酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『葡萄酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:葡萄酒 += 2
;										ENDIF
;									CASE 3
;										IF ITEMSALES:蒸留酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『蒸留酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:蒸留酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『麦酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:麦酒 += 2
;										ENDIF
;									ENDSELECT
;									PRINTFORMW 
;									PRINTFORM 
;									CALL COLORPRINT(@"--------------------------QUEST CLEAR!!-------------------------------", COLOR("シアン"))
;									PRINTFORML 
;								ELSE
;									;資金処理
;									MONEY -= 50000
;									;キャラ素質見て正しく使えるかどうかで判定して評判が上がる
;									;
;									CALL COLORPRINT(@"%ANAME(MASTER)%の陣営近くの城下町に淫靡な声が響き渡る……", COLOR("桃"))
;									PRINTFORML 
;									CALL COLORPRINT(@"%ANAME(LOCAL:5)%は性の知識もない少女の目の前で、触手に犯されていた……", COLOR("桃"))
;									PRINTFORMW 
;									PRINTFORML 
;									PRINTFORML ………………………………………………………………………………………………………………………………
;									PRINTFORML 捜索を始めた%ANAME(LOCAL:5)%は城下町で堂々と黒ローブが休息を取っていたのを見つけた
;									PRINTFORML 戦闘し追い詰めるまで良かったが、黒ローブは近くに居た少女に刃物を突きつけ人質に取ってしまった……
;									PRINTFORMW そして一瞬を突いて物陰の下から黒い触手が%ANAME(LOCAL:5)%の秘部を貫いた！
;									PRINTFORML 
;									IF TALENT:(LOCAL:5):処女 && VAGINA(LOCAL:5)
;										;処女なら奪われる
;										CALL VIRGIN_COMMON(LOCAL:5, "触手", 1)
;										処女でした = 1
;										PRINTFORML そして%ANAME(LOCAL:5)%は今、黒ローブの操る触手に犯され続けている
;									ELSE
;										PRINTFORML %ANAME(LOCAL:5)%は生娘ではなかったが、こんな人通りの真ん中で触手に犯される趣味がある訳ではない
;										PRINTFORML しかし、公開処刑を受けながらも迂闊に動くわけには行かなかった
;									ENDIF
;									PRINTFORML 人質の少女は、恐怖と心配、そして本能より来る興味から%ANAME(LOCAL:5)%をじっと見続けている
;									IF 処女でした == 1
;										PRINTFORML 今の今まで処女だった%ANAME(LOCAL:5)%を足元には小さな血痕が幾つも出来て広がっていく……
;									ELSEIF TALENT:(LOCAL:5):濡れやすい
;										PRINTFORML 元々濡れやすい体質であった%ANAME(LOCAL:5)%の内股は、洪水のように溢れかえっている……
;									ELSEIF TALENT:(LOCAL:5):巨乳 || TALENT:(LOCAL:5):爆乳
;										PRINTFORML 服は破られ露出した大きな胸は揉みしだかれ、いいように弄ばれている……
;									ELSE
;										PRINTFORML 服を破られて上半身は露出し、いいように弄ばれている……
;									ENDIF
;									PRINTFORML 
;									IF LOCAL:5 == GET_COUNTRY_BOSS(CFLAG:MASTER:1)
;										PRINTFORML 野次馬達も辺りに集まり、収めている領地内で凌辱を受ける%ANAME(LOCAL:5)%は屈辱に歪んだ顔で絶えていた
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("文") || LOCAL:5 == NAME_TO_CHARA("はたて")
;										PRINTFORML 野次馬達も辺りに集まり、偶に同業者のシャッター音が聞こえる中で凌辱を受ける%ANAME(LOCAL:5)%は必死に絶えていた
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("椛")
;										PRINTFORML 野次馬達も辺りに集まり、偶に同族のシャッター音が聞こえる中で凌辱を受ける%ANAME(LOCAL:5)%は必死に絶えていた
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("さとり") || LOCAL:5 == NAME_TO_CHARA("神子")
;										PRINTFORML 野次馬達も辺りに集まり、周囲の心が流れ込み、恥ずかしさに翻弄されながらも%ANAME(LOCAL:5)%は必死に絶えていた
;									ELSEIF TALENT:(LOCAL:5):バンドガール
;										PRINTFORML 野次馬達も辺りに集まり、自分のファンも居る中で凌辱を受ける%ANAME(LOCAL:5)%は赤い顔をして必死に絶えていた
;									ELSEIF TALENT:(LOCAL:5):恥じらい
;										PRINTFORML 野次馬達も辺りに集まり、あまりの恥ずかしさに俯いたまま凌辱を受ける%ANAME(LOCAL:5)%は必死に絶えていた
;									ELSEIF TALENT:(LOCAL:5):人気
;										PRINTFORML 野次馬達も辺りに集まり、好奇の視線を受けながらを凌辱を受ける%ANAME(LOCAL:5)%は赤い顔をして必死に絶えていた
;									ELSEIF TALENT:(LOCAL:5):美脚
;										PRINTFORML 野次馬達も辺りに集まり、いやらしい視線をその美脚に受けながら%ANAME(LOCAL:5)%は必死に絶えていた
;									ELSE
;										PRINTFORML 野次馬達も辺りに集まり、その中で触手に凌辱を受ける%ANAME(LOCAL:5)%は赤い顔をして必死に絶えていた
;									ENDIF
;									PRINTFORML 長い時間が過ぎ、触手がブルッと大きく体を震わせ大量の精を膣内へと放った！
;									PRINTFORMW %ANAME(LOCAL:5)%は思わず悲鳴を上げ、その場に崩れ落ちた
;									PRINTFORML 
;									PRINTFORML 黒ローブは思わず刃物を手放して、高笑い上げた
;									IF TALENT:(LOCAL:5):臆病 && !(TALENT:(LOCAL:5):技師 || GETBIT(TALENT:(LOCAL:5):ＳＬＧ素質, 素質_ＳＬＧ_暗殺) || GETBIT(TALENT:(LOCAL:5):ＳＬＧ素質, 素質_ＳＬＧ_策士))
;										;バット素質。当てはまると失敗する
;										討伐判定 = 0
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("阿求") || LOCAL:5 == NAME_TO_CHARA("小鈴") || LOCAL:5 == NAME_TO_CHARA("マエリベリー") || LOCAL:5 == NAME_TO_CHARA("蓮子")
;										;非戦闘書籍組は敗北する
;										討伐判定 = 0
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("魔理沙") || LOCAL:5 == NAME_TO_CHARA("アリス") || LOCAL:5 == NAME_TO_CHARA("上海人形") ||  LOCAL:5 == NAME_TO_CHARA("蓬莱人形")
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%はその大きな隙に、崩れ落ちながらも切り札のスペルを放った！", COLOR("注意"))
;										PRINTFORML 
;										討伐判定 = 1
;									ELSEIF TALENT:(LOCAL:5):暗殺 || TALENT:(LOCAL:5):策士 || TALENT:(LOCAL:5):プライド高い || TALENT:(LOCAL:5):目立ちたがり|| TALENT:(LOCAL:5):無関心
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%はその大きな隙に、崩れ落ちながらも切り札のスペルを放った！", COLOR("注意"))
;										PRINTFORML 
;										討伐判定 = 1
;									ELSEIF TALENT:(LOCAL:5):悲観的 && !(TALENT:(LOCAL:5):プライド低い || TALENT:(LOCAL:5):暗殺 || TALENT:(LOCAL:5):策士)
;										;バット素質。当てはまると失敗する
;										討伐判定 = 0
;									ELSEIF TALENT:(LOCAL:5):貞操無頓着 || TALENT:(LOCAL:5):魔力転換 || TALENT:(LOCAL:5):技師 || TALENT:(LOCAL:5):好奇心 || TALENT:(LOCAL:5):バンドガール 
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%はその大きな隙に、崩れ落ちながらも切り札のスペルを放った！", COLOR("注意"))
;										PRINTFORML 
;										討伐判定 = 1
;									ELSE
;										;成功素質を持っていない
;										討伐判定 = 0
;									ENDIF
;									IF 討伐判定 >= 1
;										;クエスト成功判定
;										EXP:(LOCAL:5):Ｃ感経験値 += ABL:(LOCAL:5):Ｃ感 * 2 + 4 + RAND:5
;										EXP:(LOCAL:5):Ｂ感経験値 += ABL:(LOCAL:5):Ｂ感 * 2 + 4 + RAND:5
;										EXP:(LOCAL:5):Ｖ感経験値 += ABL:(LOCAL:5):Ｖ感 * 10 + 15 + RAND:5
;										EXP:(LOCAL:5):欲望経験値 += ABL:(LOCAL:5):欲望 * 3 + 2 + RAND:3
;										EXP:(LOCAL:5):性交経験値 += ABL:(LOCAL:5):性交 * 2 + 15 + RAND:5
;										EXP:(LOCAL:5):触手経験値 += ABL:(LOCAL:5):触手 * 5 + 25 + RAND:5
;										EXP:(LOCAL:5):絶頂経験 += 1
;										CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;										;能力上昇メッセ
;										CALL TRAIN_AUTO_ABLUP(LOCAL:5)
;										PRINTFORML ・
;										PRINTFORML ・
;										PRINTFORMW ・
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手を奪った黒ローブの討伐に成功しました！", COLOR("シアン"))
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORMW 
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORML 
;										IF LOCAL:5 == NAME_TO_CHARA("アリス")
;											;人形ビーム
;											PRINTFORML 周囲に配置され、犯されている時もずっと狙っていた人形がビームを放ち、黒ローブだけを消し炭へ変えた！！
;										ELSEIF TALENT:(LOCAL:5):暗殺 || TALENT:(LOCAL:5):策士 || LOCAL:5 == NAME_TO_CHARA("正邪") || LOCAL:5 == NAME_TO_CHARA("上海人形") ||  LOCAL:5 == NAME_TO_CHARA("蓬莱人形")
;											;暗殺ビーム
;											PRINTFORML 的確な狙いで放たれた赤いビームは、黒ローブの眉間を打ち抜いた！！
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("魔理沙") || TALENT:(LOCAL:5):プライド高い
;											;極大ビーム。プライド高い連中は大体撃てる
;											PRINTFORML 少女の頭の上を通り過ぎた極大のビームは、黒ローブだけを消し炭へ変えた！！
;										ELSEIF TALENT:(LOCAL:5):貞操無頓着 || TALENT:(LOCAL:5):魔力転換 || TALENT:(LOCAL:5):技師 || TALENT:(LOCAL:5):好奇心 || TALENT:(LOCAL:5):バンドガール || TALENT:(LOCAL:5):無関心
;											;拡散ビーム
;											PRINTFORML 少女の合間を縫って放たれた幾つものビームは、黒ローブだけを消し炭へ変えた！！
;										ENDIF
;										PRINTFORML 今まで%ANAME(LOCAL:5)%を犯していた触手は黒ローブと一体化していたのか、ぼろりと崩れ去った
;										IF TALENT:(LOCAL:5):子供体型
;											PRINTFORML %ANAME(LOCAL:5)%は泣いて駆け寄ってくる人質の幼子の涙をそっと拭った
;										ELSEIF LOCAL:5 ==  NAME_TO_CHARA("正邪")
;											PRINTFORML %ANAME(LOCAL:5)%は天邪鬼なめんな！と中指を突きたてた
;										ELSE
;											PRINTFORML %ANAME(LOCAL:5)%は泣いて駆け寄ってくる人質の幼子を優しく抱きかかえた
;										ENDIF
;										IF 処女でした == 1
;											PRINTFORML 無理に笑いを浮かべる%ANAME(LOCAL:5)%の膣からは、ぽたぽたと血と精が混ざった雫が垂れ堕ちた……
;										ELSE
;											PRINTFORML 無理に笑いを浮かべる%ANAME(LOCAL:5)%の膣からは、ぽたぽたと触手の精が垂れ堕ちた……
;										ENDIF
;										IF LOCAL:5 == GET_COUNTRY_BOSS(CFLAG:MASTER:1) && 処女でした == 1
;											;処女だと美談っぽくなって評判が上がる
;											PRINTFORML 
;											CALL COLORPRINT(@"君主の%ANAME(LOCAL:5)%が純潔と引き換えに町を守った噂が広まり、大きく評価が上昇しました。", COLOR("注意"))
;											PRINTFORMW
;											FOR LOCAL:0, 0, MAX_COUNTRY
;												CALL CHANGE_RELATION_C_TO_C(LOCAL, CFLAG:MASTER:1, 300, -150)
;											NEXT
;										ELSEIF 処女でした == 1
;											;処女だと美談っぽくなって評判が上がる
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%の純潔と引き換えに町を守った噂が広まり、大きく評価が上昇しました。", COLOR("注意"))
;											PRINTFORMW
;											FOR LOCAL:0, 0, MAX_COUNTRY
;												CALL CHANGE_RELATION_C_TO_C(LOCAL, CFLAG:MASTER:1, 200, -100)
;											NEXT
;										ELSEIF LOCAL:5 == GET_COUNTRY_BOSS(CFLAG:MASTER:1)
;											;君主ボーナス
;											PRINTFORML 
;											CALL COLORPRINT(@"君主の%ANAME(LOCAL:5)%が必死に町を守った噂が広まり、評価が上昇しました。", COLOR("注意"))
;											PRINTFORMW
;											FOR LOCAL:0, 0, MAX_COUNTRY
;												CALL CHANGE_RELATION_C_TO_C(LOCAL, CFLAG:MASTER:1, 150, -75)
;											NEXT
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("文") || LOCAL:5 == NAME_TO_CHARA("はたて") || LOCAL:5 == NAME_TO_CHARA("椛")
;											;天狗ボーナス
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%が必死に町を守った噂は天狗の新聞で拡散され、評価が上昇しました。", COLOR("注意"))
;											PRINTFORMW
;											FOR LOCAL:0, 0, MAX_COUNTRY
;												CALL CHANGE_RELATION_C_TO_C(LOCAL, CFLAG:MASTER:1, 150, -75)
;											NEXT
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("魔理沙") || TALENT:(LOCAL:5):人気 || TALENT:(LOCAL:5):バンドガール || TALENT:(LOCAL:5):美脚
;											;人気ボーナス
;											PRINTFORML 
;											CALL COLORPRINT(@"必死に町を守った噂が美談として広まり、他の勢力からの評価が上昇しました。", COLOR("注意"))
;											PRINTFORMW
;											FOR LOCAL:0, 0, MAX_COUNTRY
;												CALL CHANGE_RELATION_C_TO_C(LOCAL, CFLAG:MASTER:1, 150, -75)
;											NEXT
;										ELSE
;											PRINTFORML 
;											CALL COLORPRINT(@"必死に町を守った噂が広まり、他の勢力からの評価が上昇しました。", COLOR("注意"))
;											PRINTFORMW
;											FOR LOCAL:0, 0, MAX_COUNTRY
;												CALL CHANGE_RELATION_C_TO_C(LOCAL, CFLAG:MASTER:1, 100, -50)
;											NEXT
;										ENDIF
;										IF TALENT:(LOCAL:5):苗床
;											;苗床持ちだとついでに孕む
;											PRINTFORML 
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ELSEIF RAND:39 < 1
;											;1/40の確立で孕む
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ENDIF
;										PRINTFORM 
;										CALL COLORPRINT(@"--------------------------QUEST CLEAR!!-------------------------------", COLOR("シアン"))
;										PRINTFORML 
;									ELSE
;										PRINTFORML %ANAME(LOCAL:5)%は人質の事を考えると、どうしても身体が動かなかった……
;										PRINTFORML 下種な笑い声と、人質の少女の悲鳴を聞きながら%ANAME(LOCAL:5)%は意識を失った……
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%が意識を失っている間も犯されつづけました", COLOR("桃"))
;										PRINTFORML
;										PRINTFORML 
;										EXP:(LOCAL:5):Ｃ感経験値 += ABL:(LOCAL:5):Ｃ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｂ感経験値 += ABL:(LOCAL:5):Ｂ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｍ感経験値 += ABL:(LOCAL:5):Ｍ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｖ感経験値 += ABL:(LOCAL:5):Ｖ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):Ａ感経験値 += ABL:(LOCAL:5):Ａ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):欲望経験値 += ABL:(LOCAL:5):欲望 * 3 + 2 + RAND:7
;										EXP:(LOCAL:5):性交経験値 += ABL:(LOCAL:5):性交 * 5 + 15 + RAND:11
;										EXP:(LOCAL:5):触手経験値 += ABL:(LOCAL:5):触手 * 10 + 25 + RAND:11
;										EXP:(LOCAL:5):キス経験 += 2 + RAND:8
;										EXP:(LOCAL:5):口淫経験 += 3 + RAND:10
;										EXP:(LOCAL:5):絶頂経験 += 3 + RAND:10
;										CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;										CALL TIGHTNESS_DECREASE_A(LOCAL:5, RAND(5, 15))
;										;能力上昇メッセ
;										CALL TRAIN_AUTO_ABLUP(LOCAL:5)
;										CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;										CALL TIGHTNESS_DECREASE_A(LOCAL:5, RAND(5, 15))
;										CALL KISS_COMMON(LOCAL:5, @"触手")
;										;能力上昇メッセ
;										CALL TRAIN_AUTO_ABLUP(LOCAL:5)
;										PRINTFORML ・
;										PRINTFORML ・
;										PRINTFORMW ・
;										PRINTFORM 
;										CALL COLORPRINT(@"…………任務を達成することが出来ませんでした…………", COLOR("緑"))
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"------------------QUEST FAILED!!--------------------", COLOR("緑"))
;										PRINTFORML 
;										PRINTFORML その後、町の外れで粘液まみれの%ANAME(LOCAL:5)%と人質の少女が発見された……
;										PRINTFORML 
;										;LOCALだとエラー出るので置き換え
;										任命 = LOCAL:5
;										CALL COLORPRINT(@"敗北した%ANAME(LOCAL:5)%に2ターンのクールタイムがつきました", COLOR("警告"))
;										PRINTFORMW 
;										CALL ADD_COOLTIME(任命, 2)
;										PRINTFORML 
;										IF TALENT:(LOCAL:5):苗床 || 処女でした == 1
;											;苗床持ちだとに孕む。処女なのにこんな危険な任務を受けた子もついでに孕む
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ELSEIF RAND:19 < 1
;											;負けると1/20の確立で孕む
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ENDIF
;									ENDIF
;								ENDIF
;							ELSEIF 対策グッズID == 1
;								;対触手用「封印呪文と封じ札と壺」------------------------------------------------------------------
;								IF LOCAL:5 == NAME_TO_CHARA("こいし")
;									;状況と能力の相性が良かったので空気を読まずに解決する。ゲットだぜ
;									;ただし巫女装束か陰陽師服を着ていると真面目にやろうと思ってスルーする
;									PRINTFORMW 話を聞いた%ANAME(LOCAL:5)%は、任務そっちのけで川辺で遊び始めました
;									PRINTFORMW 偶然にもその辺に逃げた触手が隠れていたので壺をぶつけて呪文を唱えました
;									PRINTFORM
;									CALL COLORPRINT(@"ぽーん。バシュゥゥゥーーーンンッ！！", COLOR("注意"))
;									PRINTFORML 
;									PRINTFORML ・
;									PRINTFORML ・
;									PRINTFORMW ・
;									PRINTFORM 
;									CALL COLORPRINT(@"%ANAME(LOCAL:5)%は奪われた触手を無事回収しました！", COLOR("シアン"))
;									PRINTFORML 
;									PRINTFORML 割とあっさり終わったので支度金が帰ってきました！
;									PRINTFORML 
;									;おまけ報酬。ちゃんと開発してると色々貰える
;									;開発ができてないとしょぼくなる
;									IF ITEMSALES:媚薬
;										ITEM:媚薬 += 3
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%が持ち帰った壺から『媚薬』を3個抽出しました", COLOR("注意"))
;										PRINTFORML
;										PRINTFORML 
;									ENDIF
;									IF ITEMSALES:排卵誘発剤
;										ITEM:排卵誘発剤 += 3
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%が持ち帰った壺から『排卵誘発剤』を3個抽出しました", COLOR("注意"))
;										PRINTFORML
;										PRINTFORML 
;									ENDIF
;									ITEM:ローション += 10
;									CALL COLORPRINT(@"%ANAME(LOCAL:5)%が持ち帰った壺から『ローション』を10個抽出しました", COLOR("注意"))
;									PRINTFORML 
;									PRINTFORML
;									ITEM:蜂蜜水 += 2
;									CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『蜂蜜水』をお土産に2本持ち帰りました", COLOR("注意"))
;									PRINTFORMW 
;									PRINTFORM 
;									CALL COLORPRINT(@"--------------------------QUEST CLEAR!!-------------------------------", COLOR("シアン"))
;									PRINTFORML 
;								ELSE
;									;資金処理
;									MONEY -= 50000
;									;キャラ素質見て正しく使えるかどうかで判定して技能が増える
;									;巫女装束か陰陽師服を着ていると素質無視してクリアする
;									CALL COLORPRINT(@"%ANAME(LOCAL:5)%は滝のような汗を流しながら道を進んでいた……", COLOR("桃"))
;									PRINTFORML 
;									CALL COLORPRINT(@"内股はじんわり滲み、雌の匂いを振りまきながらたどたどしく歩いている……", COLOR("桃"))
;									PRINTFORMW 
;									PRINTFORML 
;									PRINTFORML ………………………………………………………………………………………………………………………………
;									PRINTFORML 奪われた触手が川辺に放たれたのを%ANAME(LOCAL:5)%は突き止めた
;									PRINTFORML 連れて来た兵士達を散開させて探索を進めていたが、それは迂闊な行動であった
;									PRINTFORMW 兵士達は次々と触手の不意打ちを受けて倒れてしまったのだ！
;									PRINTFORML 
;									PRINTFORML 触手の一部でも入れ物に納める事が出来れば、呪文を使って一気に封印することが出来る
;									IF LOCAL:5 ==  NAME_TO_CHARA("正邪")
;										PRINTFORML 道具を使っての何時も通りの下克上だ、一瞬で決めて見せる
;									ELSE
;										PRINTFORML 追い詰められていても一瞬で決める事が出来る
;									ENDIF
;									PRINTFORMW そんな希望を持っていた%ANAME(LOCAL:5)%の秘部を黒い触手が貫いた！
;									PRINTFORML 
;									IF TALENT:(LOCAL:5):処女 && VAGINA(LOCAL:5)
;										;処女なら奪われる
;										CALL VIRGIN_COMMON(LOCAL:5, "触手", 1)
;										処女でした = 1
;									ENDIF
;									PRINTFORML 草むらで触手が構えていた事に%ANAME(LOCAL:5)%は気がつけなかったのである……
;									PRINTFORML 深々と自分の股に刺さりこんだ触手を血の気が引いた顔で%ANAME(LOCAL:5)%は見つめた
;								
;									IF (TALENT:(LOCAL:5):暗殺 || TALENT:(LOCAL:5):ステゴロ || LOCAL:5 == NAME_TO_CHARA("正邪") )
;										;2…壺封印　1…子宮封印　0…失敗
;										PRINTFORML しかし、%ANAME(LOCAL:5)%は素早く手刀で触手を切断すると、切れ端を壺に突き入れ呪文を唱えた
;										PRINTFORM 
;										CALL COLORPRINT(@"触手は壺内に吸い込まれ、全てが収まった所で%ANAME(LOCAL:5)%は封じ札を張り付けた！", COLOR("注意"))
;										PRINTFORML 
;										討伐判定 = 2
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("る～こと") || LOCAL:5 == NAME_TO_CHARA("VIVIT") || LOCAL:5 == NAME_TO_CHARA("メディスン") || LOCAL:5 == NAME_TO_CHARA("上海人形") || LOCAL:5 == NAME_TO_CHARA("蓬莱人形") || LOCAL:5 == NAME_TO_CHARA("大江戸人形") || LOCAL:5 == NAME_TO_CHARA("ゴリアテ人形") || LOCAL:5 == NAME_TO_CHARA("雛") 
;										PRINTFORML しかし、%ANAME(LOCAL:5)%は覚悟を決めると、触手を封じるための呪文を唱えた……
;										PRINTFORM 
;										CALL COLORPRINT(@"触手はなんと膣内へと吸い込まれ、全てが収まった所で%ANAME(LOCAL:5)%は封じ札を張り付けた！", COLOR("注意"))
;										PRINTFORML 
;										討伐判定 = 1
;									ELSEIF TALENT:(LOCAL:5):解放 || TALENT:(LOCAL:5):魅惑
;										PRINTFORML しかし、%ANAME(LOCAL:5)%は覚悟を決めると、触手を封じるための呪文を唱えた……
;										PRINTFORM 
;										CALL COLORPRINT(@"触手はなんと子宮内へと吸い込まれ、全てが収まった所で%ANAME(LOCAL:5)%は封じ札を張り付けた！", COLOR("注意"))
;										PRINTFORML 
;										討伐判定 = 1
;									ELSEIF TALENT:(LOCAL:5):幼稚 || TALENT:(LOCAL:5):子供体型
;										;バット素質。当てはまると失敗する
;										討伐判定 = 0
;									ELSEIF TALENT:(LOCAL:5):痛みに強い || TALENT:(LOCAL:5):哨戒
;										PRINTFORML しかし、%ANAME(LOCAL:5)%は冷静に刃物で触手を切断すると、切れ端を壺に突き入れ呪文を唱えた
;										PRINTFORM 
;										CALL COLORPRINT(@"触手は壺内に吸い込まれ、全てが収まった所で%ANAME(LOCAL:5)%は封じ札を張り付けた！", COLOR("注意"))
;										PRINTFORML 
;										討伐判定 = 2
;									ELSE
;										討伐判定 = 0
;									ENDIF
;									IF 討伐判定 >= 1
;										;クエスト成功判定
;										PRINTFORML ・
;										PRINTFORML ・
;										PRINTFORMW ・
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は奪われた触手を無事回収しました！", COLOR("シアン"))
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORMW 
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORML 
;										IF 討伐判定 == 2
;											PRINTFORML 壺に触手を封じ込めた%ANAME(LOCAL:5)%は小さく喘いで崩れ落ちた……
;											PRINTFORML 触手は壺に封印することが出来たが、切断した触手が%ANAME(LOCAL:5)%の子宮内に潜り込み凌辱し続けているのだ
;										ELSE
;											PRINTFORML 壺の代わりに胎内に触手を封じ込めた%ANAME(LOCAL:5)%は小さく喘いで崩れ落ちた……
;											PRINTFORML 封印呪文で触手は胎内に収まる程度に小さくなっているが、内側から%ANAME(LOCAL:5)%を凌辱し続けているのだ
;										ENDIF
;										IF 処女でした == 1
;											PRINTFORML つい先ほどまで処女であった%ANAME(LOCAL:5)%は未知の感覚に悶えながら、兵士たちに介抱されて帰路についた……
;										ELSE
;											PRINTFORMW 腰砕けになった%ANAME(LOCAL:5)%は、兵士たちに介抱されながら帰路を急いだ……
;										ENDIF
;										EXP:(LOCAL:5):Ｃ感経験値 += ABL:(LOCAL:5):Ｃ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｖ感経験値 += ABL:(LOCAL:5):Ｖ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):欲望経験値 += ABL:(LOCAL:5):欲望 * 20 + 20 + RAND:7
;										EXP:(LOCAL:5):性交経験値 += ABL:(LOCAL:5):性交 * 5 + 15 + RAND:11
;										EXP:(LOCAL:5):触手経験値 += ABL:(LOCAL:5):触手 * 10 + 25 + RAND:11
;										EXP:(LOCAL:5):絶頂経験 += 10 + RAND:30
;										CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;										CALL TIGHTNESS_DECREASE_A(LOCAL:5, RAND(5, 15))
;										;能力上昇メッセ
;										CALL TRAIN_AUTO_ABLUP(LOCAL:5)
;										CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;										CALL TIGHTNESS_DECREASE_A(LOCAL:5, RAND(5, 15))
;										PRINTFORML 
;										IF !TALENT:(LOCAL:5):自慰しやすい 
;											;奥底疼き
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は%ANAME(MASTER)%の元に帰るまでに触手に体質を変化させられてしまったようだ", COLOR("桃"))
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜自慰しやすい＞を得た", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):自慰しやすい = 1
;										ELSEIF TALENT:(LOCAL:5):濡れにくい 
;											;体質改善
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は%ANAME(MASTER)%の元に帰るまでに触手に体質を変化させられてしまったようだ", COLOR("桃"))
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜濡れにくい＞を失った", COLOR("注意"))
;											PRINTFORMW 
;												TALENT:(LOCAL:5):濡れにくい = 0
;										ELSEIF !TALENT:(LOCAL:5):濡れやすい 
;											;エロボディ化
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は%ANAME(MASTER)%の元に帰るまでに触手に体質を変化させられてしまったようだ", COLOR("桃"))
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜濡れやすい＞を得た", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):濡れやすい = 1
;										ELSEIF !TALENT:(LOCAL:5):中毒しやすい
;											;明らかな後遺症
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は%ANAME(MASTER)%の元に帰るまでに触手に体質を変化させられてしまったようだ", COLOR("桃"))
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜中毒しやすい＞を得た", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):中毒しやすい = 1
;										ELSEIF TALENT:(LOCAL:5):回復遅い
;											;後遺症で治癒速度上がるってロマンあるよね
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は%ANAME(MASTER)%の元に帰るまでに触手に体質を変化させられてしまったようだ", COLOR("桃"))
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜回復遅い＞を失った", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):回復遅い = 0
;										ELSEIF !TALENT:(LOCAL:5):回復早い
;											;上記同文
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は%ANAME(MASTER)%の元に帰るまでに触手に体質を変化させられてしまったようだ", COLOR("桃"))
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜回復早い＞を得た", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):回復早い = 1
;										ELSEIF TALENT:(LOCAL:5):快感の否定
;											;諦めた……
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は%ANAME(MASTER)%の元に帰るまでに触手に体質を変化させられてしまったようだ", COLOR("桃"))
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜快感の否定＞を失った", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):快感の否定 = 0
;										ELSEIF !TALENT:(LOCAL:5):快感に素直 
;											;開き直った！
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は%ANAME(MASTER)%の元に帰るまでに触手に体質を変化させられてしまったようだ", COLOR("桃"))
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜快感に素直＞を得た", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):快感に素直 = 1
;										ELSEIF !TALENT:(LOCAL:5):おもらし癖 
;											;最後の砦
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は%ANAME(MASTER)%の元に帰るまでに触手に体質を変化させられてしまったようだ", COLOR("桃"))
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜おもらし癖＞を得た", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):おもらし癖 = 1
;										ENDIF
;										IF TALENT:(LOCAL:5):苗床 || 処女でした == 1
;											;苗床持ちだとに孕む。処女なのにこんな危険な任務を受けた子もついでに孕む
;											PRINTFORML 
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ELSEIF RAND:39 < 1
;											;1/40の確立で孕む
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ENDIF
;										PRINTFORM 
;										CALL COLORPRINT(@"--------------------------QUEST CLEAR!!-------------------------------", COLOR("シアン"))
;										PRINTFORML 
;									ELSE
;										PRINTFORML 突然の胎内の熱に%ANAME(LOCAL:5)%は壺を手放し、触手を握り締めて力づくで引き抜こうとした……
;										IF TALENT:(LOCAL:5):子供体型
;											PRINTFORML しかし、触手は小さい%ANAME(LOCAL:5)%を嘲笑うかのように膣内の肉壁を舐め廻すと、一気に暴れ始めた！
;										ELSE
;											PRINTFORML しかし、触手は意に返さず膣内の肉壁を舐め廻すと、一気に%ANAME(LOCAL:5)%の中で暴れ始めた！
;										ENDIF
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は胎内にたっぷり媚薬を注がれました", COLOR("桃"))
;										PRINTFORML
;										PRINTFORML 
;										EXP:(LOCAL:5):Ｃ感経験値 += ABL:(LOCAL:5):Ｃ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｂ感経験値 += ABL:(LOCAL:5):Ｂ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｖ感経験値 += ABL:(LOCAL:5):Ｖ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):欲望経験値 += ABL:(LOCAL:5):欲望 * 20 + 20 + RAND:7
;										EXP:(LOCAL:5):性交経験値 += ABL:(LOCAL:5):性交 * 5 + 15 + RAND:11
;										EXP:(LOCAL:5):触手経験値 += ABL:(LOCAL:5):触手 * 10 + 25 + RAND:11
;										EXP:(LOCAL:5):絶頂経験 += 10 + RAND:30
;										CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;										;能力上昇メッセ
;										CALL TRAIN_AUTO_ABLUP(LOCAL:5)
;										PRINTFORML ・
;										PRINTFORML ・
;										PRINTFORMW ・
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"…………任務を達成することが出来ませんでした…………", COLOR("緑"))
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"------------------QUEST FAILED!!--------------------", COLOR("緑"))
;										PRINTFORML 
;										PRINTFORML 数時間、散々触手に犯された%ANAME(LOCAL:5)%が帰還しました……
;										PRINTFORML 
;										;LOCALだとエラー出るので置き換え
;										任命 = LOCAL:5
;										CALL COLORPRINT(@"敗北した%ANAME(LOCAL:5)%に1ターンのクールタイムがつきました", COLOR("警告"))
;										PRINTFORMW
;										CALL ADD_COOLTIME(任命, 1)
;										PRINTFORML 
;										IF TALENT:(LOCAL:5):苗床 || 処女でした == 1
;											;苗床持ちだとに孕む。処女なのにこんな危険な任務を受けた子もついでに孕む
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ELSEIF RAND:19 < 1
;											;負けると1/20の確立で孕む
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ENDIF
;									ENDIF
;								ENDIF
;							ELSEIF 対策グッズID == 2
;								;永琳製「動植物に命令権を持てるドリンク」------------------------------------------------------------------
;								IF LOCAL:5 == NAME_TO_CHARA("幽香")
;									;逆鱗に触れたので空気を読まずに解決する
;									;ただし普段着て無かった場合スルーする
;									PRINTFORMW 奪われた触手は花畑を占領し、コロニーを形成していた
;									PRINTFORMW それを操る黒ローブは巨大な触手の上からドヤ顔で%ANAME(LOCAL:5)%を見下している
;									PRINTFORM 
;									CALL COLORPRINT(@"腹が立った%ANAME(LOCAL:5)%は全力でビームを放ちました！！！", COLOR("注意"))
;									PRINTFORML 
;									PRINTFORML ・
;									PRINTFORML ・
;									PRINTFORMW ・
;									PRINTFORM 
;									CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手を奪った黒ローブの討伐に成功しました！", COLOR("シアン"))
;									PRINTFORML 
;									PRINTFORML 切り札も使わずあっさり終わったので支度金が帰ってきました！
;									PRINTFORML
;									;おまけ報酬
;									;ランダムで酒を買って帰ってくる。開発してると上位酒になる
;									SELECTCASE RAND:4
;									CASE 0
;										IF ITEMSALES:日本酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『日本酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:日本酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『濁り酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:濁り酒 += 2
;										ENDIF
;									CASE 1
;										IF ITEMSALES:醸造酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『醸造酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:醸造酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『清酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:清酒 += 2
;										ENDIF
;									CASE 2
;										IF ITEMSALES:果実酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『果実酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:果実酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『葡萄酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:葡萄酒 += 2
;										ENDIF
;									CASE 3
;										IF ITEMSALES:蒸留酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『蒸留酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:蒸留酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『麦酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:麦酒 += 2
;										ENDIF
;									ENDSELECT
;									PRINTFORMW 
;									PRINTFORM 
;									CALL COLORPRINT(@"--------------------------QUEST CLEAR!!-------------------------------", COLOR("シアン"))
;									PRINTFORML 
;								ELSE
;									;資金処理
;									MONEY -= 50000
;									;キャラ素質見て正しく使えるかどうかで判定して割と兵数が増える
;									;踊り子衣装、吟遊詩人服だと素質を無視してクリアする。ただし人形組は性質上不可能
;									CALL COLORPRINT(@"%ANAME(LOCAL:5)%は花畑で触手に捕獲され、両穴を犯され続けていた……", COLOR("桃"))
;									PRINTFORML 
;									CALL COLORPRINT(@"触手達は代わる代わる%ANAME(LOCAL:5)%の蜜壷へと触手を突き入れ、精を放っている……", COLOR("桃"))
;									PRINTFORMW 
;									PRINTFORML 
;									PRINTFORML ………………………………………………………………………………………………………………………………
;									PRINTFORML 奪われた触手は花畑を占領し、コロニーを形成していた
;									PRINTFORML 辺りの妖精を取り込み巨大化し、幻想郷内でも強大な力を手に入れていた
;									PRINTFORML それを操る黒ローブは触手の上から勝ち誇った顔をして%ANAME(LOCAL:5)%を見下している
;									PRINTFORMW
;									IF LOCAL:5 == NAME_TO_CHARA("鈴仙")
;										;2…飲む　1…飲むけど効果ない　0…飲まない
;										;どう考えても怪しいよね
;										PRINTFORML しかし、切り札がある%ANAME(LOCAL:5)%は冷や汗を流して笑って見せた
;										PRINTFORML %ANAME(LOCAL:5)%の手には師匠製「動植物に命令権を持てるドリンク」がある！
;										PRINTFORML どう考えても怪しい物体だが、飲まないのは目の前の触手に襲われるのより怖い！！！！！
;										討伐判定 = 2
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("サグメ") || LOCAL:5 == NAME_TO_CHARA("豊姫") || LOCAL:5 == NAME_TO_CHARA("依姫") || LOCAL:5 == NAME_TO_CHARA("清蘭") || LOCAL:5 == NAME_TO_CHARA("鈴瑚") || LOCAL:5 == NAME_TO_CHARA("レイセン")
;										;全力で地雷を踏みに行くスタイル
;										PRINTFORML しかし、切り札がある%ANAME(LOCAL:5)%は不敵に笑って見せた
;										PRINTFORML %ANAME(LOCAL:5)%の手にはあの八意様の「動植物に命令権を持てるドリンク」がある！
;										PRINTFORML 細かい原理は聞かされていないが、これを使えば切り抜けられるに違いない！！
;										討伐判定 = 2
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("永琳")
;										;おまえ何言ってんの？
;										PRINTFORML しかし、切り札がある%ANAME(LOCAL:5)%はニヤリと笑って見せた
;										PRINTFORML %ANAME(LOCAL:5)%の手には自信作の「動植物に命令権を持てるドリンク」がある！
;										PRINTFORML 兎達を実験台にして効果は確認済み、この月の頭脳に間違いは無い！！
;										討伐判定 = 2
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("輝夜")
;										;おまえも何言ってんの？
;										PRINTFORML しかし、切り札がある%ANAME(LOCAL:5)%はニヤリと笑って見せた
;										PRINTFORML %ANAME(LOCAL:5)%の手には永琳製の「動植物に命令権を持てるドリンク」がある！
;										PRINTFORML 効果はさておき美味しい味がするらしいから、飲まないという選択肢は無い！！
;										討伐判定 = 2
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("てゐ")
;										;捨ててきた
;										PRINTFORML %ANAME(LOCAL:5)%は冷や汗をかいて考えていた
;										PRINTFORML 渡された永琳製「動植物に命令権を持てるドリンク」はどう考えても怪しかったので捨ててきたが
;										PRINTFORML 思いのほかヤバイ状態になってた。よしここは逃げよう狡兎三窟！！！
;										討伐判定 = 0
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("る～こと") || LOCAL:5 == NAME_TO_CHARA("VIVIT") || LOCAL:5 == NAME_TO_CHARA("メディスン") || LOCAL:5 == NAME_TO_CHARA("上海人形") || LOCAL:5 == NAME_TO_CHARA("蓬莱人形") || LOCAL:5 == NAME_TO_CHARA("大江戸人形") || LOCAL:5 == NAME_TO_CHARA("ゴリアテ人形") || LOCAL:5 == NAME_TO_CHARA("雛") 
;										;人形
;										PRINTFORML しかし、切り札がある%ANAME(LOCAL:5)%はニヤリと笑って見せた
;										PRINTFORML %ANAME(LOCAL:5)%の手には永琳製「動植物に命令権を持てるドリンク」がある！
;										PRINTFORML これを飲めば特殊なフェロモンが分泌され、単純な生命は命令を聞くようになるのである！！
;										討伐判定 = 1
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("純狐") || LOCAL:5 == NAME_TO_CHARA("正邪") 
;										;純狐に関しては今後の作品展開次第では変更
;										PRINTFORML しかし、切り札がある%ANAME(LOCAL:5)%はニヤリと笑って見せた
;										PRINTFORML %ANAME(LOCAL:5)%の手には月の賢者共の作った「動植物に命令権を持てるドリンク」がある！
;										PRINTFORML これを飲めば特殊なフェロモンが分泌され、単純な生命は命令を聞くようになるのである！！
;										討伐判定 = 2
;									ELSEIF TALENT:(LOCAL:5):中毒しやすい || TALENT:(LOCAL:5):素直 || TALENT:(LOCAL:5):献身的 || TALENT:(LOCAL:5):小柄体型 || TALENT:(LOCAL:5):痛みに弱い
;										;
;										PRINTFORML しかし、切り札がある%ANAME(LOCAL:5)%はニヤリと笑って見せた
;										PRINTFORML %ANAME(LOCAL:5)%の手には永琳製「動植物に命令権を持てるドリンク」がある！
;										PRINTFORML これを飲めば特殊なフェロモンが分泌され、単純な生命は命令を聞くようになるのである！！
;										討伐判定 = 2
;									ELSE
;										;成功素質を持っていない
;										PRINTFORML %ANAME(LOCAL:5)%は冷や汗をかいて考えていた
;										PRINTFORML 渡された永琳製「動植物に命令権を持てるドリンク」はどう考えても怪しかったので捨ててきたが
;										PRINTFORML 思いのほかヤバイ状態になってた。一端引いて体勢を整えることにしよう……
;										討伐判定 = 0
;									ENDIF
;									IF 討伐判定 == 2
;										;クエスト成功判定
;										PRINTFORML 
;										PRINTFORML 一気にドリンクを飲み干すと、%ANAME(LOCAL:5)%は触手に黒ローブを倒すよう命令を下した
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%が出すフェロモンに当てられた触手は黒ローブを一撃でなぎ払った！！！", COLOR("注意"))
;										PRINTFORML 
;										PRINTFORML ・
;										PRINTFORML ・
;										PRINTFORMW ・
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手を奪った黒ローブの討伐に成功しました！", COLOR("シアン"))
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORMW 
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORMW 長く険しい戦いだった……
;										PRINTFORMW その褒美をねだる様に%ANAME(LOCAL:5)%の身体へ絡みつくと触手は大量の精子をかけはじめた
;										PRINTFORMW 突然身体に精子を掛けられて%ANAME(LOCAL:5)%は理解が遅れる……
;										PRINTFORMW そして一際大きい触手が内股へと擦り寄るってきたところで我に返る
;										PRINTFORML 
;										PRINTFORML 触手に停止するように命令を出すが聞かない。効果が効き過ぎて触手が暴走している
;										IF LOCAL:5 == NAME_TO_CHARA("鈴仙")
;											;知ってた
;											PRINTFORMW とりあえず全力で%ANAME(LOCAL:5)%は暴れた！凄い力で抑え込まれた！！
;											PRINTFORMW 最初から詰んでいたのだ、そう考えた%ANAME(LOCAL:5)%は遠い目をして色々諦めた
;											PRINTFORML 無気力な%ANAME(LOCAL:5)%の膣へ巨大な触手が滑り込んでいった……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("永琳")
;											;やりすぎ
;											PRINTFORMW 自分の才能が怖くなった%ANAME(LOCAL:5)%だったが、とりあえず全力で逃走した！
;											PRINTFORMW だが策が敗れた策士は脆い物である。次第に距離を詰められて足をつかまれ
;											PRINTFORML 涙を浮かべたな%ANAME(LOCAL:5)%の膣へ巨大な触手が滑り込んでいった……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("輝夜")
;											;
;											PRINTFORMW 予定外の展開に%ANAME(LOCAL:5)%は必死に暴れた！
;											PRINTFORMW %ANAME(LOCAL:5)%は助けて永琳！イナバ！と必死になって叫んだ
;											PRINTFORML 願いは届かず巨大な触手が%ANAME(LOCAL:5)%の膣へと滑り込んでいった……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("アリス")
;											;これを言わせたかったのでこのパッチ作った
;											PRINTFORMW 予定外の展開に%ANAME(LOCAL:5)%は必死に暴れた！
;											PRINTFORMW %ANAME(LOCAL:5)%は助けて、ママぁーー！と思わず叫んだ
;											PRINTFORML 願いは届かず巨大な触手が%ANAME(LOCAL:5)%の膣へと滑り込んでいった……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("レミリア")
;											;今の素質だと実は失敗するけど一応仕込んどく
;											PRINTFORMW 予定外の展開に%ANAME(LOCAL:5)%は必死に暴れた！
;											PRINTFORMW %ANAME(LOCAL:5)%は助けて、咲夜ぁ！と思わず叫んだ
;											PRINTFORML 願いは届かず巨大な触手が%ANAME(LOCAL:5)%の膣へと滑り込んでいった……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("フランドール")
;											;今の素質だと実は失敗するけど一応仕込んどく2
;											PRINTFORMW 予定外の展開に%ANAME(LOCAL:5)%は必死に暴れた！
;											PRINTFORMW %ANAME(LOCAL:5)%は助けて、おねぇさま！と思わず叫んだ
;											PRINTFORML 願いは届かず巨大な触手が%ANAME(LOCAL:5)%の膣へと滑り込んでいった……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("こいし")
;											;今の素質だと実は失敗するけど一応仕込んどく3
;											PRINTFORMW 予定外の展開に%ANAME(LOCAL:5)%は必死に暴れた！
;											PRINTFORMW %ANAME(LOCAL:5)%は助けて、おねえちゃん！と思わず叫んだ
;											PRINTFORML 願いは届かず巨大な触手が%ANAME(LOCAL:5)%の膣へと滑り込んでいった……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("早苗")
;											;今の素質だと実は失敗するけど一応仕込んどく4
;											PRINTFORMW 予定外の展開に%ANAME(LOCAL:5)%は必死に暴れた！
;											PRINTFORMW %ANAME(LOCAL:5)%は助けて神奈子様！神奈子様！と思わず叫んだ
;											PRINTFORML 願いは届かず巨大な触手が%ANAME(LOCAL:5)%の膣へと滑り込んでいった……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("蓮子")
;											;今の素質だと実は失敗するけど一応仕込んどく5
;											PRINTFORMW 予定外の展開に%ANAME(LOCAL:5)%は必死に暴れた！
;											PRINTFORMW %ANAME(LOCAL:5)%は助けてメリー！と思わず叫んだ
;											PRINTFORML 願いは届かず巨大な触手が%ANAME(LOCAL:5)%の膣へと滑り込んでいった……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("マエリベリー")
;											;
;											PRINTFORMW 予定外の展開に%ANAME(LOCAL:5)%は必死に暴れた！
;											PRINTFORMW %ANAME(LOCAL:5)%は助けて蓮子！と思わず叫んだ
;											PRINTFORML 願いは届かず巨大な触手が%ANAME(LOCAL:5)%の膣へと滑り込んでいった……
;										ELSEIF TALENT:(LOCAL:5):恋慕
;											;恋慕持ちver
;											PRINTFORMW 予定外の展開に%ANAME(LOCAL:5)%は必死に暴れた！
;											PRINTFORMW %ANAME(LOCAL:5)%は%ANAME(MASTER)%助けて！と思わず叫んだ
;											PRINTFORML 願いは届かず巨大な触手が%ANAME(LOCAL:5)%の膣へと滑り込んでいった……
;										ELSEIF LOCAL:5 ==  NAME_TO_CHARA("正邪") 
;											;
;											PRINTFORMW 予定外の展開に%ANAME(LOCAL:5)%は必死に暴れた！
;											PRINTFORMW 最終的に信じられるのは何時も自分だけか！と恨み言を吐きつつ
;											PRINTFORML 捕獲された%ANAME(LOCAL:5)%の膣へ巨大な触手が滑り込んでいった……
;										ELSE
;											PRINTFORMW 予定外の展開に%ANAME(LOCAL:5)%は必死に暴れた！
;											PRINTFORMW %ANAME(LOCAL:5)%は誰か助けて！と必死になって叫んだ
;											PRINTFORML 願いは届かず巨大な触手が%ANAME(LOCAL:5)%の膣へと滑り込んでいった……
;										ENDIF
;										PRINTFORML 
;										IF TALENT:(LOCAL:5):処女 && VAGINA(LOCAL:5)
;											;処女なら奪われる
;											CALL VIRGIN_COMMON(LOCAL:5, "触手", 1)
;											処女でした = 1
;										ENDIF
;										EXP:(LOCAL:5):Ｃ感経験値 += ABL:(LOCAL:5):Ｃ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｂ感経験値 += ABL:(LOCAL:5):Ｂ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｍ感経験値 += ABL:(LOCAL:5):Ｍ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｖ感経験値 += ABL:(LOCAL:5):Ｖ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):Ａ感経験値 += ABL:(LOCAL:5):Ａ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):欲望経験値 += ABL:(LOCAL:5):欲望 * 3 + 2 + RAND:7
;										EXP:(LOCAL:5):性交経験値 += ABL:(LOCAL:5):性交 * 5 + 15 + RAND:11
;										EXP:(LOCAL:5):触手経験値 += ABL:(LOCAL:5):触手 * 10 + 25 + RAND:11
;										EXP:(LOCAL:5):キス経験 += 2 + RAND:8
;										EXP:(LOCAL:5):口淫経験 += 3 + RAND:10
;										EXP:(LOCAL:5):絶頂経験 += 10 + RAND:50
;										CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;										CALL TIGHTNESS_DECREASE_A(LOCAL:5, RAND(5, 15))
;										;能力上昇メッセ
;										CALL TRAIN_AUTO_ABLUP(LOCAL:5)
;										CALL KISS_COMMON(LOCAL:5, @"触手")
;										PRINTFORML ・
;										PRINTFORML ・
;										PRINTFORMW ・
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は奪われた触手を無事回収しました！", COLOR("シアン"))
;										PRINTFORML 
;										PRINTFORML ドリンクの効果で%ANAME(LOCAL:5)%が触手を引きつけていたお陰で被害は最小限で済んだ様だ……
;										PRINTFORML 数日後、大人しくなった触手と、それに懐かれて種付けされ続ける%ANAME(LOCAL:5)%が回収された……
;										IF TALENT:(LOCAL:5):苗床 || 処女でした == 1
;											;苗床持ちだとに孕む。処女なのにこんな危険な任務を受けた子もついでに孕む
;											PRINTFORML 
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ELSEIF RAND:11 < 1
;											;状況が状況なので1/12の確立で孕む
;											PRINTFORML 
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ENDIF
;										IF (RAND:5 < 1) && !TALENT:(LOCAL:5):魅惑
;											;更に1/6の確立で後遺症が残る
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%はドリンクの効果と触手の調教でフェロモン体質に変化させられてしまったようだ", COLOR("桃"))
;											PRINTFORML 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜魅惑＞を得た", COLOR("注意"))
;											PRINTFORMW 
;											TALENT:(LOCAL:5):魅惑 = 1
;										ENDIF
;										PRINTFORML 
;										IF 処女でした == 1 && TALENT:(LOCAL:5):苗床
;											兵数 = RAND(8000, 10000)
;											COUNTRY_SOLDIER:(CFLAG:MASTER:所属) += 兵数
;											PRINTFORM 回収された
;											CALL COLORPRINT(@"兵{兵数}に相当する触手", COLOR("緑"))
;											PRINTFORMW は、%ANAME(LOCAL:5)%の処女を奪って孕ませた罪悪感から軍に加わりました
;										ELSEIF TALENT:(LOCAL:5):触手妊娠
;											兵数 = RAND(6500, 9000)
;											COUNTRY_SOLDIER:(CFLAG:MASTER:所属) += 兵数
;											PRINTFORM 回収された
;											CALL COLORPRINT(@"兵{兵数}に相当する触手", COLOR("緑"))
;											PRINTFORMW は、%ANAME(LOCAL:5)%を孕ませた責任を取って軍に加わりました
;										ELSE
;											兵数 = RAND(5000, 8000)
;											COUNTRY_SOLDIER:(CFLAG:MASTER:所属) += 兵数
;											PRINTFORM %ANAME(LOCAL:5)%に懐いた
;											CALL COLORPRINT(@"兵{兵数}に相当する触手", COLOR("緑"))
;											PRINTFORMW が軍に加わりました
;										ENDIF
;										PRINTFORM 
;										CALL COLORPRINT(@"--------------------------QUEST CLEAR!!-------------------------------", COLOR("シアン"))
;										PRINTFORML 
;									ELSEIF 討伐判定 == 1
;										;人形達
;										PRINTFORML 
;										PRINTFORML 一気にドリンクを飲み干すと、%ANAME(LOCAL:5)%は触手に黒ローブを倒すよう命令を下した
;										PRINTFORML しかしなにも起らない
;										PRINTFORML ・
;										PRINTFORML ・
;										PRINTFORMW ・
;										PRINTFORM 
;										PRINTFORMW よく考えてみればフェロモンなんて発せられる身体ではなかった！！！
;										PRINTFORML 急いで踵を返した%ANAME(LOCAL:5)%の足は素早く触手に払われた
;										PRINTFORML 
;										PRINTFORML 触手は足を掴み宙吊りにして持ち上げると、着ている服を一枚一枚破っていき、
;										PRINTFORML 露になった%ANAME(LOCAL:5)%の花弁へに吸い付くよう滑り込んだ……
;										IF TALENT:(LOCAL:5):処女 && VAGINA(LOCAL:5)
;											;処女なら奪われる
;											CALL VIRGIN_COMMON(LOCAL:5, "触手", 1)
;											処女でした = 1
;										ENDIF
;										PRINTFORML 犯された%ANAME(LOCAL:5)%は抵抗をしようとしたが、力づくで振り回され程なくして静かになった……
;										PRINTFORML %ANAME(LOCAL:5)%は触手の塊に吸い寄せられるのを感じながら意識を失った……
;										PRINTFORMW 
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の内部で犯されつづけました", COLOR("桃"))
;										PRINTFORML
;										PRINTFORML 
;										EXP:(LOCAL:5):Ｃ感経験値 += ABL:(LOCAL:5):Ｃ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｂ感経験値 += ABL:(LOCAL:5):Ｂ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｖ感経験値 += ABL:(LOCAL:5):Ｖ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):Ａ感経験値 += ABL:(LOCAL:5):Ａ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):欲望経験値 += ABL:(LOCAL:5):欲望 * 3 + 2 + RAND:7
;										EXP:(LOCAL:5):性交経験値 += ABL:(LOCAL:5):性交 * 5 + 15 + RAND:11
;										EXP:(LOCAL:5):触手経験値 += ABL:(LOCAL:5):触手 * 10 + 25 + RAND:11
;										EXP:(LOCAL:5):絶頂経験 += 10 + RAND:50
;										CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;										CALL TIGHTNESS_DECREASE_A(LOCAL:5, RAND(5, 15))
;										;能力上昇メッセ
;										CALL TRAIN_AUTO_ABLUP(LOCAL:5)
;										PRINTFORML ・
;										PRINTFORML ・
;										PRINTFORMW ・
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は任務を達成することが出来ませんでした……", COLOR("緑"))
;										PRINTFORML 
;										PRINTFORML 数日後、花畑で白濁液まみれの%ANAME(LOCAL:5)%が発見された……
;										PRINTFORML 
;										;LOCALだとエラー出るので置き換え
;										任命 = LOCAL:5
;										CALL COLORPRINT(@"敗北した%ANAME(LOCAL:5)%に3ターンのクールタイムがつきました", COLOR("警告"))
;										PRINTFORML 
;										CALL ADD_COOLTIME(任命, 3)
;										;このルートでは確定で孕まされる
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"さらに%ANAME(LOCAL:5)%は触手の子を腸内に宿しました", COLOR("注意"))
;										PRINTFORMW 
;										PRINTFORML 
;										TALENT:(LOCAL:5):触手妊娠 = 1
;										CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										TALENT:(LOCAL:5):Ａ触手妊娠 = 1
;										CFLAG:(LOCAL:5):56 = GET_ID(MPLY:0)
;									ELSE
;										PRINTFORMW そう思ったが時は遅く、踵を返した%ANAME(LOCAL:5)%の足は触手に素早く払われた……
;										PRINTFORML 
;										PRINTFORML 触手は足を掴み宙吊りにして持ち上げると、着ている服を一枚一枚破っていき、
;										PRINTFORML 露になった%ANAME(LOCAL:5)%の花弁へに吸い付くよう滑り込んだ……
;											IF TALENT:(LOCAL:5):処女 && VAGINA(LOCAL:5)
;												;処女なら奪われる
;												CALL VIRGIN_COMMON(LOCAL:5, "触手", 1)
;												処女でした = 1
;											ENDIF
;										PRINTFORML 犯された%ANAME(LOCAL:5)%は抵抗をしようとしたが、子宮へ注入された痺れ液の効果で程なくして静かになった……
;										PRINTFORMW %ANAME(LOCAL:5)%は触手の塊に吸い寄せられるのを感じながら意識を失った……
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の内部で犯されつづけました", COLOR("桃"))
;										PRINTFORML
;										PRINTFORML 
;										EXP:(LOCAL:5):Ｃ感経験値 += ABL:(LOCAL:5):Ｃ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｂ感経験値 += ABL:(LOCAL:5):Ｂ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｖ感経験値 += ABL:(LOCAL:5):Ｖ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):Ａ感経験値 += ABL:(LOCAL:5):Ａ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):欲望経験値 += ABL:(LOCAL:5):欲望 * 3 + 2 + RAND:7
;										EXP:(LOCAL:5):性交経験値 += ABL:(LOCAL:5):性交 * 5 + 15 + RAND:11
;										EXP:(LOCAL:5):触手経験値 += ABL:(LOCAL:5):触手 * 10 + 25 + RAND:11
;										EXP:(LOCAL:5):絶頂経験 += 3 + RAND:10
;										CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;										CALL TIGHTNESS_DECREASE_A(LOCAL:5, RAND(5, 15))
;										;能力上昇メッセ
;										CALL TRAIN_AUTO_ABLUP(LOCAL:5)
;										PRINTFORML ・
;										PRINTFORML ・
;										PRINTFORMW ・
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"…………任務を達成することが出来ませんでした…………", COLOR("緑"))
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"------------------QUEST FAILED!!--------------------", COLOR("緑"))
;										PRINTFORML 
;										PRINTFORML 数日後、花畑で白濁液まみれの%ANAME(LOCAL:5)%が発見された……
;										PRINTFORML 
;										;LOCALだとエラー出るので置き換え
;										任命 = LOCAL:5
;										CALL COLORPRINT(@"敗北した%ANAME(LOCAL:5)%に3ターンのクールタイムがつきました", COLOR("警告"))
;										PRINTFORML 
;										CALL ADD_COOLTIME(任命, 3)
;										;このルートでは確定で孕まされる
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"さらに%ANAME(LOCAL:5)%は触手の子を腸内に宿しました", COLOR("注意"))
;										PRINTFORMW 
;										PRINTFORML 
;										TALENT:(LOCAL:5):触手妊娠 = 1
;										CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										TALENT:(LOCAL:5):Ａ触手妊娠 = 1
;										CFLAG:(LOCAL:5):56 = GET_ID(MPLY:0)
;									ENDIF
;								ENDIF
;							ELSEIF 対策グッズID == 3
;								;「旧地獄大蜘蛛の精製毒」を塗ったナイフ-----------------------------------------------------------
;								IF LOCAL:5 == NAME_TO_CHARA("紫")
;									;めんどくさそうな相手だったので空気を読まずに解決する
;									;ただし普段着、巫女服、陰陽師装束を着て無かった場合は普通に相手してあげる
;									PRINTFORMW 森林奥で%ANAME(LOCAL:5)%は巨大な触手獣と化していた奪われた触手を発見しました
;									PRINTFORMW 触手獣は幻想郷随一の強者へ種付けすること夢見て突撃してきました！！！！！
;									PRINTFORM -
;									CALL COLORPRINT(@"8", COLOR("赤"))
;									CALL COLORPRINT(@"--この触手はスキマ送りにされました--", COLOR("パ紫"))
;									CALL COLORPRINT(@"8", COLOR("赤"))
;									PRINTFORMW -
;									PRINTFORML ・
;									PRINTFORML ・
;									PRINTFORMW ・
;									PRINTFORM 
;									CALL COLORPRINT(@"%ANAME(LOCAL:5)%は奪われた触手の放逐に成功しました！", COLOR("シアン"))
;									PRINTFORML 
;									PRINTFORML 切り札も使わずあっさり終わったので支度金が帰ってきました！
;									PRINTFORML
;									;おまけ報酬
;									;ランダムで酒を買って帰ってくる。開発してると上位酒になる
;									SELECTCASE RAND:4
;									CASE 0
;										IF ITEMSALES:日本酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『日本酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:日本酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『濁り酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:濁り酒 += 2
;										ENDIF
;									CASE 1
;										IF ITEMSALES:醸造酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『醸造酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:醸造酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『清酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:清酒 += 2
;										ENDIF
;									CASE 2
;										IF ITEMSALES:果実酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『果実酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:果実酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『葡萄酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:葡萄酒 += 2
;										ENDIF
;									CASE 3
;										IF ITEMSALES:蒸留酒
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『蒸留酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:蒸留酒 += 2
;										ELSE
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は『麦酒』をお土産に2本持ち帰りました", COLOR("注意"))
;											ITEM:麦酒 += 2
;										ENDIF
;									ENDSELECT
;									PRINTFORMW 
;									PRINTFORM 
;									CALL COLORPRINT(@"--------------------------QUEST CLEAR!!-------------------------------", COLOR("シアン"))
;									PRINTFORML 
;								ELSE
;									;資金処理
;									MONEY -= 50000
;									;キャラ素質見て正しく使えるかどうかで判定して心体が育つ
;									;メイド服、踊り子衣装、執事服を着ている場合は素質無視してクリアする
;									CALL COLORPRINT(@"巨躯の触手の獣が激しく動いて小さな裸体を強姦していた……", COLOR("桃"))
;									PRINTFORML 
;									CALL COLORPRINT(@"闇に包まれた森林で泣き喘ぐ%ANAME(LOCAL:5)%の声が響いた……", COLOR("桃"))
;									PRINTFORMW 
;									PRINTFORML 
;									PRINTFORML ………………………………………………………………………………………………………………………………
;									PRINTFORML 夕日が差し込む森林の奥で%ANAME(LOCAL:5)%は奪われた触手を発見した
;									PRINTFORML 何らかの呪術を使われたのか姿は変貌し、大きく目玉のついた触手獣となっていた……
;									IF TALENT:(LOCAL:5):子供体型 || TALENT:(LOCAL:5):小柄体型
;										PRINTFORML %ANAME(LOCAL:5)%の身長の数倍はありそうな触手獣は備わった目玉で狙いを定めると
;									ELSE
;										PRINTFORML %ANAME(LOCAL:5)%の身長の倍はありそうな触手獣は備わった目玉で狙いを定めると
;									ENDIF
;									PRINTFORML あたりを薙ぎ払い、取り込みながら%ANAME(LOCAL:5)%を追い回した
;									PRINTFORMW 
;									
;									IF LOCAL:5 == NAME_TO_CHARA("咲夜") || LOCAL:5 == NAME_TO_CHARA("妖夢") || LOCAL:5 == NAME_TO_CHARA("小町") || LOCAL:5 == NAME_TO_CHARA("椛") || LOCAL:5 == NAME_TO_CHARA("明羅") || LOCAL:5 == NAME_TO_CHARA("夢子") || LOCAL:5 == NAME_TO_CHARA("依姫")
;										PRINTFORML 途中、%ANAME(LOCAL:5)%は普段の獲物で攻撃を試みたが直ぐに再生されてしまう！
;									ELSE
;										PRINTFORML 途中、%ANAME(LOCAL:5)%は弾幕で攻撃を試みたが直ぐに再生されてしまう！
;									ENDIF
;									IF LOCAL:5 == NAME_TO_CHARA("リグル") || LOCAL:5 == NAME_TO_CHARA("てゐ") || LOCAL:5 == NAME_TO_CHARA("ナズーリン")
;										PRINTFORML %ANAME(LOCAL:5)%はこういった場所で逃げるのは慣れていたが
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("魔理沙") || LOCAL:5 == NAME_TO_CHARA("アリス") || LOCAL:5 == NAME_TO_CHARA("ヤマメ")
;										PRINTFORML %ANAME(LOCAL:5)%はこういった場所で移動しながらの戦闘は慣れていたが
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("文") || LOCAL:5 == NAME_TO_CHARA("はたて") || TALENT:(LOCAL:5):疾風迅雷
;										PRINTFORML %ANAME(LOCAL:5)%は速度には自信を持っていたが、あたりは木々だらけでスピードが出ず
;									ENDIF
;									PRINTFORML 身体に差がある上に障害物を無視して進んでくる触手獣との差はどんどん縮まり
;									PRINTFORML %ANAME(LOCAL:5)%の身体を撥ね飛ばし、押し倒した！
;									
;									IF TALENT:(LOCAL:5):しっぽ || TALENT:(LOCAL:5):Ｓ気質 || TALENT:(LOCAL:5):突貫 || TALENT:(LOCAL:5):防衛戦術 || TALENT:(LOCAL:5):感情乏しい
;										討伐判定 = 1
;									ELSEIF LOCAL:5 == NAME_TO_CHARA("リグル") || LOCAL:5 == NAME_TO_CHARA("正邪") || LOCAL:5 == NAME_TO_CHARA("針妙丸") || LOCAL:5 == NAME_TO_CHARA("魔理沙")|| LOCAL:5 == NAME_TO_CHARA("メディスン") || LOCAL:5 == NAME_TO_CHARA("ヤマメ") || LOCAL:5 == NAME_TO_CHARA("キスメ") 
;										討伐判定 = 1
;									ELSEIF TALENT:(LOCAL:5):痛みに弱い
;										;バット素質。当てはまると失敗する
;										討伐判定 = 0
;									ELSEIF TALENT:(LOCAL:5):一線越えない || TALENT:(LOCAL:5):自制心 || TALENT:(LOCAL:5):小悪魔 || TALENT:(LOCAL:5):回復早い || TALENT:(LOCAL:5):サボり魔
;										討伐判定 = 1
;									ELSE
;										;成功素質を持っていない
;										討伐判定 = 0
;									ENDIF
;									
;									IF 討伐判定 >= 1
;										;クエスト成功判定
;										PRINTFORM
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は力を振り絞ると、毒の塗られたナイフを触手へ突き刺した！", COLOR("注意"))
;										PRINTFORMW 
;										PRINTFORML 
;										PRINTFORML 突き刺された部分から壊死が始まり、触手はボロボロと崩れていく……
;										PRINTFORML 触手は大きな鳴き声を上げると%ANAME(LOCAL:5)%を叩きつけ、邪魔だとばかりに衣服を剥ぎ取った！
;										PRINTFORML そして%ANAME(LOCAL:5)%の身体を、胸を、口を、耳を、膣を、尻穴を、侵していく！
;										PRINTFORML 毒で身体が崩壊する触手は生きていた証拠を残さんと、%ANAME(LOCAL:5)%の身体を激しく犯し続けた！！
;										IF TALENT:(LOCAL:5):処女 && VAGINA(LOCAL:5)
;											;処女なら奪われる
;											CALL VIRGIN_COMMON(LOCAL:5, "触手", 1)
;											処女でした = 1
;										ENDIF
;										EXP:(LOCAL:5):Ｃ感経験値 += ABL:(LOCAL:5):Ｃ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｂ感経験値 += ABL:(LOCAL:5):Ｂ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｍ感経験値 += ABL:(LOCAL:5):Ｍ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｖ感経験値 += ABL:(LOCAL:5):Ｖ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):Ａ感経験値 += ABL:(LOCAL:5):Ａ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):欲望経験値 += ABL:(LOCAL:5):欲望 * 3 + 2 + RAND:7
;										EXP:(LOCAL:5):性交経験値 += ABL:(LOCAL:5):性交 * 5 + 15 + RAND:11
;										EXP:(LOCAL:5):触手経験値 += ABL:(LOCAL:5):触手 * 10 + 25 + RAND:11
;										EXP:(LOCAL:5):キス経験 += 2 + RAND:8
;										EXP:(LOCAL:5):口淫経験 += 3 + RAND:10
;										EXP:(LOCAL:5):絶頂経験 += 3 + RAND:10
;										EXP:(LOCAL:5):Ｖ拡張経験 += 3 + RAND:10
;										EXP:(LOCAL:5):Ａ拡張経験 += 3 + RAND:10
;										CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;										CALL TIGHTNESS_DECREASE_A(LOCAL:5, RAND(5, 15))
;										;能力上昇メッセ
;										CALL TRAIN_AUTO_ABLUP(LOCAL:5)
;										CALL KISS_COMMON(LOCAL:5, @"触手")
;										PRINTFORML ・
;										PRINTFORML ・
;										PRINTFORMW ・
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は奪われた触手の討伐に成功しました！", COLOR("シアン"))
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORMW 
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORML 
;										PRINTFORMW %ANAME(LOCAL:5)%は長い時間触手の凌辱に耐え続けた……
;										PRINTFORML 
;										PRINTFORML 朝の光が差し込んだ頃、触手は生殖器を突き上げて最後の精を放つと
;										IF LOCAL:5 == NAME_TO_CHARA("正邪") || LOCAL:5 == NAME_TO_CHARA("霊夢") || LOCAL:5 == NAME_TO_CHARA("魔理沙")
;											PRINTFORMW ざまぁみろ、と吐き捨てた%ANAME(LOCAL:5)%の前で塵へ帰っていた……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("針妙丸")
;											PRINTFORMW 自分の数倍も小さい勝者を見つめながら、塵へ帰っていた……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("メディスン") || LOCAL:5 == NAME_TO_CHARA("ヤマメ")
;											PRINTFORMW 自分を滅ぼした猛毒の王を見つめながら、塵へ帰っていた……
;										ELSEIF TALENT:(LOCAL:5):Ｓ気質 && TALENT:(LOCAL:5):孤高
;											PRINTFORMW 惨めな物を見るような目をした%ANAME(LOCAL:5)%の前で塵へ帰っていた……
;										ELSEIF TALENT:(LOCAL:5):快感に素直 && 処女でした == 1 || TALENT:(LOCAL:5):Ｖ敏感 && 処女でした == 1 
;											PRINTFORMW 純潔を流しながらも、絶頂を迎えて倒れた%ANAME(LOCAL:5)%を見つめながら塵へ帰っていた……
;										ELSEIF 処女でした == 1
;											PRINTFORMW 秘部から純潔を流し、苦しそうに肩で息をする%ANAME(LOCAL:5)%を見ながらで塵へ帰っていた……
;										ELSE
;											PRINTFORMW 未練がましい雄叫びを上げ、虚ろな目をした%ANAME(LOCAL:5)%の前で塵へ帰っていた……
;										ENDIF
;										PRINTFORML 
;										IF LOCAL:5 == NAME_TO_CHARA("針妙丸") || 処女でした == 1
;											PRINTFORML 数時間後、おびただしい精液の中で笑みを浮かべて眠る%ANAME(LOCAL:5)%が回収された……
;										ELSEIF LOCAL:5 == NAME_TO_CHARA("正邪") || LOCAL:5 == NAME_TO_CHARA("霊夢") || LOCAL:5 == NAME_TO_CHARA("魔理沙")
;											PRINTFORML 数時間後、おびただしい精液の中で勝利の笑みを浮かべて眠る%ANAME(LOCAL:5)%が回収された……
;										ELSEIF TALENT:(LOCAL:5):感情乏しい
;											PRINTFORML 数時間後、おびただしい精液の中で僅かに笑みを浮かべて眠る%ANAME(LOCAL:5)%が回収された……
;										ELSE
;											PRINTFORML 数時間後、おびただしい精液の中で勝利の笑みを浮かべて眠る%ANAME(LOCAL:5)%が回収された……
;										ENDIF
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%は強大な敵との戦いの中で成長したようです", COLOR("注意"))
;										PRINTFORML 
;										PRINTFORML 
;										;武闘成長
;										IF (ABL:(LOCAL:5):武闘 + ABL:(LOCAL:5):知略 + ABL:(LOCAL:5):政治) >= 250 
;											アップ能力 = GETNUM(ABL, "武闘")
;											成長率 = 1
;										ELSEIF (ABL:(LOCAL:5):武闘 + ABL:(LOCAL:5):知略 + ABL:(LOCAL:5):政治) >= 200 
;											アップ能力 = GETNUM(ABL, "武闘")
;											成長率 = 2 + RAND:2
;										ELSEIF (ABL:(LOCAL:5):武闘 + ABL:(LOCAL:5):知略 + ABL:(LOCAL:5):政治) >= 150 
;											アップ能力 = GETNUM(ABL, "武闘")
;											成長率 = 3 + RAND:3
;										ELSE
;											アップ能力 = GETNUM(ABL, "武闘")
;											成長率 = 4 + RAND:4
;										ENDIF
;										ABL:(LOCAL:5):アップ能力 += 成長率
;										PRINTFORML 
;										;能力上昇メッセ
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%の%ABLNAME:アップ能力%が{成長率}上昇しました(Lv{ABL:(LOCAL:5):アップ能力})",  COLOR("オレンジ"))
;										PRINTFORML 
;										;知略成長
;										IF (ABL:(LOCAL:5):武闘 + ABL:(LOCAL:5):知略 + ABL:(LOCAL:5):政治) >= 250 
;											アップ能力 = GETNUM(ABL, "知略")
;											成長率 = 1
;										ELSEIF (ABL:(LOCAL:5):武闘 + ABL:(LOCAL:5):知略 + ABL:(LOCAL:5):政治) >= 200 
;											アップ能力 = GETNUM(ABL, "知略")
;											成長率 = 2 + RAND:2
;										ELSEIF (ABL:(LOCAL:5):武闘 + ABL:(LOCAL:5):知略 + ABL:(LOCAL:5):政治) >= 150 
;											アップ能力 = GETNUM(ABL, "知略")
;											成長率 = 3 + RAND:3
;										ELSE
;											アップ能力 = GETNUM(ABL, "知略")
;											成長率 = 4 + RAND:4
;										ENDIF
;										ABL:(LOCAL:5):アップ能力 += 成長率
;										;能力上昇メッセ
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%の%ABLNAME:アップ能力%が{成長率}上昇しました(Lv{ABL:(LOCAL:5):アップ能力})",  COLOR("オレンジ"))
;										PRINTFORML 
;										PRINTFORML 
;										
;										IF (ABL:(LOCAL:5):武闘 >= 80) && !TALENT:(LOCAL:5):突貫 && !TALENT:(LOCAL:5):防衛戦術
;											;防衛戦術が無くて武闘が高いと突貫がつく。優先
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜突貫＞を得た", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):突貫 = 1
;										ELSEIF (ABL:(LOCAL:5):知略 >= 80) && !TALENT:(LOCAL:5):防衛戦術 && !TALENT:(LOCAL:5):突貫
;											;突貫が無くて知略が高いと防衛戦術がつく
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は＜防衛戦術＞を得た", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):防衛戦術 = 1
;										ENDIF
;										IF TALENT:(LOCAL:5):苗床 || 処女でした == 1
;											;苗床持ちだとに孕む。処女なのにこんな危険な任務を受けた子もついでに孕む
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ELSEIF RAND:5 < 1
;											;勝ったけれど命をかけて交尾されたので1/6の確立で孕む
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ENDIF
;										PRINTFORM 
;										CALL COLORPRINT(@"--------------------------QUEST CLEAR!!-------------------------------", COLOR("シアン"))
;										PRINTFORML 
;									ELSE
;										PRINTFORMW 強く頭を打った%ANAME(LOCAL:5)%はそのまま気を失った……
;										PRINTFORML 
;										PRINTFORML 次に目覚めた時に視界に映ったのは夜の暗い闇
;										PRINTFORML そして、粘液を撒き散らしながら%ANAME(LOCAL:5)%の膣を貫く巨大な触手の生殖器だった……
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"%ANAME(LOCAL:5)%に覆いかぶさった触手は、夜が明けるまで激しく種付けを続けました", COLOR("桃"))
;										PRINTFORML
;										PRINTFORML
;										IF TALENT:(LOCAL:5):処女 && VAGINA(LOCAL:5)
;											CALL VIRGIN_COMMON(LOCAL:5, "触手", 1)
;											処女でした = 1
;										ENDIF
;										EXP:(LOCAL:5):Ｃ感経験値 += ABL:(LOCAL:5):Ｃ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｂ感経験値 += ABL:(LOCAL:5):Ｂ感 * 4 + 4 + RAND:10
;										EXP:(LOCAL:5):Ｖ感経験値 += ABL:(LOCAL:5):Ｖ感 * 20 + 15 + RAND:11
;										EXP:(LOCAL:5):欲望経験値 += ABL:(LOCAL:5):欲望 * 3 + 2 + RAND:7
;										EXP:(LOCAL:5):性交経験値 += ABL:(LOCAL:5):性交 * 5 + 15 + RAND:11
;										EXP:(LOCAL:5):触手経験値 += ABL:(LOCAL:5):触手 * 10 + 25 + RAND:11
;										EXP:(LOCAL:5):絶頂経験 += 3 + RAND:20
;										EXP:(LOCAL:5):Ｖ拡張経験 += 3 + RAND:10
;										CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;										CALL TIGHTNESS_DECREASE_A(LOCAL:5, RAND(5, 15))
;										;能力上昇メッセ
;										CALL TRAIN_AUTO_ABLUP(LOCAL:5)
;										PRINTFORML ・
;										PRINTFORML ・
;										PRINTFORMW ・
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"…………任務を達成することが出来ませんでした…………", COLOR("緑"))
;										PRINTFORML 
;										PRINTFORM 
;										CALL COLORPRINT(@"------------------QUEST FAILED!!--------------------", COLOR("緑"))
;										PRINTFORML 
;										PRINTFORMW 
;										PRINTFORML 次の日の朝、精液で真っ白に染まりきった%ANAME(LOCAL:5)%が発見された……
;										PRINTFORML 
;										;LOCALだとエラー出るので置き換え
;										任命 = LOCAL:5
;										CALL COLORPRINT(@"敗北した%ANAME(LOCAL:5)%に3ターンのクールタイムがつきました", COLOR("警告"))
;										PRINTFORML 
;										PRINTFORMW 
;										CALL ADD_COOLTIME(任命, 3)
;										IF TALENT:(LOCAL:5):苗床 || 処女でした == 1
;											;苗床持ちだとに孕む。処女なのにこんな危険な任務を受けた子もついでに孕む
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ELSEIF RAND:20 < 1
;											;負けると1/21の確立で孕む
;											PRINTFORM 
;											CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;											PRINTFORMW 
;											PRINTFORML 
;											TALENT:(LOCAL:5):触手妊娠 = 1
;											CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;										ENDIF
;									ENDIF
;								ENDIF
;								;温泉ルート(未定)
;								;
;						ENDIF
;					ELSE
;						;男キャラなら適当に状況を納めてくれる
;						PRINTFORML 信頼を置いている%ANAME(LOCAL:5)%に触手討伐の任務を与える事にしました
;						PRINTFORM 支度するために
;						CALL COLORPRINT(@"金50000", COLOR("注意"))
;						PRINTFORMW を渡し、犯人を捜してもらう事にします
;						PRINTFORML ・
;						PRINTFORML ・
;						PRINTFORMW ・
;						PRINTFORM
;						CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手を奪った黒ローブを無事討伐しました！", COLOR("シアン"))
;						PRINTFORML 
;						PRINTFORML 触手は近隣で凌辱の限りを尽くしていた為、現地の人にとても感謝されました！！
;						CALL COLORPRINT(@"他の勢力からの評価が上昇しました。", COLOR("注意"))
;						PRINTFORMW
;						PRINTFORM 
;						CALL COLORPRINT(@"--------------------------QUEST CLEAR!!-------------------------------", COLOR("シアン"))
;						PRINTFORML 
;						FOR LOCAL:0, 0, MAX_COUNTRY
;							CALL CHANGE_RELATION_C_TO_C(LOCAL, CFLAG:MASTER:1, 50, 0)
;						NEXT
;					ENDIF
;				ELSE
;					CLEARLINE 1
;				GOTO INPUT_LOOP
;				ENDIF
;			ENDIF
;
;	ELSEIF RESULT == 1
;		;幻想郷脳を拗らせた結果
;		PRINTFORML 
;		CALL COLORPRINT(@"……いや、自分で不審者を倒せばいいのだ！", COLOR("警告"))
;		PRINTFORML
;		PRINTFORML %ANAME(MASTER)%も触手を従える者の端くれ
;		PRINTFORMW これは挑戦状である！妖術を使っての戦いに負けるわけには行かない！！
;		PRINTFORML 
;		PRINTFORML 触手使いとしての格の違いと言うものを見せ付けてやろう！！！
;		PRINTFORML 下から吹き上げる謎の風を纏った%ANAME(MASTER)%は、単身で犯人探しへと乗り出した……
;		PRINTFORML ・
;		PRINTFORML ・
;		PRINTFORMW ・
;		IF (RAND:100 <= ABL:(MASTER:0):妖術) 
;			;マッチポンプ成功
;			PRINTFORM
;			CALL COLORPRINT(@"%ANAME(MASTER)%は触手を奪った黒ローブを無事討伐しました！", COLOR("シアン"))
;			PRINTFORML 
;			PRINTFORML 触手は近隣で凌辱の限りを尽くしていた為、現地の人にとても感謝されました！！
;			CALL COLORPRINT(@"他の勢力からの評価が上昇しました。", COLOR("注意"))
;			PRINTFORMW
;			PRINTFORM 
;			CALL COLORPRINT(@"--------------------------QUEST CLEAR!!-------------------------------", COLOR("シアン"))
;			PRINTFORML 
;			
;			FOR LOCAL:0, 0, MAX_COUNTRY
;				CALL CHANGE_RELATION_C_TO_C(LOCAL, CFLAG:MASTER:1, 50, 0)
;			NEXT
;		ELSE
;			;くっころ
;			PRINTFORML %ANAME(MASTER)%は触手を奪った黒ローブを見つけ出しましたが、敗北してしまいました……
;			PRINTFORML
;			PRINTFORM 
;			CALL COLORPRINT(@"…………任務を達成することが出来ませんでした…………", COLOR("緑"))
;			PRINTFORML 
;			PRINTFORM 
;			CALL COLORPRINT(@"------------------QUEST FAILED!!--------------------", COLOR("緑"))
;			PRINTFORML 
;			PRINTFORML 
;			IF TALENT:(MASTER):処女 && VAGINA(MASTER)
;				PRINTFORML 人里を襲っていた触手は%ANAME(MASTER)%を交えて凌辱の限りを尽くしました
;				PRINTFORML %ANAME(MASTER)%は屈辱的な敗北の中で触手に処女を奪われました……
;				PRINTFORML 
;			ELSEIF VAGINA(MASTER)
;				PRINTFORML 人里を襲っていた触手は%ANAME(MASTER)%を交えて凌辱の限りを尽くしました
;				PRINTFORML %ANAME(MASTER)%は屈辱的な敗北の中で触手に犯され続けました……
;				PRINTFORMW 
;			ELSE
;				PRINTFORML 人里を襲っていた触手は%ANAME(MASTER)%の目の前で凌辱の限りを尽くしました
;				PRINTFORML 響き渡る女子供の声を聞きながら、%ANAME(MASTER)%は意識を失いました……
;				PRINTFORMW 
;			ENDIF
;		
;			CALL KISS_COMMON(MASTER, @"触手")
;
;			IF TALENT:(MASTER):処女 && VAGINA(MASTER)
;				CALL VIRGIN_COMMON(LOCAL:5, "触手", 1)
;				処女でした = 1
;			ENDIF
;			IF VAGINA(MASTER)
;				;男とか触手で犯して何楽しいんだよって人が作ったので女性だけソースが入るよ
;				EXP:(MASTER):Ｃ感経験値 += ABL:(MASTER):Ｃ感 * 4 + 4 + RAND:10
;				EXP:(MASTER):Ｂ感経験値 += ABL:(MASTER):Ｂ感 * 4 + 4 + RAND:10
;				EXP:(MASTER):Ｍ感経験値 += ABL:(MASTER):Ｍ感 * 4 + 4 + RAND:10
;				EXP:(MASTER):Ｖ感経験値 += ABL:(MASTER):Ｖ感 * 20 + 15 + RAND:11
;				EXP:(MASTER):Ａ感経験値 += ABL:(MASTER):Ａ感 * 20 + 15 + RAND:11
;				EXP:(MASTER):欲望経験値 += ABL:(MASTER):欲望 * 3 + 2 + RAND:7
;				EXP:(MASTER):性交経験値 += ABL:(MASTER):性交 * 5 + 15 + RAND:11
;				EXP:(MASTER):触手経験値 += ABL:(MASTER):触手 * 10 + 25 + RAND:11
;				EXP:(MASTER):キス経験 += 2 + RAND:8
;				EXP:(MASTER):口淫経験 += 3 + RAND:10
;				EXP:(MASTER):絶頂経験 += 3 + RAND:10
;				;触手使い同士戦ったので成長する
;				EXP:(MASTER):妖術経験値 += 10 + RAND:20
;				CALL TIGHTNESS_DECREASE_V(LOCAL:5, RAND(5, 15))
;				CALL TIGHTNESS_DECREASE_A(LOCAL:5, RAND(5, 15))
;				;能力上昇メッセ
;				CALL TRAIN_AUTO_ABLUP(LOCAL:5)
;			ENDIF
;			CALL COLORPRINT(@"敗北した%ANAME(MASTER)%に3ターンのクールタイムがつきました", COLOR("警告"))
;			CALL ADD_COOLTIME(MASTER, 3)
;			PRINTFORMW
;			
;			IF VAGINA(MASTER) && (RAND:20 < 1)
;				;負けると1/21の確立で孕む。あなただし孕む時は両方孕む
;				PRINTFORML 
;				PRINTFORM 
;				CALL COLORPRINT(@"%ANAME(LOCAL:5)%は触手の子を胎内に宿しました", COLOR("注意"))
;				PRINTFORML 
;				PRINTFORML 
;				PRINTFORM 
;				CALL COLORPRINT(@"さらに%ANAME(LOCAL:5)%は触手の子を腸内に宿しました", COLOR("注意"))
;				PRINTFORMW 
;				PRINTFORML 
;				TALENT:(LOCAL:5):触手妊娠 = 1
;				CFLAG:(LOCAL:5):触手産卵実行者 = GET_ID(MPLY:0)
;				TALENT:(LOCAL:5):Ａ触手妊娠 = 1
;				CFLAG:(LOCAL:5):56 = GET_ID(MPLY:0)
;			ENDIF
;			
;		ENDIF
;
;	ELSEIF RESULT == 2
;		;そんなことよりお腹が空いたよ
;		PRINTFORML と思ったけどまーいいか、今の血気盛んな幻想郷ならどうにかなるだろう
;		PRINTFORMW そう決断した%ANAME(MASTER)%は、真剣な顔で今日の夕食を考え始めた……
;	ENDIF
;




